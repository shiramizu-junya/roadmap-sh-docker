# P2. Dockerfile — 自分で型を作る

ここから**自分の手で型（イメージ）を作る**フェーズ。全5ステップ + プロジェクト PJ1。

**このフェーズの進め方**（P0・P1 と同じ）

- 1回で**覚えることは1つだけ**。新しい言葉は**4つまで**
- 読んでから確かめるのではなく、**先に予想してから打つ**
- 各回は `❓ この回の問い` から始まる。**前の回の答えから生まれた問い**
- 各回の最後に `📇 まとめカード` がある。**翌日はそれだけ見返せばよい**

計画は [`docs/00-plan.md`](00-plan.md) §4 を参照。

---

## P2-1: Dockerfile を1枚書いてビルドする

**❓ この回の問い**: P1 では**他人が作った型**（`nginx`）を使ってきた。→ **では、自分のアプリを動かす型は、どうやって作るのか？**

**この回で覚えること（1つだけ）**

> **Dockerfile は「型の作り方を書いたレシピ」。`docker build` がレシピから型を作る。**

**重要度**: 🔴 毎日使う — 自分のアプリをコンテナにするたびに書く
**前ステップとの接続**: P1-2 で「イメージは型」と学んだ。**その型を自分で作る**のが P2

### 1-0. このステップの初出トークン

**Docker**: `FROM` / `CMD` / `docker build`

**今回はアプリのコードを入れない。** 土台を選んで、起動時の指示を書くだけ。
⏭️ **後で回収**: 自分のコードを入れるのは **P2-2**。

---

### 1-1. 実践①: レシピを書く

リポジトリのルート（`docs/` ではなく、その1つ上）に **`Dockerfile`** という名前でファイルを作る。**拡張子は付けない。**

**ファイル**: `~/workspace/github.com/shiramizu-junya/roadmap-sh-docker/Dockerfile`

```dockerfile
# P2-1: いちばん短い Dockerfile
# ベースイメージを選んで、起動コマンドを決めるだけ

FROM python:3.12-slim

CMD ["python", "-c", "print('コンテナの中から、こんにちは')"]
```

**中身は2行だけ。** `#` で始まる行はコメントで、動作には関係ない。

| 行 | 何を言っているか |
| --- | --- |
| `FROM python:3.12-slim` | **どの型を土台にするか**。ゼロから作らず、既にあるものの上に足す |
| `CMD [...]` | **コンテナが動き出したとき、何を実行するか** |

> 🧒 **かみくだくと**: **カレーのレシピ**を思い浮かべるとよい。
> `FROM` は「**市販のルウを使う**」という宣言。小麦粉から作らない。
> `CMD` は「**食べるときはご飯にかける**」という最後の指示。
> Dockerfile はレシピなので、**書いただけでは何も起きない**。作る（ビルドする）と初めて型になる。

---

### 1-2. 実践②: レシピから型を作る

**実行する場所は `Dockerfile` があるディレクトリ。** ここを間違えると動かない。

```bash
cd ~/workspace/github.com/shiramizu-junya/roadmap-sh-docker
docker build -t my-first:v1 .
```

```
#1 [internal] load build definition from Dockerfile
#1 transferring dockerfile: 252B done
#1 DONE 0.0s

#2 [internal] load metadata for docker.io/library/python:3.12-slim
#2 DONE 0.0s

#3 [internal] load .dockerignore
#3 transferring context: 2B done
#3 DONE 0.0s

#4 [1/1] FROM docker.io/library/python:3.12-slim@sha256:2f17fc044b57...
#4 CACHED

#5 exporting to image
#5 naming to docker.io/library/my-first:v1 done
#5 DONE 0.0s
```

✅ 検証済み: Docker Desktop 4.81.0 / **0.28秒**

**5つの段階に分かれている。** 番号順に読むとやっていることが分かる。

| # | やっていること |
| --- | --- |
| `#1` | **レシピを読み込む**（252バイト） |
| `#2` | **土台の情報を調べる**（`python:3.12-slim` がどんな型か）。**手元に無ければ、ここで取りに行く** |
| `#3` | **送らないファイルの指定を読む**（⏭️ **P2-2 で回収**） |
| `#4` | **土台を用意する**。`CACHED` ＝ 手元にあったので使い回した |
| `#5` | **型として書き出し、名前を付ける** |

**打ったコマンドの読み方**

| 部分 | 意味 |
| --- | --- |
| `docker build` | レシピから型を作れ |
| `-t my-first:v1` | **作る型に名前を付ける**。`-t` は tag（名札）の略。`:v1` は版の名前 |
| **`.`**（最後の点） | **レシピを探す場所**。`.` は「いまいるディレクトリ」 |

> ⚠️ **最後の `.` を忘れると失敗する**（よくある間違い）
>
> ```bash
> docker build -t test
> → ERROR: docker: 'docker buildx build' requires 1 argument
> ```
> ✅ 検証済み: Docker Desktop 4.81.0
>
> 「引数が1つ必要です」＝ **場所を教えてください**という意味。

#### `docker pull` を先に打つ必要はない

**よくある勘違い**: 「`FROM` に書いた土台は、先に `docker pull` しておかないといけないのでは？」

**要りません。`FROM` がその宣言です。** 手元に無ければ `docker build` が自分で取りに行きます。

実際に、土台が1つも無い状態でビルドだけを打つとこうなる。

```bash
docker images          # mysql:8.0 しか無い状態
docker build -t my-first:v1 .
```

```
#3 [auth] library/python:pull token for registry-1.docker.io
#2 [internal] load metadata for docker.io/library/python:3.12-slim
#2 DONE 4.0s
...
```

```bash
docker images
```

```
my-first:v1   203MB
```

✅ 検証済み: Docker Desktop 4.81.0 / 全体で5秒

**`[auth] ... pull token` の行に注目**。ビルドが**自分でレジストリに認証して、土台を取りに行っている**。`docker pull` を打っていないのに、である。

> 🧒 **かみくだくと**: カレーのレシピに「市販のルウを使う」と書いてあれば、**作る人が買ってきてくれる**。あなたが先に買っておく必要はない。

> ⚠️ **ただし回線が遅いときは別**: この環境では、土台のダウンロードに**5分以上**かかることがある。ビルドの中で取得すると**待ち時間の上限に引っかかって失敗する**ことがあった。
>
> ```
> #2 ERROR: DeadlineExceeded: context deadline exceeded
> ```
>
> このエラーが出たら、**先に `docker pull python:3.12-slim` だけを打って、終わってからビルドする**。これは**仕組み上の必要ではなく、遅い回線への回避策**。

> ⚠️ **`-t` を忘れると、名前のない型ができる**
>
> `docker build .` だけで打つと、ビルドは成功するが `docker images` に **`<none>`** という名前で残る。使いにくいので、**必ず `-t` を付ける**。
> 片づけるには `docker image prune -f`（名前のない型だけ消す）。
> ✅ 検証済み: Docker Desktop 4.81.0

---

### 1-3. 実践③: 作った型を動かす

**🔮 先に予想する**

> 作った型を `docker run` する。`nginx` のときは画面が返ってこなかった。今回はどうなると思うか。

```bash
docker run --rm my-first:v1
```

```
コンテナの中から、こんにちは
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: **1行出して、すぐ終わった。** 画面もすぐ返ってくる。

P1-1 の `nginx` は「アクセスを待つ」のが仕事だから終わらなかった。今回の `print` は**出したら仕事が終わる**ので、コンテナも終わる。**P1-1 で学んだとおり。**

**型の大きさも見てみる。**

```bash
docker images --format 'table {{.Repository}}:{{.Tag}}\t{{.Size}}' | grep -E "my-first|python"
```

```
my-first:v1          203MB
python:3.12-slim     205MB
```

✅ 検証済み: Docker Desktop 4.81.0

**ほぼ同じ。** `CMD` を足しても大きくならない。

> 🧒 **かみくだくと**: `CMD` は**ファイルを足しているのではなく、指示を書き込んでいるだけ**。レシピの最後に「ご飯にかける」とメモを足しても、材料は増えないのと同じ。

---

### 1-4. 🔬 仕組み解剖: レシピ・型・実物の3段階

**なぜを下ってみる。**

```
なぜ Dockerfile とイメージが別なのか
  → Dockerfile はただの文章で、それ自体は動かないから
      → なぜ文章のままではいけないのか
          → 動かすたびに土台をダウンロードして組み立て直すことになるから
              → なぜ1回組み立てておけば済むのか
                  → 組み立てた結果（イメージ）は変わらないと決まっているから（P1-5 で確認済み）
                      → つまり Dockerfile の役目は？
                          → 「同じ型を、いつでも、誰のパソコンでも作り直せる」ようにする設計図
```

**最後の1行が Dockerfile の存在理由。** イメージそのものを配ってもよいが（P1-2 のレジストリ）、**レシピがあれば中身を検証でき、直して作り直せる**。

```
┌────────────────────────────────────┐
│             Dockerfile             │
│       （レシピ・ただの文章）       │
│                                    │
│  FROM python:3.12-slim             │
│  CMD ["python", "-c", "..."]       │
└────────────────────────────────────┘
            │
            │  docker build -t my-first:v1 .
            ↓
┌────────────────────────────────────┐
│           イメージ（型）           │
│            my-first:v1             │
│                                    │
│        読むだけ・変わらない        │
└────────────────────────────────────┘
            │
            │  docker run my-first:v1
            ↓
┌────────────────────────────────────┐
│          コンテナ（実物）          │
│                                    │
│           動いて、終わる           │
└────────────────────────────────────┘
```

**読み方**: 3つは**別のもの**。Dockerfile を書き換えても、既に作った型は変わらない。**もう一度ビルドして初めて新しい型ができる。**

| 部品 | 正式名称 | かみくだくと | いつのことか |
| --- | --- | --- | --- |
| `FROM` | FROM 命令 | **土台にする型を選ぶ** | **ビルド時**。Dockerfile の**1行目**に必ず書く |
| `CMD` | CMD 命令 | **起動したとき何をするかの指示** | **書くのはビルド時、効くのはランタイム時**。ここがずれやすい |
| `docker build` | build コマンド | **レシピから型を作る** | ビルド時 |

**`CMD` は書くときと効くときがずれる** — ここが今回いちばん間違えやすい点。ビルド中に `print` が実行されるわけではない。**イメージの中に「動き出したらこれを実行せよ」とメモが書かれるだけ。** 実行されるのは `docker run` したとき。

**あとから上書きできる**

```bash
docker run --rm my-first:v1 python -c "print('あとから指示したほうが勝つ')"
```

```
あとから指示したほうが勝つ
```

✅ 検証済み: Docker Desktop 4.81.0

`docker run` のうしろにコマンドを書くと、**`CMD` を無視してそちらが実行される**。`CMD` は「**何も言われなければこれ**」という既定値。

**失敗するとどうなるか**: `CMD` に存在しないコマンドを書くと、**ビルドは通るのに `docker run` で初めて落ちる**。ビルド時には実行されないので、書き間違いに気づけない。⏭️ **後で回収**: 起動直後に落ちるコンテナの調べ方は **P2-5**。

**あなたが知っているものでいうと**: **`package.json` の `scripts.start`** に近い。書いてあるだけでは動かず、`npm start` したときに実行される。しかも `npm start -- 別の引数` で上書きできる点まで同じ。

### 1-5. 解説 — なぜこうするか

P1-2 で「イメージは型」と学んだとき、**その型がどこから来たのかは分からないまま**だった。今回それが埋まった。

| | 誰が作ったか | どう作られたか |
| --- | --- | --- |
| `nginx`（P1） | 他人 | **見えなかった** |
| `my-first:v1`（今回） | 自分 | **Dockerfile に全部書いてある** |

> 🧠 **Docker の考え方**: Dockerfile は**イメージの取扱説明書であり、レシピでもある**。中身が分からないイメージは怖いが、Dockerfile が読めれば「何が入っているか」が全部分かる。だから**Dockerfile を読める**ことに価値がある。

### 1-6. ✅ 想起チェック

何も見ずに答えてみる。

1. `docker build -t my-first:v1 .` の最後の `.` は何を指しているか
2. `CMD` に書いた内容は、いつ実行されるか
3. `my-first:v1` が土台の `python:3.12-slim` とほぼ同じ大きさだったのはなぜか

<details><summary>答え</summary>

1. **レシピ（Dockerfile）を探す場所。** `.` は「いまいるディレクトリ」。**省略できない**（忘れると `requires 1 argument` エラー）
2. **`docker run` したとき。** ビルド中には実行されない。イメージの中に「動き出したらこれを実行せよ」とメモが書かれるだけ
3. **`CMD` はファイルを足していないから。** 指示を書き込んだだけなので、大きさはほぼ変わらない

</details>

### 1-7. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `FROM` | 1-4 で仕組み解剖。「土台にする型を選ぶ」。Dockerfile の1行目に必ず書く。**`docker pull` を別途打つ必要はない**ことを 1-2 で実証 |
| `CMD` | 1-4 で仕組み解剖。**書くのはビルド時、効くのはランタイム時**。`docker run` のうしろで上書きできることも確認 |
| `docker build` | 1-2 で使用。`-t`（名札）と `.`（場所）の意味、両方を忘れた場合の挙動も確認 |
| `.dockerignore` | 1-2 のビルド出力 `#3` に登場。⏭️ **P2-2 で回収** |
| `CACHED` | 1-2 のビルド出力 `#4` に登場。⏭️ **P2-4 で回収** |
| `docker image prune` | 1-2 の注意書きで使用。名前のない型を片づける |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌──────────────────────────────────────────────────┐
│ P2-1  Dockerfile を1枚書いてビルドする           │
│                                                  │
│ 覚えること（1つ）                                │
│   Dockerfile は「型の作り方を書いたレシピ」。    │
│   docker build がレシピから型を作る              │
│                                                  │
│ 書いた2行                                        │
│   FROM python:3.12-slim   … 土台にする型         │
│   CMD ["python", "-c", "..."]  … 起動時の指示    │
│                                                  │
│ 打ったコマンド                                   │
│   docker build -t my-first:v1 .                  │
│     -t … 作る型に名前を付ける                    │
│     .  … レシピを探す場所（省略不可）            │
│                                                  │
│ 確かめたこと                                     │
│   ビルドは 0.28 秒（土台が手元にあったので）     │
│   大きさ 203MB ≒ 土台の 205MB                    │
│     → CMD は場所を取らない（ただの指示）         │
│   docker run のうしろに書くと CMD を上書きできる │
│                                                  │
│ 3つの段階                                        │
│   Dockerfile → イメージ → コンテナ               │
│   レシピ      → 型        → 実物                 │
│                                                  │
│ 次の問い                                         │
│   この型には自分のアプリが入っていない。         │
│   どうやって中に入れる？                         │
└──────────────────────────────────────────────────┘
```

---

**❓ 次の問い**: 型は作れた。でもこの型には**自分のアプリが1行も入っていない**。中で動いているのは `print` だけ。**では、自分が書いたコードを、どうやって型の中に入れるのか。**

▶ **次**: `M1: P2 ステップ2`（下へ続く）

---

## P2-2: アプリのコードをイメージに入れる

**❓ この回の問い**: P2-1 の答えは「**Dockerfile はレシピ、`docker build` が型を作る**」だった。でもその型には**自分のコードが1行も入っていない**。→ **では、自分が書いたコードを、どうやって型の中に入れるのか？**

**この回で覚えること（1つだけ）**

> **`COPY` は、ビルド時に手元のファイルをイメージの中に写す。**

**重要度**: 🔴 毎日使う — 自分のアプリを入れる、いちばん基本の命令
**前ステップとの接続**: P2-1 の Dockerfile に**2行足す**だけ

### 2-0. このステップの初出トークン

**Docker**: `COPY` / `WORKDIR` / `.dockerignore`

---

### 2-1. 実践①: 題材アプリを書く

**ここは自分の手で書く。** これから P5 まで、ずっとこのアプリを育てていく。

**作るファイル**: `app/main.py`（リポジトリのルートに `app/` を作り、その中）

```bash
mkdir -p app
```

`app/main.py` に、次の内容を書く。**エディタで作る**（`cat > app/main.py` は、あなたの環境では `cat` が別ツールに置き換わっているため使えない）。

```python
"""最小の FastAPI アプリ。この回では /health だけ。"""

from fastapi import FastAPI

app = FastAPI()


@app.get("/health")
def health():
    """動いているかを確かめるためだけの窓口"""
    return {"status": "ok"}
```

**11行だけ。** `/health` を叩くと `{"status": "ok"}` を返すだけのアプリ。

> 🧒 **かみくだくと**: **アプリの中身は主役ではない**。主役は「これをどうやってコンテナに入れるか」。だから中身はいちばん小さくしてある。
> P3-4 で MySQL につなぐようになり、少しずつ育っていく。

---

### 2-2. 実践②: Dockerfile に2行足す

**ファイル**: リポジトリのルートの `Dockerfile`

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY app/ .

CMD ["python", "-c", "import main; print('読み込めました')"]
```

**P2-1 から足したのは `WORKDIR` と `COPY` の2行だけ。** `CMD` も、動作確認のため一時的に書き換えている。

| 行 | 何をしているか |
| --- | --- |
| `WORKDIR /app` | **イメージの中での作業場所**を `/app` に決める。無ければ作られる |
| `COPY app/ .` | 手元の `app/` の**中身**を、作業場所（`.` ＝ `/app`）へ写す |

> 🧒 **かみくだくと**: `WORKDIR` は「**この部屋で作業します**」という宣言。
> `COPY app/ .` は「**手元の `app` フォルダの中身を、その部屋に運び入れる**」。
> `.` は「いまの作業場所」なので、`WORKDIR` を先に書いておくと `COPY` が短く書ける。

---

### 2-3. 実践③: ビルドして、中に入ったか確かめる

```bash
docker build -t myapp:v1 .
```

**P2-1 のときには無かった行**が出る。

```
#5 [internal] load build context
#5 transferring context: 312B done
```

✅ 検証済み: Docker Desktop 4.81.0

**これが「手元のファイルを送っている」行。** P2-1 では `COPY` が無かったので、この行自体が出なかった。

**🔮 先に予想する**

> ビルドできた。では、`main.py` は**イメージの中のどこに**入っていると思うか。

```bash
docker run --rm myapp:v1 ls -la
docker run --rm myapp:v1 pwd
```

```
total 12
drwxr-xr-x 1 root root 4096 Sep 21 11:41 .
drwxr-xr-x 1 root root 4096 Sep 21 11:41 ..
-rw-r--r-- 1 root root  245 Sep 21 11:41 main.py

/app
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: `/app/main.py` に入っていた。

- `pwd` が `/app` → **`WORKDIR` が効いている**
- `ls` に `main.py` がある → **`COPY` が効いている**
- `app/main.py` ではなく `main.py` → **`app/` の中身が写された**（フォルダごとではない）

> 🐧 **`docker run` のうしろに `ls` と書けるのはなぜか**: P2-1 で学んだとおり、**`CMD` は上書きできる**から。ここでは `CMD` を無視して `ls` を実行している。**中を覗くのに便利な使い方。**

---

### 2-4. 実践④: 動かしてみる（わざと失敗する）

**🔮 先に予想する**

> コードは中に入った。`CMD` も書いてある。**動かしたら成功すると思うか。**

```bash
docker run --rm myapp:v1
```

```
Traceback (most recent call last):
  File "<string>", line 1, in <module>
  File "/app/main.py", line 3, in <module>
    from fastapi import FastAPI
ModuleNotFoundError: No module named 'fastapi'
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: **失敗した。** しかも、**コードが原因ではない**。

エラーをよく読む。`/app/main.py` の3行目で `from fastapi import FastAPI` に失敗している。**ファイルはちゃんとそこにある。読み込まれてもいる。** 足りないのは **`fastapi` という道具**。

> 🧒 **かみくだくと**: **引っ越しで、自分の荷物だけ運んだ状態**。
> P0-3 で「コンテナは上半分（道具）を運ぶ」と学んだ。いま運んだのは**自分のコードだけ**で、**コードが使う道具は運んでいない**。
> 料理のレシピと材料は持ってきたが、**鍋を持ってきていない**。

⏭️ **後で回収**: 道具の入れ方は **P2-3**。

---

### 2-5. 🔬 仕組み解剖: COPY はいつ、何を写すのか

**なぜを下ってみる。**

```
なぜ COPY が必要なのか
  → イメージの中には、手元のファイルが1つも入っていないから
      → なぜ勝手に入らないのか
          → イメージは「どこでも同じものが作れる」ことが取り柄だから
              → 手元のファイルが勝手に入るとどうなるのか
                  → 人によって中身が変わり、「自分のパソコンでは動く」が復活する
                      → だから？
                          → 入れるものを Dockerfile に**明示的に書かせる**
```

**P0-1 で出発した問題に戻ってきた。** `COPY` が面倒なのではなく、**何が入るかを全部書かせることで、再現できるようにしている**。

| 部品 | 正式名称 | かみくだくと | いつのことか |
| --- | --- | --- | --- |
| `COPY` | COPY 命令 | **手元のファイルをイメージに写す** | **ビルド時だけ**。動かしている間は写されない |
| `WORKDIR` | WORKDIR 命令 | **中での作業場所を決める** | ビルド時に決まり、ランタイム時にも効く（`pwd` の結果になる）|

**ビルド時だけ、が大事** — ビルドしたあとに手元の `main.py` を書き換えても、**イメージの中身は変わらない**。P1-5 で学んだとおり、イメージは読むだけで変わらないから。**直したら、もう一度ビルドする。**

```
┌────────────────────────────────┐
│    この Mac（あなたの手元）    │
│                                │
│  roadmap-sh-docker/            │
│    ├── Dockerfile              │
│    ├── app/                    │
│    │     └── main.py   ★       │
│    ├── docs/                   │
│    └── .git/                   │
└────────────────────────────────┘
              │
              │  COPY app/ .  で写す（ビルド時だけ）
              ↓
┌────────────────────────────────┐
│          イメージの中          │
│                                │
│  /app/            ← WORKDIR    │
│    └── main.py   ★ 写された    │
│                                │
│    docs と .git は入らない     │
└────────────────────────────────┘
```

**読み方**: 左の手元にあるもののうち、**`COPY` に書いたものだけ**が右に写る。`docs/` も `.git/` も書いていないので入らない。

**失敗するとどうなるか**: `COPY` に書いたパスが**ビルドコンテキストの外**にあると失敗する。たとえば `COPY ../secret.txt .` は**必ず失敗する**。P2-1 で見たとおり、渡したフォルダの外は見えない。

**あなたが知っているものでいうと**: **対応物なし。** 近いのは `.gitignore` を書いて `git add` するときの判断だが、`COPY` は**書いたものだけが入る**（`git add .` のように「全部」が既定ではない）。

---

### 2-6. 実践⑤: 送られる量は、書き方で変わる

P2-1 で「`.` はフォルダを渡す指定」と学んだ。**では、渡したフォルダは全部送られるのか。**

実験した。`app/`（4KB）のほかに `.git/`（8MB）と `docs/`（4MB）がある状態で、**書き方だけを変えて**ビルドする。

| Dockerfile の書き方 | `.dockerignore` | 送られた量 |
| --- | --- | --- |
| `COPY app/ .` | なし | **56B** |
| `COPY . /app` | なし | **12.59MB** |
| `COPY . /app` | あり（`.git` と `docs` を除外） | **134B** |

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: **必要なものしか送られない。** `COPY app/ .` と書けば、`.git` が 8MB あっても送られない。

> 🧒 **かみくだくと**: 引っ越し業者に「**この部屋の荷物**」ではなく「**この段ボールだけ**」と言えば、それだけ運ぶ。
> 部屋ごと指定（`COPY . /app`）すると全部運ばれるので、**いらない物は「これは運ばないで」と紙に書いて貼る**。その紙が `.dockerignore`。

**`.dockerignore` の書き方**: リポジトリのルートに `.dockerignore` という名前で作り、**入れたくないものを1行ずつ**書く。

```
.git
docs
__pycache__
.venv
```

> 💡 **何のために使うのか**: **ビルドを速くする**のが1つめ。もう1つは**事故を防ぐ**こと。`COPY . /app` と書いたときに `.env`（パスワードが入っている）まで写ってしまうと、**イメージを配った相手に見られる**。⏭️ **後で回収**: 認証情報の扱いは **P4-5**。

**今回は `COPY app/ .` と書いているので、`.dockerignore` は無くても困らない。** ただし P2-3 で依存ファイルを足すと `COPY` が増えるので、そのときに効いてくる。

### 2-7. ✅ 想起チェック

何も見ずに答えてみる。

1. ビルドしたあとに手元の `main.py` を直したら、イメージの中身はどうなるか
2. `COPY app/ .` の `.` は何を指しているか
3. `ModuleNotFoundError` が出たのは、コードが悪いからか

<details><summary>答え</summary>

1. **変わらない。** `COPY` はビルド時にだけ写す。イメージは読むだけで変わらない（P1-5）。**直したら、もう一度ビルドする**
2. **`WORKDIR` で決めた作業場所**（今回は `/app`）。`WORKDIR` を先に書いてあるので短く書ける
3. **違う。** コードは正しく `/app/main.py` に入り、読み込まれてもいる。足りないのは **`fastapi` という道具**。コードと道具は別物

</details>

### 2-8. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `COPY` | 2-5 で仕組み解剖。**ビルド時だけ**写す。ビルドコンテキストの外は写せない |
| `WORKDIR` | 2-5 で仕組み解剖。中での作業場所。`pwd` で確認済み |
| `.dockerignore` | 2-6 で説明。送る量を減らし、事故を防ぐ。⏭️ 認証情報の話は **P4-5** |
| ビルドコンテキスト（P2-1 から） | 2-6 で「書き方で送る量が変わる」ことを実測し、**回収完了** |
| `docker run` のうしろにコマンド | 2-3 で中を覗くのに使用。P2-1 で学んだ上書きの応用 |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌─────────────────────────────────────────────┐
│ P2-2  アプリのコードをイメージに入れる      │
│                                             │
│ 覚えること（1つ）                           │
│   COPY はビルド時に、手元のファイルを       │
│   イメージの中に写す                        │
│                                             │
│ 足した3行                                   │
│   WORKDIR /app   … 中での作業場所を決める   │
│   COPY app/ .    … app/ の中身を作業場所へ  │
│   （CMD は P2-1 から引き続き）              │
│                                             │
│ 確かめたこと                                │
│   docker run ... ls   → main.py が居る      │
│   docker run ... pwd  → /app に居る         │
│   docker run ...      → ModuleNotFoundError │
│     コードは入ったが、道具が無い            │
│                                             │
│ 送られる量は書き方で変わる                  │
│   COPY app/ .            →     56B          │
│   COPY . /app            → 12.59MB          │
│   COPY . /app + ignore   →    134B          │
│                                             │
│ 次の問い                                    │
│   fastapi が無いと言われた。                │
│   どうやってイメージの中に道具を入れる？    │
└─────────────────────────────────────────────┘
```

---

**❓ 次の問い**: コードは入った。でも `fastapi` が無いと言われて動かない。**では、その道具はどうやってイメージの中に入れるのか。** `COPY` で持ち込むのか、それとも別の方法があるのか。

▶ **次**: `M1: P2 ステップ3`（下へ続く）

---

## P2-3: 依存をインストールし、レイヤが積まれるのを見る

**❓ この回の問い**: P2-2 の答えは「**コードは入ったが、道具が無い**」だった。`ModuleNotFoundError: No module named 'fastapi'` で止まっている。→ **では、その道具をどうやってイメージの中に入れるのか？**

**この回で覚えること（1つだけ）**

> **`RUN` はビルド時にコンテナの中でコマンドを実行し、その結果を層として焼き付ける。**

**重要度**: 🔴 毎日使う — 依存を入れる、いちばん基本の命令
**前ステップとの接続**: P2-2 で止まった `ModuleNotFoundError` を、ここで解消する

### 3-0. このステップの初出トークン

**Docker**: `RUN` / `docker history`
**その他**: レイヤ（層）

> ⏭️ **P1-2 から持ち越していた「レイヤ」を、この回で回収する。**

---

### 3-1. 実践①: 何を入れるかの一覧を作る

コンテナの中で `pip install` するには、**何を入れるかの一覧**が要る。手元の `pyproject.toml` から書き出す。

```bash
uv add uvicorn
uv export --no-hashes --no-dev --format requirements-txt -o requirements.txt
```

`uvicorn`（ユビコーン）は **FastAPI アプリを動かすためのサーバ**。FastAPI 単体ではアプリを起動できない。

できた `requirements.txt` は、こういう内容になる（抜粋）。

```
fastapi==0.141.1
uvicorn==0.53.0
```

> 🧒 **かみくだくと**: **買い物リスト**。「この店でこれとこれを買ってきて」という指示書。
> 版まで書いてあるのは、**いつビルドしても同じものが入るようにする**ため。
> 書かないと、半年後にビルドしたとき別の版が入って動かなくなる。

> ⚠️ **存在しない版を書くと、ビルドで止まる**
>
> 実際にこの教材を作るとき、`uvicorn==0.41.1` と書き間違えた。結果はこうなった。
>
> ```
> ERROR: Could not find a version that satisfies the requirement uvicorn==0.41.1
> ERROR: No matching distribution found for uvicorn==0.41.1
> ```
> ✅ 検証済み: Docker Desktop 4.81.0
>
> **手で書かず、`uv export` で書き出す**のが確実。

---

### 3-2. 実践②: Dockerfile に2行足す

**ファイル**: リポジトリのルートの `Dockerfile`

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY app/ .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**変えたのは3か所。**

| 変更 | 何をするか |
| --- | --- |
| `COPY requirements.txt .` を**追加** | 買い物リストを先に入れる |
| `RUN pip install ...` を**追加** | リストどおりに道具を入れる |
| `CMD` を**書き換え** | 動作確認用の `python -c` から、本物のサーバ起動に |

**`CMD` の中身**

```
uvicorn main:app --host 0.0.0.0 --port 8000
        ↑         ↑              ↑
     どのファイルの  どこからの接続を  何番で
     どの変数を     受けるか         待つか
```

> 🐧 **`--host 0.0.0.0` が要る理由**: 既定の `127.0.0.1` は「**自分自身からの接続だけ受ける**」という意味。コンテナの中でそう設定すると、**コンテナの外（あなたのブラウザ）からは届かない**。`0.0.0.0` は「どこからでも受ける」。P1-4 で学んだ NET のカーテンがあるので、これでも外の世界に晒されるわけではない。

---

### 3-3. 実践③: ビルドして、動かす

**🔮 先に予想する**

> P2-2 のビルドは 0.3 秒だった。`pip install` が加わると、どのくらいかかると思うか。

```bash
docker build -t myapp:v3 .
```

```
#7 [3/5] COPY requirements.txt .
#7 DONE 0.0s
#8 [4/5] RUN pip install --no-cache-dir -r requirements.txt
#8 DONE 8.9s
#9 [5/5] COPY app/ .
#9 DONE 0.0s
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: `RUN pip install` だけで **8.9 秒**。他の行は 0.0 秒。**時間がかかるのはここだけ**。

**動かしてみる。**

```bash
docker run -d --name apptest -p 8000:8000 myapp:v3
docker logs apptest
```

```
INFO:     Started server process [1]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

✅ 検証済み: Docker Desktop 4.81.0

```bash
curl http://localhost:8000/health
```

```
{"status":"ok"}
```

✅ 検証済み: Docker Desktop 4.81.0

**自分で書いたアプリが、コンテナの中で動いた。** P2-2 の `ModuleNotFoundError` は消えている。

> 🐧 **ログの `[1]` に注目**: `Started server process [1]` の `1` は PID。**コンテナの中で、あなたのアプリが PID 1 として動いている**（P1-3 で nginx のログに `1#1` と出ていたのと同じ）。

---

### 3-4. 実践④: 層を見る

**🔮 先に予想する**

> イメージは P2-2 の 203MB から、どのくらい増えたと思うか。

```bash
docker history myapp:v3 --format '{{.Size}}  {{.CreatedBy}}'
```

```
0B        CMD ["uvicorn" "main:app" "--host" "0.0.0.0"…
12.3kB    COPY app/ . # buildkit
26.5MB    RUN /bin/sh -c pip install --no-cache-dir -r…
12.3kB    COPY requirements.txt . # buildkit
8.19kB    WORKDIR /app
0B        CMD ["python3"]
16.4kB    RUN /bin/sh -c set -eux;  for src in idle3 p…
44.6MB    RUN /bin/sh -c set -eux;   savedAptMark="$(a…
```

✅ 検証済み: Docker Desktop 4.81.0

`docker history`（ヒストリー）は「**このイメージが、どんな層でできているか**」を出すコマンド。

**答え合わせ**: 203MB → **238MB**。増えた 35MB のうち、**26.5MB が `RUN pip install`**。

```
  ↑ 上が新しい層（あとから積まれた）

┌───────────────────────────────────────────────────────────────┐
│ CMD [uvicorn ...]          0B      起動の指示。場所を取らない │
│ COPY app/ .                12.3kB  自分のコード               │
│ RUN pip install            26.5MB  ★ 道具。ここが重い         │
│ COPY requirements.txt .    12.3kB  何を入れるかの一覧         │
│ WORKDIR /app               8.19kB  作業場所                   │
│ （土台 python:3.12-slim）  203MB   FROM で持ってきた分        │
└───────────────────────────────────────────────────────────────┘

  ↓ 下が古い層（土台）

  合計 238MB = 203MB（土台）+ 26.5MB（道具）+ 残り
```

**読み方**: **上が新しく、下が古い。** `docker history` は新しい順に出る。

**3つ読み取れる。**

1. **`CMD` は 0B** — P2-1 で「`CMD` は場所を取らない」と確認したことの裏付け
2. **重いのは `RUN pip install` だけ** — 26.5MB。他は kB 単位
3. **下のほうにも `RUN` がある** — 44.6MB や 16.4kB の行は、**`python:3.12-slim` を作った人が書いた `RUN`**。イメージは**下まで全部が層**でできている

> 🧒 **かみくだくと**: **地層**と同じ。古い層の上に新しい層が積もる。
> `docker history` は、その断面を見ているようなもの。
> **下のほうの層は、他人が積んだもの**（`FROM` で持ってきた分）。

---

### 3-5. 🔬 仕組み解剖: 1命令が1層を作る

**なぜを下ってみる。**

```
なぜ命令ごとに層に分けるのか
  → 変わっていない層を使い回せるようにするため
      → 使い回すと何が嬉しいのか
          → pip install の 8.9 秒を省ける
              → なぜ省けると分かるのか
                  → その層を作る条件（命令と入力）が前回と同じだから
                      → つまり層とは何か
                          → 「ある命令を実行した結果の差分」に、
                            再利用の判断材料が付いたもの
```

| 部品 | 正式名称 | かみくだくと | いつのことか |
| --- | --- | --- | --- |
| `RUN` | RUN 命令 | **ビルド中に、コンテナの中でコマンドを実行する** | **ビルド時だけ**。実行結果（増えたファイル）が層になる |
| レイヤ | layer | **1命令ぶんの差分** | 1命令 = 1層。積み重なって1つのイメージになる |
| `docker history` | history | **層の断面を見る** | いつでも |

**`RUN` はビルド時だけ、が大事** — `RUN` はイメージを作るときに1回走るだけ。`docker run` のたびに走るわけではない。**だから `pip install` は毎回走らない。**

| 命令 | いつ実行されるか |
| --- | --- |
| `RUN` | **ビルド時に1回**。結果が層に焼き付く |
| `CMD` | **`docker run` のたび**。結果は残らない |

**この2つの違いが、P2 でいちばん間違えやすい。**

**ホストから見るとどう見えるか**: 層は、ホスト（仮想マシン）のディスク上に**別々のフォルダ**として実在する。P1-5 で見た「イメージ + 書き込み層」の下側が、さらに細かく分かれている。

**失敗するとどうなるか**: `RUN` の中のコマンドが失敗すると、**その時点でビルドが止まる**（3-1 の `uvicorn==0.41.1` がそれ）。エラーメッセージには**何行目で止まったか**が出る。`COPY` と違い、`RUN` は**実際にコマンドが走る**ので、失敗の理由もそのまま出る。

**なぜ `--no-cache-dir` を付けるのか**: `pip` は、ダウンロードしたファイルを**あとで使い回すために保存**する。だがコンテナの中では二度と使わないので、**ただイメージが重くなるだけ**。捨てる指示がこれ。

**あなたが知っているものでいうと**: **Git のコミット**に近い。1コミットが1つの差分で、積み重なって履歴になる。`docker history` は `git log` にあたる。ただし Git と違い、**層は書き換えられない**。

---

### 3-6. 実践⑤: 2回目のビルドは速い

**🔮 先に予想する**

> 何も変えずに、もう一度 `docker build` する。`pip install` の 8.9 秒は、また かかると思うか。

```bash
docker build -t myapp:v3 .
```

```
#7 [3/5] COPY requirements.txt .
#7 CACHED
#8 [4/5] RUN pip install --no-cache-dir -r requirements.txt
#8 CACHED
#9 [5/5] COPY app/ .
#9 CACHED
```

✅ 検証済み: Docker Desktop 4.81.0 / **8.9 秒 → 2.2 秒**

**答え合わせ**: 全部 `CACHED`。**`pip install` は走らなかった。**

`CACHED` は「**前に作った層をそのまま使った**」という意味。P2-1 のビルド出力にも出ていた言葉で、**ここでようやく意味がつながる**。

> 🧒 **かみくだくと**: **前に作った料理が冷蔵庫に残っている**ので、作り直さずに出した状態。
> ただし「同じ料理」だと判断できたときだけ。**判断の基準は次回**。

⏭️ **後で回収**: どういうときに `CACHED` にならないのか、**どう書けば効きやすいのか**は **P2-4**。

### 3-7. ✅ 想起チェック

何も見ずに答えてみる。

1. `RUN` と `CMD` は、それぞれいつ実行されるか
2. `docker history` で `CMD` の行が `0B` だったのはなぜか
3. 2回目のビルドで `pip install` が走らなかったのはなぜか

<details><summary>答え</summary>

1. `RUN` は**ビルド時に1回**（結果が層に焼き付く）。`CMD` は**`docker run` のたび**（結果は残らない）
2. **ファイルを足していないから。** `CMD` は「動き出したらこれを実行せよ」という指示を書くだけで、中身が増えない（P2-1 で確認済み）
3. **前に作った層をそのまま使えたから**（`CACHED`）。命令も入力も前回と同じだったため

</details>

### 3-8. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `RUN` | 3-5 で仕組み解剖。**ビルド時だけ**実行され、結果が層になる。`CMD` との違いを表で整理 |
| `docker history` | 3-4 で使用。層の断面を見る。⏭️ **P5-1 で再訪**（どこが重いか探すとき）|
| レイヤ（層） | 3-4・3-5 で仕組み解剖。**P1-2 で名前だけ置いた言葉を回収完了**。1命令 = 1層 |
| `uvicorn` | 3-1 で説明。FastAPI アプリを動かすサーバ |
| `--host 0.0.0.0` | 3-2 で注釈。コンテナの外から届くようにする |
| `--no-cache-dir` | 3-5 で説明。pip の保存分を捨ててイメージを軽くする |
| `CACHED` | 3-6 で意味がつながった。⏭️ **効かせ方は P2-4** |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌────────────────────────────────────────────────────┐
│ P2-3  依存をインストールし、層を見る               │
│                                                    │
│ 覚えること（1つ）                                  │
│   RUN はビルド時にコンテナの中でコマンドを実行し、 │
│   その結果を層として焼き付ける                     │
│                                                    │
│ 足した2行                                          │
│   COPY requirements.txt .       … 一覧を先に入れる │
│   RUN pip install -r ...        … 道具を入れる     │
│                                                    │
│ 層の大きさ（docker history）                       │
│   CMD              0B      指示は場所を取らない    │
│   COPY app/        12.3kB  自分のコード            │
│   RUN pip install  26.5MB  ★ 道具                  │
│   土台             203MB                           │
│   合計             238MB                           │
│                                                    │
│ ビルド時間                                         │
│   1回目  8.9 秒（pip が実際に走る）                │
│   2回目  2.2 秒（全部 CACHED）                     │
│                                                    │
│ 動作確認                                           │
│   curl localhost:8000/health                       │
│   → {"status":"ok"}                                │
│                                                    │
│ 次の問い                                           │
│   main.py を1文字直したら、                        │
│   pip install はまた走る？                         │
└────────────────────────────────────────────────────┘
```

---

**後片づけ**

```bash
docker rm -f apptest
```

**❓ 次の問い**: 何も変えなければ `pip install` は走らないと分かった。**では、`app/main.py` を1文字だけ直したら、`pip install` はまた走るのか。** 走るとしたら、なぜか。

▶ **次**: `M1: P2 ステップ4`（下へ続く）

---

## P2-4: ビルドキャッシュを効かせる

**❓ この回の問い**: P2-3 の答えは「**何も変えなければ全部 `CACHED` で 2.2 秒**」だった。→ **では、`app/main.py` を1文字だけ直したら、`pip install` はまた走るのか？**

**この回で覚えること（1つだけ）**

> **層は上から確認される。1つでも変わると、そこから下は全部作り直される。**

**重要度**: 🔴 毎日使う — 書く順序を1回決めれば、以後ずっとビルドが速い
**前ステップとの接続**: P2-3 で `CACHED` の存在を見た。**効かせ方**が今回

### 4-0. このステップの初出トークン

**Docker**: `--no-cache` / `docker builder prune`

新しいコマンドは2つだけ。**中心は「どう書くか」**であって、新しい命令は出てこない。

---

### 4-1. 実践①: 1文字だけ直して、再ビルドする

いまの Dockerfile は、この順序になっている。

```dockerfile
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app/ .
```

`app/main.py` を1文字だけ変える。返す文字を小文字から大文字にするだけ。

```python
return {"status": "OK"}    # "ok" → "OK"
```

**🔮 先に予想する**

> この状態で再ビルドする。`RUN pip install`（8.9秒かかるやつ）は、また走ると思うか。
> `requirements.txt` は1文字も変えていない。

```bash
docker build -t myapp:v3 .
```

```
#6 [2/5] WORKDIR /app
#6 CACHED
#7 [3/5] COPY requirements.txt .
#7 CACHED
#8 [4/5] RUN pip install --no-cache-dir -r requirements.txt
#8 CACHED          ← 走らなかった
#9 [5/5] COPY app/ .
                   ← ここだけ作り直し
```

✅ 検証済み: Docker Desktop 4.81.0 / **2.5 秒**

**答え合わせ**: **`pip install` は走らなかった。** 作り直されたのは `COPY app/ .` から下だけ。

---

### 4-2. 実践②: 順序を入れ替えると、どうなるか

**同じ3行を、順序だけ変える。**

```dockerfile
COPY app/ .
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
```

**やっていることは完全に同じ。** 最終的にできるイメージも同じ。

**🔮 先に予想する**

> また `main.py` を1文字変えて再ビルドする。今度はどうなると思うか。

```
#6 [2/5] WORKDIR /app
#6 CACHED
#7 [3/5] COPY app/ .
                   ← 変わったので作り直し（当然）
#8 [4/5] COPY requirements.txt .
                   ← 変えていないのに作り直し
#9 [5/5] RUN pip install --no-cache-dir -r requirements.txt
                   ← 走ってしまった
```

✅ 検証済み: Docker Desktop 4.81.0 / **8.0 秒**

**答え合わせ**: **`pip install` が走った。3.2倍の時間差。**

```
┌───────────────────────────────────────┐   ┌───────────────────────────────────────┐
│             ◎ 良い順序                │   │             ✗ 悪い順序                │
│                                       │   │                                       │
│  1 WORKDIR /app          ✅ CACHED    │   │  1 WORKDIR /app          ✅ CACHED    │
│  2 COPY requirements.txt ✅ CACHED    │   │  2 COPY app/ .           🔄 作り直し  │
│  3 RUN pip install       ✅ CACHED  ★ │   │  3 COPY requirements.txt 🔄 道連れ    │
│  4 COPY app/ .           🔄 作り直し  │   │  4 RUN pip install       🔄 道連れ  ★ │
│                                       │   │                                       │
│               2.5 秒                  │   │               8.0 秒                  │
└───────────────────────────────────────┘   └───────────────────────────────────────┘

  ★ requirements.txt は1文字も変えていないのに、
     悪い順序では pip install が走り直している
```

**読み方**: 2つの違いは**書く順序だけ**。できあがるイメージは同じ。それでも再ビルドの時間が **2.5秒 と 8.0秒** に分かれる。

---

### 4-3. 🔬 仕組み解剖: なぜ道連れになるのか

**なぜを下ってみる。**

```
なぜ requirements.txt を変えていないのに pip install が走ったのか
  → その1つ上の層（COPY app/ .）が作り直されたから
      → なぜ上が変わると下も作り直すのか
          → 下の層は「上の層の状態」の上に積まれているから
              → 上が変われば、下も別物になるのでは？
                  → そのとおり。だから使い回せない
                      → つまりキャッシュの判定は？
                          → 「この命令」と「ここまでの積み重ね」が
                            両方とも前回と同じときだけ使い回せる
```

**「ここまでの積み重ね」が同じでないといけない**、が核心。

| 部品 | 正式名称 | かみくだくと | 何をするか |
| --- | --- | --- | --- |
| ビルドキャッシュ | build cache | **前に作った層の保管庫** | 命令と積み重ねが同じなら、作らずに使い回す |
| `--no-cache` | オプション | **保管庫を使わず全部作り直す** | 「おかしい」と思ったときの確認用 |
| `docker builder prune` | prune | **保管庫を空にする** | ディスクが足りなくなったとき |

> 🧒 **かみくだくと**: **積み木**と同じ。
> 下から3段目を差し替えたら、**その上に乗っていた4段目以降は全部落ちる**。
> 4段目自体は無傷でも、**置き直さないといけない**。
> だから **めったに触らないものを下に、よく触るものを上に**積む。

**Dockerfile では「上に書いたものが下の層」になる。** 書く順序と積む順序が逆に感じるので注意。

**ホストから見るとどう見えるか**: キャッシュの実体はディスク上にある。どれだけ使っているかは `docker system df` で見える。

```bash
docker system df
```

```
TYPE            SIZE      RECLAIMABLE
Images          1.518GB   1.28GB (84%)
Build Cache     537.9MB   305.8MB
```

✅ 検証済み: Docker Desktop 4.81.0

**537.9MB。** 放っておくと増え続ける。

**失敗するとどうなるか**: キャッシュが効きすぎて困ることもある。「直したはずなのに反映されない」と感じたら、`--no-cache` を付けて確かめる。

```bash
docker build --no-cache -t myapp:v3 .
```

✅ 検証済み: Docker Desktop 4.81.0 / **11.5 秒**（全部作り直すので最も遅い）

**あなたが知っているものでいうと**: **`node_modules` のキャッシュ**と同じ発想。`package.json` が変わらなければ `npm install` をやり直さない。CI の設定で「`package.json` のハッシュが同じならキャッシュを復元」と書くのと、`COPY requirements.txt` を先に書くのは**まったく同じことをしている**。

---

### 4-4. 解説 — 書く順序の原則

> 🧠 **Docker の考え方**: **変わりにくいものを上に、変わりやすいものを下に。**
> この1行で、Dockerfile の書き方の大半が決まる。

**変わる頻度で並べるとこうなる。**

| 変わる頻度 | 何を書くか | 位置 |
| --- | --- | --- |
| ほぼ変わらない | `FROM`（土台の選択） | **いちばん上** |
| たまに変わる | `COPY requirements.txt` → `RUN pip install` | 上のほう |
| **毎日変わる** | `COPY app/ .`（自分のコード） | **いちばん下** |

**自分のコードは1日に何十回も変わる。** だから**いちばん下**に置く。そうすれば、コードをいくら直しても `pip install` は走らない。

> 🏢 **実務メモ**: この順序は公式のベストプラクティスにも「変更頻度の低い命令を先に置く」として挙げられている。
> 根拠: https://docs.docker.com/build/building/best-practices/

⏭️ **後で回収**: キャッシュを**もっと効かせる**書き方（`RUN --mount=type=cache`）は **P5-1** で扱う。

### 4-5. ✅ 想起チェック

何も見ずに答えてみる。

1. `main.py` を1文字変えただけで `pip install` が走ってしまうのは、どう書いたとき
2. キャッシュが使い回される条件は
3. 「直したのに反映されない」と思ったとき、何を試すか

<details><summary>答え</summary>

1. **`COPY app/ .` を `RUN pip install` より上に書いたとき。** 上の層が作り直されると、下も道連れになる
2. **「その命令」と「ここまでの積み重ね」が、両方とも前回と同じとき。** 片方でも違えば作り直す
3. **`docker build --no-cache`。** キャッシュを一切使わずに作り直して、結果が変わるか見る

</details>

### 4-6. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `--no-cache` | 4-3 で使用。全部作り直す（11.5秒）。「反映されない」ときの確認用 |
| `docker builder prune` | 4-3 で名前と用途。キャッシュを空にする。`docker system df` で使用量を確認 |
| ビルドキャッシュ | 4-3 で仕組み解剖。**P2-1 から出ていた `CACHED` を完全に回収** |
| `RUN --mount=type=cache` | ⏭️ **P5-1 で回収** |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌────────────────────────────────────────────────────┐
│ P2-4  ビルドキャッシュを効かせる                   │
│                                                    │
│ 覚えること（1つ）                                  │
│   層は上から確認される。1つ変わると、              │
│   そこから下は全部作り直される                     │
│                                                    │
│ だから                                             │
│   変わりにくいものを 上 に                         │
│   変わりやすいものを 下 に 書く                    │
│                                                    │
│ 実測（main.py を1文字だけ変えて再ビルド）          │
│   COPY requirements → RUN → COPY app   2.5 秒      │
│   COPY app → COPY requirements → RUN   8.0 秒      │
│   --no-cache を付けた場合             11.5 秒      │
│                                                    │
│ なぜ差が出るか                                     │
│   悪い順序では、先頭の COPY app が変わるので       │
│   その下の pip install まで道連れになる            │
│   requirements.txt は変えていないのに              │
│                                                    │
│ コマンド                                           │
│   docker build --no-cache .   キャッシュを使わない │
│   docker builder prune        キャッシュを捨てる   │
│                                                    │
│ 次の問い                                           │
│   docker stop に 10 秒待たされることがある。       │
│   なぜ？                                           │
└────────────────────────────────────────────────────┘
```

---

**❓ 次の問い**: ビルドは速くなった。ところで P1-1 で `docker stop` を打ったとき、`nginx` は**すぐ**止まった。だが**10秒待たされる**コンテナもある。**同じ `docker stop` なのに、なぜ違いが出るのか。**

▶ **次**: `M1: P2 ステップ5`（下へ続く）

---

## P2-5: 起動コマンドとポート / 止まらないコンテナ

**❓ この回の問い**: P1-1 で `docker stop` を打ったとき、nginx は**すぐ**止まった。ところが**10秒待たされる**コンテナもある。→ **同じ `docker stop` なのに、なぜ違いが出るのか？**

**この回で覚えること（1つだけ）**

> **`docker stop` は「終わってください」とお願いする。PID 1 がそれを受け取れないと、10秒後に強制終了される。**

**重要度**: 🔴 毎日使う — 書き方を1つ間違えるだけで、毎回10秒待たされる
**前ステップとの接続**: P1-4 で「PID 1」を見た。**その PID 1 が誰かによって、止まり方が変わる**

### 5-0. このステップの初出トークン

**Docker**: `EXPOSE` / `ENTRYPOINT`
**Linux**: シグナル（SIGTERM / SIGKILL）

> ★ **シグナルと PID 1 は、`_prompt.md` §4.3 の「厚く扱う5つ」の1つ。** この回だけは深く掘る。

---

### 5-1. 実践①: 10秒待たされる状態を作る

**同じアプリで、`CMD` の書き方だけを変えた Dockerfile を2つ用意する。**

```dockerfile
# ① シェル形式（文字列でそのまま書く）
CMD uvicorn main:app --host 0.0.0.0 --port 8000
```

```dockerfile
# ② exec 形式（配列で書く）
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**やりたいことは同じ。** 見た目の違いは、**引用符と角カッコがあるかどうか**だけ。

**🔮 先に予想する**

> この2つ、動きに違いが出ると思うか。出るとしたら何が違うか。

両方ビルドして起動し、**コンテナの中で PID 1 が誰か**を見る。

```bash
docker exec p-shell cat /proc/1/comm
docker exec p-exec  cat /proc/1/comm
```

```
sh
uvicorn
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: **PID 1 が違う。**

- シェル形式 → **`sh`**（シェル）が PID 1。uvicorn はその子ども
- exec 形式 → **`uvicorn`** が直接 PID 1

> 🐧 **なぜ `sh` が挟まるのか**: シェル形式で書くと、Docker は内部で
> `/bin/sh -c "uvicorn main:app ..."` に変換する。
> `--host` のような引数や `$VAR` を展開するためにシェルが要るから。
> その結果、**シェルが親、アプリが子**という親子関係ができる。

---

### 5-2. 実践②: 止めてみる

**🔮 先に予想する**

> `docker stop` で両方を止める。かかる時間に差が出ると思うか。

```bash
docker stop p-shell
docker stop p-exec
```

```
p-shell → 10.2 秒
p-exec  →  0.3 秒
```

✅ 検証済み: Docker Desktop 4.81.0

**34倍の差。** 終了コードも見る。

```bash
docker inspect p-shell --format '{{.State.ExitCode}}'
docker inspect p-exec  --format '{{.State.ExitCode}}'
```

```
137
0
```

✅ 検証済み: Docker Desktop 4.81.0

**`137` と `0`。** これは**止まり方がまったく違う**ことを意味する。

| 終了コード | 意味 |
| --- | --- |
| `0` | **自分で正常に終了した** |
| `137` | `128 + 9`。**シグナル9（SIGKILL）で強制的に殺された** |
| `143` | `128 + 15`。**シグナル15（SIGTERM）を受けて終わった**（`--init` を付けたときなど。下の訂正を参照）|

**ログを見ると、差が決定的に分かる。**

```bash
docker logs p-shell | tail -3
docker logs p-exec  | tail -3
```

```
--- shell ---
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
                    ← 起動ログで途切れている

--- exec ---
INFO:     Waiting for application shutdown.
INFO:     Application shutdown complete.
INFO:     Finished server process [1]
                    ← ちゃんと片づけて終わっている
```

✅ 検証済み: Docker Desktop 4.81.0

**シェル形式のほうは、終わるときに何も言い残していない。** 途中で殺されたから。

---

### 5-3. 🔬 仕組み解剖: シグナルと PID 1 ★ 厚く扱う

**なぜを下ってみる。**

```
なぜ10秒も待たされたのか
  → docker stop は「終わってください」とお願いするだけだから
      → お願いとは具体的に何か
          → SIGTERM というシグナルを PID 1 に送ること
              → なぜ PID 1 に送るのか
                  → コンテナの代表者だから（P1-4 のカーテンの中で 1 番）
                      → シェル形式では PID 1 が sh だった。sh はどうしたのか
                          → 何もしなかった。sh は受け取ったお願いを
                            子ども（uvicorn）に伝えない
                              → だから？
                                  → 誰も終わらないまま10秒経ち、
                                    Docker が SIGKILL で強制終了した
```

| 部品 | 正式名称 | かみくだくと | 何が起きるか |
| --- | --- | --- | --- |
| シグナル | signal | **プロセスへの短い合図** | 番号で種類が決まる |
| SIGTERM | signal 15 | **「終わってください」というお願い** | 無視できる。片づける時間をもらえる |
| SIGKILL | signal 9 | **「問答無用で死ね」という命令** | **無視できない**。片づけもできない |

> 🧒 **かみくだくと**: **閉店時間**のようなもの。
> **SIGTERM** =「そろそろ閉店です、お会計をお願いします」→ 客は片づけて出ていける
> **SIGKILL** =「強制退去」→ 荷物を置いたまま追い出される
> `docker stop` は、まず**声をかけて 10秒待ち**、出ていかなければ**強制退去**させる。

**`docker stop` が実際にやっていること**

```
1. PID 1 に SIGTERM を送る（「終わってください」）
2. 10秒待つ
3. まだ生きていたら SIGKILL を送る（強制終了）
```

**この「10秒」が、あの待ち時間の正体。**

```
┌──────────────────────────────────┐   ┌────────────────────────────────────┐
│           ✗ シェル形式           │   │           ◎ exec 形式              │
│     CMD uvicorn main:app ...     │   │ CMD ["uvicorn", "main:app", ...]   │
│                                  │   │                                    │
│  PID 1  sh          ← 受け取る   │   │  PID 1  uvicorn     ← 直接受け取る │
│    └ PID 7 uvicorn  ← 伝わらない │   │                                    │
│                                  │   │                                    │
│  docker stop                     │   │  docker stop                       │
│    → sh は無視                   │   │    → uvicorn が受け取る            │
│    → 10秒待つ                    │   │    → 片づけて終了                  │
│    → 強制終了（コード 137）      │   │    → 0.3秒（コード 0）             │
└──────────────────────────────────┘   └────────────────────────────────────┘
```

**読み方**: 左は `sh` が合図を受け取るが、**子どもに伝えない**。右は `uvicorn` が**直接受け取る**。

**なぜ `sh` は伝えないのか**: `/bin/sh -c` で起動したシェルは、**子どもの終了を待つだけ**の役割しか持たない。シグナルを転送する機能は持っていない。**悪意ではなく、そういう作りではないだけ。**

**ビルド時かランタイム時か**: `CMD` の**書き方**を決めるのはビルド時。**その結果が効くのはランタイム時**（止めるとき）。P2-1 で学んだずれが、ここで実害として出る。

**ホストから見るとどう見えるか**: `docker top` で見ると、シェル形式のコンテナには**プロセスが2つ**（`sh` と `uvicorn`）並ぶ。exec 形式なら `uvicorn` だけ。

**失敗するとどうなるか（実務での実害）**

| 場面 | 何が起きるか |
| --- | --- |
| 開発中 | `docker stop` のたびに10秒待つ。1日20回なら**200秒** |
| デプロイ時 | 新しい版に入れ替えるたび、**古いコンテナが10秒居座る** |
| **データを扱うアプリ** | **書きかけのデータが片づけられずに失われる** |

3つめが最も怖い。**SIGKILL では「保存してから終わる」ができない。**

**あなたが知っているものでいうと**: `Ctrl+C`（SIGINT）と**強制終了**（`kill -9`）の違いと同じ。`Ctrl+C` はアプリに片づける機会を与えるが、強制終了は与えない。

> 🏢 **実務メモ**: 公式ドキュメントも exec 形式を推奨している。「`CMD` の shell 形式を使うと、アプリが `/bin/sh -c` の子プロセスになり、シグナルを受け取れない」と明記されている。
> 根拠: https://docs.docker.com/reference/dockerfile/#cmd

---

#### ⚠️ 訂正（2026-09-22）: exec 形式にすれば必ず直る、わけではない

**この節は後から足した。** M2 の宿題を作るために、Python 標準の Web サーバ（`python -m http.server`）で同じことを確かめたところ、**exec 形式で書いても10秒待たされた**。

| | 書き方 | PID 1 | 停止 | 終了コード |
| --- | --- | --- | --- | --- |
| uvicorn | exec 形式 | `uvicorn` | 0.3秒 | `0` |
| **http.server** | **exec 形式** | **`python`** | **10.2秒** | **`137`** |

✅ 検証済み: Docker Desktop 4.81.0

**PID 1 は正しい（`python`）のに、止まらなかった。**

**原因: PID 1 は特別扱いされる。** Linux のカーネルは、**PID 1 には SIGTERM の「既定の動作」（＝終了する）を適用しない**。プログラムが**自分で受け取り方を決めていないシグナル**は、PID 1 のときだけ**無視される**。

各プログラムが受け取り方を決めているシグナルは、`/proc/1/status` の `SigCgt` 行で見える。

```bash
docker exec <名前> grep SigCgt /proc/1/status
```

```
http.server : SigCgt: 0000000000000002   ← SIGINT（2番）だけ
uvicorn     : SigCgt: 0000000000004002   ← SIGINT と SIGTERM（15番 = 0x4000）
```

✅ 検証済み: Docker Desktop 4.81.0

**uvicorn は自分で SIGTERM を受け取る作りなので、PID 1 でも止まれた。** http.server はそうなっていないので、PID 1 になると SIGTERM が素通りする。

> 🧒 **かみくだくと**: ふつうの社員なら「退社してください」と言われれば帰る（既定の動作）。
> でも**社長（PID 1）だけは、自分で決めたルールが無い限り、その声を聞き流す**。会社がいきなり止まると困るので、そういう決まりになっている。
> uvicorn は「退社と言われたら片づけて帰る」と**自分でルールを決めている**社長。http.server は決めていない社長。

**同じ python でも、PID 1 でなければ SIGTERM で普通に終わる**ことも確かめた。問題は python ではなく、**PID 1 という立場**にある。

**直し方: `--init` を付ける**

```bash
docker run --init ...
```

| | PID 1 | python は | 停止 | 終了コード |
| --- | --- | --- | --- | --- |
| `--init` なし | `python` | PID 1 | 10.2秒 | `137` |
| **`--init` あり** | **`docker-init`** | **PID 7** | **0.2秒** | **`143`** |

✅ 検証済み: Docker Desktop 4.81.0

`--init` を付けると、Docker が**小さな「代わりの社長」（`docker-init`）を PID 1 に置く**。この社長は受け取った合図を子どもに伝える専門。python は PID 1 ではなくなるので、SIGTERM の既定の動作が効いて**すぐ終わる**。

終了コード `143` は `128 + 15`（**SIGTERM で終わった**）。`137`（SIGKILL で殺された）とは違い、**お願いを受け入れて終わった**ことを意味する。

**まとめ直すと、`docker stop` がすぐ効く条件は2つとも要る。**

| 条件 | 満たさないと |
| --- | --- |
| ① PID 1 がアプリ本体であること（**exec 形式**） | `sh` が PID 1 になり、合図が伝わらない |
| ② PID 1 が **SIGTERM を自分で受け取る**こと、または **`--init` を付ける**こと | PID 1 の特別扱いで、合図が無視される |

**uvicorn・nginx などのサーバはたいてい②を満たしている。** 自作のスクリプトや、道具として作られたプログラムは満たしていないことが多い。**迷ったら `--init` を付ければよい。**

⏭️ **後で回収**: compose で同じことをする書き方（`init: true`）は **P4**。

### 5-4. 実践③: EXPOSE は穴を開けない

`EXPOSE` という命令がある。**名前から「ポートを開ける」と思いがちだが、違う。**

```dockerfile
EXPOSE 8000
```

**🔮 先に予想する**

> `EXPOSE 8000` と書いたイメージを、**`-p` を付けずに**起動する。外から `/health` に届くと思うか。

```bash
docker run -d --name e1 s:expose
docker ps --filter name=e1 --format '{{.Ports}}'
curl http://localhost:9999/health
```

```
8000/tcp
（届かない）
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: **届かない。** `PORTS` の欄も `8000/tcp` だけで、**外側の番号が書かれていない**（P1-3 で見たのと同じ状態）。

`-p` を付けると変わる。

```bash
docker run -d --name e2 -p 9999:8000 s:expose
docker ps --filter name=e2 --format '{{.Ports}}'
curl http://localhost:9999/health
```

```
0.0.0.0:9999->8000/tcp
{"status":"OK"}
```

✅ 検証済み: Docker Desktop 4.81.0

| 命令 | 何をするか |
| --- | --- |
| `EXPOSE 8000` | **「このイメージは 8000 を使います」という覚え書き**。穴は開かない |
| `-p 9999:8000` | **実際に穴を開ける**（外:中） |

> 🧒 **かみくだくと**: `EXPOSE` は**ドアに貼る紙**。「この部屋は 8000 号室です」と書いてあるだけ。
> **紙を貼っても鍵は開かない。** 鍵を開けるのが `-p`。

> 💡 **では `EXPOSE` は何のために書くのか**: **読む人（と道具）のため**。Dockerfile を見た人が「このアプリは何番を使うのか」を知れる。`docker run -P`（大文字）を使うと、`EXPOSE` した番号を自動で割り当ててくれる。P4 の compose でも参照される。

---

### 5-5. 実践④: ENTRYPOINT で「変えない部分」を固定する

`CMD` は**まるごと上書きできた**（P2-1）。これは便利だが、**うっかり全部消してしまう**こともある。

`ENTRYPOINT` と組み合わせると、**変えない部分と変える部分を分けられる**。

```dockerfile
ENTRYPOINT ["uvicorn", "main:app", "--host", "0.0.0.0"]
CMD ["--port", "8000"]
```

```bash
docker image inspect s:expose --format 'ENTRYPOINT: {{.Config.Entrypoint}}
CMD       : {{.Config.Cmd}}'
```

```
ENTRYPOINT: [uvicorn main:app --host 0.0.0.0]
CMD       : [--port 8000]
```

✅ 検証済み: Docker Desktop 4.81.0

**うしろに引数を書くと、`CMD` の部分だけが差し替わる。**

```bash
docker run -d -p 9998:9000 s:expose --port 9000
```

```
PORTS: 0.0.0.0:9998->9000/tcp
応答: {"status":"OK"}
```

✅ 検証済み: Docker Desktop 4.81.0

**`uvicorn main:app --host 0.0.0.0` は残ったまま、ポートだけが 9000 に変わった。**

| 命令 | 役割 |
| --- | --- |
| `ENTRYPOINT` | **変えない部分**（プログラム本体）。うしろに書いても消えない |
| `CMD` | **既定の引数**。うしろに書くと差し替わる |

> 🧒 **かみくだくと**: **`ENTRYPOINT` が包丁で、`CMD` が切る材料**。
> 「にんじん」と指定すれば材料だけ変わり、**包丁は変わらない**。

> ⚠️ **`ENTRYPOINT` も exec 形式で書く。** シェル形式で書くと、5-1 と同じ問題（`sh` が PID 1 になる）が起きる。

### 5-6. ✅ 想起チェック

何も見ずに答えてみる。

1. `docker stop` で10秒待たされるのは、何が起きているときか
2. 終了コード `137` は何を意味するか
3. `EXPOSE 8000` と書けば、外から届くようになるか

<details><summary>答え</summary>

1. **PID 1 が SIGTERM を受け取っても何もしないとき。** `CMD` をシェル形式で書くと `sh` が PID 1 になり、子どものアプリに合図を伝えないため。10秒経つと Docker が SIGKILL で強制終了する。**exec 形式でも、PID 1 のプログラムが SIGTERM を自分で受け取る作りでなければ同じことが起きる**（PID 1 の特別扱い）。そのときは `--init` を付ける
2. **`128 + 9` で、シグナル9（SIGKILL）による強制終了。** 自分で終了したなら `0` になる
3. **ならない。** `EXPOSE` は「この番号を使う」という覚え書きにすぎない。実際に穴を開けるのは `-p`

</details>

### 5-7. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `EXPOSE` | 5-4 で仕組み解剖。**穴は開かない**ことを実測。書く意味は「読む人と道具のため」 |
| `ENTRYPOINT` | 5-5 で仕組み解剖。変えない部分を固定する。exec 形式で書くこと |
| シグナル（🐧） | 5-3 で**厚く解剖**。SIGTERM（お願い・無視できる）と SIGKILL（命令・無視できない） |
| PID 1（P1-4 から） | 5-3 で**実害として回収完了**。誰が PID 1 かで止まり方が変わる |
| `docker stop`（P1-1 から） | 5-3 で**「お願いしている」の中身を回収完了**。SIGTERM → 10秒 → SIGKILL |
| exec 形式 / シェル形式 | 5-1・5-3 で対比。**配列で書くのが基本** |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌─────────────────────────────────────────────────────┐
│ P2-5  起動コマンドと、止まらないコンテナ            │
│                                                     │
│ 覚えること（1つ）                                   │
│   docker stop は「終わってください」とお願いする。  │
│   PID 1 が受け取れないと 10秒後に強制終了される     │
│                                                     │
│ 実測（同じアプリ・書き方だけ違う）                  │
│   CMD uvicorn ...        PID 1 = sh                 │
│     → 10.2 秒 / 終了コード 137（強制終了）          │
│   CMD ["uvicorn", ...]   PID 1 = uvicorn            │
│     →  0.3 秒 / 終了コード 0（正常終了）            │
│                                                     │
│ ログにも差が出る                                    │
│   シェル形式: 起動ログで途切れる                    │
│   exec 形式 : Application shutdown complete が残る  │
│                                                     │
│ 配列で書く（exec 形式）が基本                       │
│   CMD ["prog", "arg1", "arg2"]                      │
│                                                     │
│ EXPOSE と -p                                        │
│   EXPOSE 8000  … 「8000 を使う」という覚え書き      │
│                   これだけでは外から届かない        │
│   -p 9999:8000 … 実際に穴を開ける（外:中）          │
│                                                     │
│ ENTRYPOINT と CMD                                   │
│   ENTRYPOINT … 変えない部分（プログラム本体）       │
│   CMD        … 既定の引数。うしろに書くと差し替わる │
│                                                     │
│ 終了コードの読み方                                  │
│   0   自分で正常に終了した                          │
│   137 = 128+9 → SIGKILL で強制終了された            │
└─────────────────────────────────────────────────────┘
```

---

## 🎓 P2 修了（プロジェクトを除く）

**他人の型を使う側から、自分で型を作る側になった。**

| 回 | 覚えたこと1つ | 決め手になった実験 |
| --- | --- | --- |
| P2-1 | Dockerfile はレシピ、build が型を作る | 2行だけでイメージができた |
| P2-2 | COPY はビルド時にファイルを写す | 入ったのに ModuleNotFoundError |
| P2-3 | RUN の結果が層になる | pip install だけが 26.5MB |
| P2-4 | 上が変わると下は作り直し | 順序だけで 2.5秒 と 8.0秒 |
| P2-5 | PID 1 が合図を受け取れないと殺される | 書き方だけで 0.3秒 と 10.2秒 |

**P2-4 と P2-5 は、どちらも「書き方を1つ変えるだけ」で結果が数倍変わった。** Docker は、知っているかどうかで差がつく場面が多い。

---

**後片づけ**

```bash
docker rm -f p-shell p-exec e1 e2 e3
```

**❓ 次の問い**: 型は自分で作れるようになった。**では、その型を使って「動くもの」を最後まで自力で組めるか。**

▶ **次**: `MP: PJ1`（下へ続く）

---

# MP: PJ1 — Basic Dockerfile

**出典**: https://roadmap.sh/projects/basic-dockerfile （beginner）

**❓ この回の問い**: P2 で5つのことを学んだ。→ **では、要件だけ渡されたとき、自力で最後まで組めるか？**

> **この回は解答を先に読まない。** 判定基準を見て、自分で組んで、コマンドの出力で合否を確かめる。
> 解答例は末尾の折りたたみにある。**開くのは、自分で組んでから。**

---

## 1. 要件

**課題は2つある。A が出典の課題、B がこの教材の本題。**

### 課題A: 出典どおりのもの（最低ライン）

コンテナを動かすと **`Hello, Captain!`** と表示して終了するイメージを作る。

- ベースイメージは **Alpine Linux** を使う
- 表示は**1つの命令**で行う
- **任意課題**: 名前を差し替えられるようにする。`docker run <イメージ> Junya!` と打つと `Hello, Junya!` になる

### 課題B: 本題（自分のアプリのイメージ化）

`app/main.py`（P2-2 で書いた FastAPI アプリ）を、**そのまま動くイメージ**にする。

- ポートを指定して起動できること
- `docker stop` で**すぐ止まる**こと
- コードを直したとき、**依存のインストールが走り直さない**こと

> 🔄 **素材からの変更**
>
> | 出典 | この教材 |
> | --- | --- |
> | Node.js のアプリ | **FastAPI（Python 3.12）** |
> | `package.json` | **`requirements.txt`** |
> | `npm install` | **`pip install`** |
> | `node server.js` | **`uvicorn main:app`** |
>
> **課題B は出典には無い。** 出典の課題Aは `FROM` と1命令だけで解けてしまい、P2 で学んだ5つのうち1つしか使わないため、本題を足した（`docs/00-plan.md` §6.1）。

> 🔓 **教材用の簡略化**: 出典は `alpine:latest` を指定しているが、**この教材では版を固定する**（例: `alpine:3.22`）。
> **`latest` は「そのとき最新のもの」という意味で、中身が変わる。** 半年後に同じ Dockerfile をビルドすると別のものができてしまう。
> **本番では**必ず版を固定する。
> 根拠: https://docs.docker.com/build/building/best-practices/

---

## 2. このフェーズで学んだどれを使うか

| P2 の回 | 学んだこと | 課題A | 課題B |
| --- | --- | --- | --- |
| **P2-1** | `FROM` と `CMD`、`docker build -t` | ⭕ 使う | ⭕ 使う |
| **P2-2** | `COPY` / `WORKDIR` / `.dockerignore` | **✗ 不要**（コピーするものが無い） | ⭕ 使う |
| **P2-3** | `RUN` で依存を入れる、層になる | **✗ 不要**（依存が無い） | ⭕ 使う |
| **P2-4** | 書く順序でキャッシュの効きが変わる | **✗ 不要**（1命令だけ） | ⭕ 使う |
| **P2-5** | `ENTRYPOINT` / `EXPOSE` / exec 形式 | ⭕ 任意課題で使う | ⭕ 使う |

**課題A で使うのは P2-1 と P2-5 だけ。** これが「出典のままでは軽すぎる」と判断した理由。

---

## 3. 判定基準

**すべてコマンドの出力で確かめられる。** 講評は要らない。

### 課題A

| # | 打つコマンド | 合格の条件 |
| --- | --- | --- |
| A-1 | `docker run --rm <イメージ>` | **`Hello, Captain!`** と出る |
| A-2 | `docker run --rm <イメージ> Junya!` | **`Hello, Junya!`** と出る（任意課題）|
| A-3 | `docker images <イメージ> --format '{{.Size}}'` | **20MB 未満** |
| A-4 | `grep FROM Dockerfile` | **`latest` を使っていない**（版が固定されている）|

### 課題B

| # | 打つコマンド | 合格の条件 |
| --- | --- | --- |
| B-1 | `curl http://localhost:9990/health` | **`{"status":"ok"}`** が返る（`-p 9990:8000` で起動した場合）|
| B-2 | `docker exec <名前> cat /proc/1/comm` | **`uvicorn`** と出る（`sh` ではない）|
| B-3 | `docker stop <名前>` の所要時間 | **1秒未満**（10秒待たされない）|
| B-4 | `docker inspect <名前> --format '{{.State.ExitCode}}'` | **`0`**（`137` ではない）|
| B-5 | `app/main.py` を1行変えて再ビルド | **3秒未満**で終わり、`pip install` が **`CACHED`** |

**B-3 と B-5 が、この課題の山場。** 動くだけなら誰でもできるが、**この2つは書き方を知らないと通らない**。

> 💡 **時間の測り方**: `time docker stop <名前>` と打つと、かかった秒数が出る。

---

## 4. 詰まったときの確認順

**推測しない。上から順に打つ。** どの層で止まっているかが必ず分かる。

| 順 | 打つコマンド | 分かること |
| --- | --- | --- |
| **1** | `docker ps -a` | **そもそも起動したか**。`Exited` なら落ちている。`Created` なら起動に失敗している |
| **2** | `docker logs <名前>` | **何を言い残したか**。落ちた理由はたいていここに書いてある |
| **3** | `docker inspect <名前> --format '{{.Config.Cmd}} {{.Config.Entrypoint}}'` | **起動コマンドが意図どおりか**。書き間違いがここで見つかる |
| **4** | `docker run --rm <イメージ> ls -la` | **ファイルが入っているか**。`CMD` を上書きして中を覗く |
| **5** | `docker exec <名前> cat /proc/1/comm` | **PID 1 が誰か**。`sh` なら P2-5 の問題 |

**よくある詰まり方と、それがどの段で見つかるか**

| 症状 | 見つかる段 | 原因 |
| --- | --- | --- |
| すぐ `Exited` になる | 1 → 2 | 起動コマンドの書き間違い、依存不足 |
| `curl` がつながらない | 1（`PORTS` 欄） | `-p` の付け忘れ、または `--host 0.0.0.0` の書き忘れ |
| `docker stop` が10秒 | 5 | `CMD` をシェル形式で書いている。**exec 形式でも起きるなら**、PID 1 が SIGTERM を受け取らない作り（`grep SigCgt /proc/1/status` で確認）→ `--init` を付ける（P2-5 の訂正）|
| 毎回 `pip install` が走る | — | `COPY app/` を `RUN` より上に書いている |

---

## 5. ヒント

**設定ファイルの中身は書かない。考え方だけ。**

**課題A について**

- 「表示して終了する」なら、待ち続ける必要はない。P1-1 で `nginx` が終わらなかった理由を思い出す
- 任意課題（名前の差し替え）は、**変えない部分と変える部分を分ける**と解ける。P2-5 でやった
- Alpine の版は [Docker Hub の alpine](https://hub.docker.com/_/alpine) で確認できる

**課題B について**

- 必要な行は **P2-5 までに全部出てきている**。新しい命令は要らない
- 書く順序は P2-4 の原則どおり。**変わりにくいものを上に**
- `docker stop` を速くしたいなら、**PID 1 を誰にするか**を考える（P2-5）
- `requirements.txt` は手で書かない。`uv export` で書き出す（P2-3）

**詰まったら**

- **4章の確認順を上から打つ。** 推測で直そうとしない
- どうしても分からない行があれば、**その行を扱った回の 🔬 仕組み解剖だけ**読み返す

---

<details><summary>解答例（自力で組んでから開く）</summary>

### 課題A

**ファイル**: 任意のディレクトリに `Dockerfile`

```dockerfile
FROM alpine:3.22

ENTRYPOINT ["echo", "Hello,"]
CMD ["Captain!"]
```

```bash
docker build -t pj1:hello .
docker run --rm pj1:hello
docker run --rm pj1:hello "Junya!"
docker images pj1:hello --format '{{.Size}}'
```

```
Hello, Captain!
Hello, Junya!
13.3MB
```

✅ 検証済み: Docker Desktop 4.81.0 / alpine:3.22

**考え方**: `ENTRYPOINT` に「変えない部分」（`echo Hello,`）、`CMD` に「差し替えたい部分」（`Captain!`）を置く。うしろに引数を書くと `CMD` だけが入れ替わるので、任意課題も同時に解ける（P2-5）。

**`echo` は出力したら終わる**ので、コンテナもすぐ終了する。待ち続ける必要がない（P1-1）。

---

### 課題B

**ファイル**: リポジトリのルートの `Dockerfile`

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ .

EXPOSE 8000

ENTRYPOINT ["uvicorn", "main:app", "--host", "0.0.0.0"]
CMD ["--port", "8000"]
```

**判定基準の確認**

```bash
docker build -t pj1:app .
docker run -d --name pj1t -p 9990:8000 pj1:app
curl http://localhost:9990/health
docker exec pj1t cat /proc/1/comm
time docker stop pj1t
docker inspect pj1t --format '{{.State.ExitCode}}'
docker images pj1:app --format '{{.Size}}'
```

```
{"status":"ok"}
uvicorn
0.4 秒
0
238MB
```

✅ 検証済み: Docker Desktop 4.81.0

**キャッシュの確認**（`app/main.py` を1行変えてから）

```bash
docker build -t pj1:app .
```

```
#8 CACHED          ← pip install は走らなかった
所要: 2.4 秒
```

✅ 検証済み: Docker Desktop 4.81.0

**なぜこの形なのか**

| 行 | 理由 |
| --- | --- |
| `COPY requirements.txt` を先、`COPY app/` を後 | **P2-4**。コードを直しても `pip install` が走らない |
| `ENTRYPOINT` を配列（exec 形式）で書く | **P2-5**。`uvicorn` が PID 1 になり、`docker stop` が 0.4秒で終わる |
| `--host 0.0.0.0` | **P2-3**。既定の `127.0.0.1` では、コンテナの外から届かない |
| `EXPOSE 8000` | **P2-5**。穴は開かないが、読む人と道具のために書く |
| `CMD ["--port", "8000"]` | **P2-5**。既定のポート。`docker run ... --port 9000` で差し替えられる |

**書かなくても動くが、書いたほうがよいもの**

- `EXPOSE` — 無くても `-p` があれば動く。だが **Dockerfile を読む人が困る**
- `.dockerignore` — 今回は `COPY app/` と絞っているので効果は小さい。だが `COPY .` に変えた瞬間に効いてくる

</details>

---

## 6. 終わったら記録する

`projects/basic-dockerfile/README.md` に、次を書き残す。

- **要件**（この章の1をそのまま貼ってよい）
- **判定基準**（3章のチェックを ✅ で埋める）
- **詰まった記録** — これがいちばん価値がある

| 症状 | 切り分けに使ったコマンド | 原因 |
| --- | --- | --- |
| （例）`docker stop` が10秒 | `docker exec ... cat /proc/1/comm` → `sh` | `CMD` をシェル形式で書いていた |

**詰まらなかったなら「詰まらなかった」と書く。** それも記録。
P3 以降で似た症状が出たとき、**ここを見返すのがいちばん速い**。

---

**❓ 次の問い**: イメージは作れた。`docker stop` も速い。**だが、このアプリにはまだデータが無い。** P1-5 で「コンテナに書いたものは消える」と学んだ。**データベースをつなぐなら、そのデータはどこに置けばいいのか。**

▶ **次**: `M2: P2`（下へ続く）

---

# M2: P2 フェーズ末パック

**P3 に進む前に、手で思い出す回。** 新しいことは学ばない。

---

## ✍️ ブランクページ再現

> **これは翌日にやること。** 読んだ直後だと、短期の記憶が答えてしまう。
> 一晩あけて書けるかどうかが、身についたかの判定になる。

### やり方

1. **ルートの `Dockerfile` を閉じる。** 開いたままにしない
2. **新しいファイル**に書く。いまの `Dockerfile` は消さない
   リポジトリのルートに `Dockerfile.blank` という名前で作る（`.gitignore` 済みなのでコミットされない）
   ```bash
   touch Dockerfile.blank
   ```
   > ⚠️ **`/tmp` に置いてはいけない**（最初はこう案内していたが誤りだった）。
   > Dockerfile がビルドコンテキストの外にあると、Docker は**その Dockerfile が入っているフォルダごと**調べに行く。
   > `/tmp` には他のアプリのファイルも置かれていて、読めないものがあると次のエラーで止まる。
   > ```
   > failed to read dockerfile: error from sender: failed to xattr /private/tmp/devio_semaphore_logi_hpp_...: permission denied
   > ```
   > ✅ 検証済み: Docker Desktop 4.81.0（Logicool Options+ が作ったフォルダで再現）
3. 何も見ずに、**課題B（FastAPI アプリのイメージ化）の Dockerfile** を白紙から書く
4. 書けなかった行・迷った行に **印**（`# ?`）を付ける
5. 書き終えたら、本物と見比べる
   ```bash
   diff Dockerfile.blank Dockerfile
   ```
6. **印を付けた行の「🔬 仕組み解剖」だけ**読み返す。本文は読まなくてよい

### 書くべきもの（見出しだけ。中身は書かない）

```
① 土台を選ぶ
② 作業場所を決める
③ 依存の一覧を入れる
④ 依存をインストールする
⑤ 自分のコードを入れる
⑥ 使うポートを書き残す
⑦ 起動のしかたを決める
```

**③〜⑤の順番を間違えないこと**が最大の山場。ここを逆にすると P2-4 の「8.0 秒」側になる。

### 書いたものを動かして確かめる

見比べるだけでなく、**自分で書いたほうをビルドして動かす**。

```bash
docker build -f Dockerfile.blank -t blank .
docker run -d --name blankt -p 9993:8000 blank
curl http://localhost:9993/health
time docker stop blankt
docker rm blankt
```

`-f` は「**この名前の Dockerfile を使え**」という指定。いつもは `Dockerfile` という名前を自動で探すが、別の名前のものを使うときに付ける。

### 印を付けた行の読み返し先

| 迷ったもの | 読み返す場所 |
| --- | --- |
| `FROM` の書き方・版 | P2-1 の 1-4 |
| `WORKDIR` と `COPY` の `.` | P2-2 の 2-5 |
| `RUN` と `CMD` の違い | P2-3 の 3-5 |
| ③〜⑤ の順番 | P2-4 の 4-3 |
| `CMD` を配列で書く理由 | P2-5 の 5-3 と、その訂正 |
| `--host 0.0.0.0` | P2-3 の 3-2 |

---

## 宿題

P2 には**プロジェクト回（PJ1）がある**ので、**Lv1 と Lv2 だけ**を出す（`_prompt.md` §8.2）。Lv3 の役目は PJ1 が担った。
**判定基準はすべてコマンドの出力で書いてある。**

---

### 🟢 Lv1: 別のアプリを型にする

**課題**: 自分の HTML を1枚配信するイメージを作る。サーバは **Python に最初から入っているもの**を使う（`pip install` 不要）。

**準備**（どこでもよい。リポジトリの外に作るのがおすすめ）

```bash
mkdir -p ~/tmp/lv1/site
echo '<h1>hello from container</h1>' > ~/tmp/lv1/site/index.html
cd ~/tmp/lv1
```

**ヒント**: Python には `python -m http.server <ポート>` と打つだけで、**いまいる場所のファイルを配信する**機能がある。

**判定基準**

| # | 打つコマンド | こう出れば合格 |
| --- | --- | --- |
| 1 | `curl http://localhost:9991/` | `<h1>hello from container</h1>` |
| 2 | `docker exec <名前> cat /proc/1/comm` | `python`（`sh` ではない）|
| 3 | `docker images <イメージ> --format '{{.Size}}'` | **210MB 未満**（`pip install` していないので土台とほぼ同じ）|
| 4 | `docker inspect <名前> --format '{{.Config.ExposedPorts}}'` | `map[8000/tcp:{}]` のように**ポートが書かれている** |

<details><summary>解答例</summary>

```dockerfile
FROM python:3.12-slim
WORKDIR /site
COPY site/ .
EXPOSE 8000
CMD ["python", "-m", "http.server", "8000"]
```

```bash
docker build -t lv1 .
docker run -d --name lv1t -p 9991:8000 lv1
curl http://localhost:9991/
docker exec lv1t cat /proc/1/comm
docker images lv1 --format '{{.Size}}'
```

```
<h1>hello from container</h1>
python
203MB
```

✅ 検証済み: Docker Desktop 4.81.0

</details>

**Lv1 が終わったら、コンテナを消さずに Lv2 へ進む。**

---

### 🟡 Lv2: 止まらないコンテナを直す

**Lv1 のコンテナを止めてみる。**

```bash
time docker stop lv1t
docker inspect lv1t --format '{{.State.ExitCode}}'
```

**たぶん10秒待たされて、終了コードは `137` になる。** exec 形式で書き、PID 1 も `python` なのに、である。

**課題**: 次の2つを満たすようにする。

1. `docker stop` が **1秒未満**で終わる
2. 終了コードが **`137` 以外**になる

そのうえで、**なぜ10秒かかったのかを1〜2文で説明する**。

**判定基準**

| # | 確かめること | 合格の条件 |
| --- | --- | --- |
| 1 | `time docker stop <名前>` | **1秒未満** |
| 2 | `docker inspect <名前> --format '{{.State.ExitCode}}'` | **`137` ではない** |
| 3 | 直す前に `docker exec <名前> grep SigCgt /proc/1/status` を打った | 出力を**自分で読んで**説明に使っている |
| 4 | 説明 | 「PID 1」と「SIGTERM」という言葉を使って、**なぜ無視されたか**が書けている |

> 💡 **ヒント**: 直し方は `Dockerfile` を書き換える方法と、`docker run` に何かを足す方法がある。P2-5 の訂正の節を読む。

<details><summary>解答例</summary>

**直し方**: `docker run` に `--init` を付ける。

```bash
docker rm lv1t
docker run -d --init --name lv1t -p 9991:8000 lv1
docker exec lv1t cat /proc/1/comm
time docker stop lv1t
docker inspect lv1t --format '{{.State.ExitCode}}'
```

```
docker-init
0.2 秒
143
```

✅ 検証済み: Docker Desktop 4.81.0

**直す前の `SigCgt`**

```
SigCgt: 0000000000000002
```

✅ 検証済み: Docker Desktop 4.81.0

**説明の例**: `http.server` は SIGTERM（15番）の受け取り方を自分で決めていない（`SigCgt` に 0x4000 が無い）。PID 1 のプログラムには SIGTERM の既定の動作が適用されないので、`docker stop` の合図が無視され、10秒後に SIGKILL で殺された。`--init` を付けると PID 1 が `docker-init` になり、python は PID 1 でなくなるので、SIGTERM の既定の動作で終わる。

**終了コード 143** は `128 + 15`。**SIGTERM で終わった**ことを意味する。`137`（SIGKILL で殺された）ではない。

**別解**: Dockerfile に `ENTRYPOINT` で init を入れる方法、アプリ側で SIGTERM を受け取るように書く方法もある。どちらでも判定基準を満たせばよい。

</details>

**片づけ**

```bash
docker rm -f lv1t
```

---

## ✅ P3 に進んでよいかの目安

| | 状態 |
| --- | --- |
| ブランクページ再現 | 7つのうち **6つ以上**書けた。**③〜⑤の順番は必ず正しい** |
| 書いたものを動かす | `/health` が返り、`docker stop` が1秒未満 |
| Lv1 | 判定基準4つがすべて ✅ |
| Lv2 | 1秒未満になり、**理由を自分の言葉で書けた** |

**Lv2 の説明が書けていれば、P2 でいちばん難しいところ（PID 1 とシグナル）が身についている。**

---

**❓ 次の問い**: 型は作れた。止め方も分かった。でも P1-5 で見たとおり、**コンテナを消すと中に書いたものは消える**。**では、データベースのように消えては困るものは、どこに置けばいいのか。**

▶ **次**: `M1: P3 ステップ1` — **volume でデータを残す**（P3 開始。コンテナを消してもデータが残る場所を作る）
