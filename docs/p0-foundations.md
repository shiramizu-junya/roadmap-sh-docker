# P0. 基礎 — なぜコンテナか / アーキテクチャ / 用語

手を動かしながら、言葉と全体像を作るフェーズ。全6ステップ。

**このフェーズの進め方**

- 1回で**覚えることは1つだけ**。新しい言葉は**4つまで**
- 読んでから確かめるのではなく、**先に予想してから打つ**
- 各回の最後に `📇 まとめカード` がある。**翌日はそれだけ見返せばよい**
- **各回は「❓ この回の問い」から始まる。** その問いは**前の回の答えから生まれたもの**。
  「なぜ動かないのか」→「では下とは何か」→「では何を運ぶのか」→…と、**1本のなぜなぜで最後までつながっている**

計画は [`docs/00-plan.md`](00-plan.md) §4 を参照。

---

## P0-1: 「動かない」はどこで起きるのか

**この回で覚えること（1つだけ）**

> **アプリが動くかどうかは、コードではなく「コードの下」で決まる。**

**重要度**: ⚪ 背景知識 — ここが分からないと、この先ずっと「なぜこんな面倒なことをするのか」が分からないまま進むことになる
**❓ この回の問い**: **なぜ「自分のパソコンでは動くのに、人のパソコンでは動かない」が起きるのか？**
**前ステップとの接続**: 最初のステップ。準備は [`docs/00-plan.md`](00-plan.md) §2 で済んでいる

### 1-0. このステップの初出トークン

**Docker**: `コンテナ`（**名前を置くだけ**。中身は P0-3 で扱う）
**Linux**: シンボリックリンク（ショートカット）
**その他**: 土台（この教材で使うたとえ）

新しい言葉は**3つだけ**。そのかわり、手を8回動かす。

---

### 1-1. 実践①: 自分のパソコンの「下」を見る

あなたが書いたコードは、**何かの上に乗って動いている**。その「何か」を3つ見てみる。

打つ場所はどこでもよい。ターミナルを開いて、1行ずつ打つ。

**🔮 先に予想する**（打つ前に、頭の中で決めてから進む）

> この3つの値は、**隣の席の人のパソコンでも同じ**だと思うか？

```bash
sw_vers -productVersion
```

```
26.6.2
```

これは **macOS のバージョン**。

```bash
uname -m
```

```
arm64
```

これは **CPU の種類**。CPU はパソコンの中で計算をする部品。

```bash
python3 --version
```

```
Python 3.14.3
```

これは **Python のバージョン**。

✅ 検証済み: macOS 26.6.2 / arm64 / Python 3.14.3

**答え合わせ**: この3つは**人によって違う**。Windows の人もいる。CPU が違う人もいる。Python を入れていない人もいる。

そして大事なのはここ。

> **この3つは、あなたのコードのどこにも書かれていない。**

書かれていないので、Git にも入らない。GitHub にも上がらない。**なのに、これが違うと動かない。** これが「自分のパソコンでは動くのに」の正体。

---

### 1-2. 実践②: `python3` は、どこにいる誰なのか

さっき `python3 --version` と打った。では **その `python3` はどこにあるファイル**なのか。

**🔮 先に予想する**

> `python3` は1台のパソコンに**1つだけ**だと思うか？ それとも**いくつもある**と思うか？

```bash
which python3
```

```
/Users/（あなたの名前）/.local/bin/python3
```

✅ 検証済み: macOS 26.6.2

`which`（ウィッチ）は「**その名前で打ったとき、どのファイルが動くのか**」を教えてくれるコマンド。

> 🐧 **`~` について**: 上の `/Users/（あなたの名前）` の部分は、**自分の持ち物を置く場所**（ホームディレクトリ）。`~`（チルダ）と書いても同じ意味になるので、この先は `~/.local/bin/python3` と短く書く。

#### 「あれ、uv で入れた Python はどこ？」

ここで詰まる人が多い。**`uv` や `pyenv` で Python を入れたはずなのに、出てきた場所に `uv` の文字が無い。**

結論から言うと、**この `~/.local/bin/python3` こそが uv の入れた Python**。ただし**そう見えない**。理由を確かめる。

```bash
/bin/ls -la ~/.local/bin/python3
```

```
lrwxr-xr-x  1 ... ~/.local/bin/python3 -> ~/.local/share/uv/python/cpython-3.14-macos-aarch64-none/bin/python3.14
```

✅ 検証済み: uv 0.10.9 / macOS 26.6.2

**矢印（`->`）に注目**。このファイルは**本体ではなく、本体への道しるべ**だった。矢印の先を見ると `share/uv/python/...` と書いてある。**uv の置き場所**。

> 🧒 **かみくだくと**: これを**シンボリックリンク**（ショートカット）と呼ぶ。
> パソコンの画面に置くアプリのショートカットと同じ。**アイコンは机の上にあるが、アプリ本体は別の場所**にある。
> `~/.local/bin/python3` は机の上のショートカット。本体は uv の倉庫の中。

なぜこうするのか。**uv は Python を自分の倉庫に入れるが、それだけでは `python3` と打っても見つけてもらえない**。そこで、みんなが見に行く場所（`~/.local/bin`）にショートカットを置いて、「`python3` と打ったらこっちだよ」と案内している。

#### もう1つの `python3` を見つける

**🔮 もう一度予想する**

> `/usr/bin/python3` にも Python がある。**さっきの 3.14.3 と同じ版**だと思うか？

```bash
python3 --version
/usr/bin/python3 --version
```

```
Python 3.14.3
Python 3.9.6
```

✅ 検証済み: macOS 26.6.2

**答え合わせ**: **別物だった。** 1台のパソコンに **Python が2つ**入っていて、**版が5つも違う**。

| 打ち方 | 実体 | 版 | 誰が入れたか |
| --- | --- | --- | --- |
| `python3` | `~/.local/bin/python3`（ショートカット） | **3.14.3** | **自分**（uv で） |
| `/usr/bin/python3` | 本体 | **3.9.6** | **macOS に最初から** |

> 🧒 **かみくだくと**: 「田中さん」と呼んだとき、**どの田中さんが返事をするか**は、その場にいる人によって変わる。`python3` も同じ。**名前が同じでも、中身は別人**。

ここで P0-1 の要点につながる。**あなたのコードは「`python3` という名前の誰か」に頼って動いている。** その誰かが 3.9.6 だったら、3.14 向けに書いたコードは動かないかもしれない。**そしてコードのどこにも「3.14 を使え」とは書かれていない。**

### 1-3. 🔬 仕組み解剖: 「同じコードなのに動かない」はなぜ起きるのか

| 部品 | 正式名称 | かみくだくと | いつ効いてくるか |
| --- | --- | --- | --- |
| コンテナ | container | **必要なものを全部そろえて、ひとまとめにして運ぶ方法** | **動かすとき**。今は名前だけ覚えればよい。中身は P0-3 |

**何が起きているのか**

あなたのコードが動くには、**コードの外にあるもの**が必要になる。

```
あなたが書いたコード        ← Git で運べる
─────────────────────
その下にあるもの            ← Git で運べない
  ・Python のバージョン
  ・Python が使う部品
  ・OS の種類
  ・CPU の種類
```

**Git で運べるのは上だけ。** 下は運べない。だから引っ越し先（他の人のパソコン、サーバ）で下が違うと、上が同じでも動かない。

**あなたが知っているものでいうと**: Python の `venv` に近い。`venv` は「この作業のときは、この Python の部品を使う」と決める道具。**目的は同じ**で、「他の環境と混ざらないようにする」こと。ただし `venv` が面倒を見るのは**Python の部品だけ**。OS の種類も CPU の種類も面倒を見てくれない。

**失敗するとどうなるか**: エラーの出方がバラバラになる。`ModuleNotFoundError`（部品が見つからない）で止まることもあれば、動くけれど結果が違う、ということもある。**どこが原因か分かりにくい**のが、この問題のいちばん厄介なところ。

**なぜコンテナという方法が生まれたのか**: 「下も一緒に運んでしまえばいい」という発想。運ぶ範囲をどこまでにするかで方法が分かれる。その比較は P0-3 で扱う。

---

### 1-4. この教材ぜんたいで使うたとえ: 引っ越し

この先ずっと、**引っ越し**にたとえて話す。たとえを途中で変えないので、これだけ覚えればよい。

| たとえ | 本当は何のことか |
| --- | --- |
| **家** | パソコン |
| **土台**（家を支えている部分） | 1-1 で見た「コードの下」。**正体は P0-2 で明かす** |
| **荷物** | あなたが書いたコードと、それが使う道具 |

引っ越しで困るのは、**荷物だけ運んで、土台のことを忘れたとき**。

> 🧒 **かみくだくと**: 冷蔵庫（荷物）だけ持っていっても、引っ越し先に**コンセントが無ければ**使えない。コードも同じ。**コードだけ運んでも、下がそろっていなければ動かない。**

### 1-5. 🐧 Linux注

> 🐧 **`which`**: 「その名前で打つと、どのファイルが動くか」を調べるコマンド。**同じ名前のファイルが複数あるとき**に、どれが選ばれているかが分かる。

> 🐧 **`uname`**: パソコン自身の情報を出すコマンド。`-m` を付けると CPU の種類が出る。`m` は machine（マシン）の頭文字。

### 1-6. 解説 — なぜこうするか

プログラムが動かないとき、つい**自分の書いたコードを疑う**。でも今日見たとおり、**コードの下にも原因になる場所が4つ**あった。

この先 Docker を学ぶと、コマンドや書き方がたくさん出てくる。それらは全部、**「下も一緒に運ぶ」という1つの目的**のためにある。迷ったらここに戻ってくればよい。

> 🧠 **Docker の考え方**: Docker がやっているのは、**「コードの下」を持ち運べる形にすること**。難しい機能に見えるものも、たどればここに行き着く。

### 1-7. 🔮 予測 → 動作確認

最後にもう1回、手を動かす。**今日の話の証拠**を自分の目で見る。

**🔮 先に予想する**

1. さっき `which python3` で出てきたファイルを、**USB メモリにコピーして Windows のパソコンに持っていったら動く**と思うか
2. そのファイルの中身には、**どのパソコン向けに作られたか**が書いてあると思うか

**動作確認**

```bash
file "$(which python3)"
```

`file`（ファイル）は「**このファイルは何者か**」を調べるコマンド。

**こう出るはず**

```
/Users/（あなたの名前）/.local/bin/python3: Mach-O 64-bit executable arm64
```

✅ 検証済み: macOS 26.6.2 / arm64

**読み方**

| 出てきた文字 | 意味 |
| --- | --- |
| `Mach-O`（マックオー） | **macOS 専用の形式**で作られている、という印 |
| `arm64` | **この CPU の種類向け**に作られている、という印 |

**答え合わせ**: このファイルは **macOS 用**で、しかも **arm64 用**。Windows に持っていっても動かないし、CPU の種類が違うパソコンでも動かない。

つまり `python3` は「どこでも動く魔法のファイル」ではなく、**このパソコン向けに作られた1つのファイル**だった。だから**一緒に運ばないといけない**。

### 1-8. ✅ 想起チェック

何も見ずに答えてみる。

1. 「自分のパソコンでは動くのに」が起きるのは、何が違うから？
2. あなたのパソコンに `python3` はいくつあったか。版はどうだったか
3. `file` で見た `Mach-O arm64` は、何を表していたか？

<details><summary>答え</summary>

1. **コードの下にあるもの**が違うから。OS の種類・CPU の種類・Python のバージョン・Python が使う部品。**どれもコードには書かれていない**
2. **2つ。** `python3` と打つと uv の **3.14.3**、`/usr/bin/python3` は macOS の **3.9.6**。名前が同じでも中身は別人
3. そのファイルが **macOS 用**（Mach-O）で、**この CPU の種類用**（arm64）に作られていること。**他のパソコンでは動かない**

</details>

### 1-9. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `コンテナ` | 1-3 で名前だけ置いた。⏭️ **P0-3 で回収**（何を運ぶ方法なのかを、他の方法と比べて明らかにする）|
| 土台（たとえ） | 1-4 で導入。**正体の名前は明かしていない**。⏭️ **P0-2 で回収** |
| シンボリックリンク | 1-2 で説明。「ショートカット」。`/bin/ls -la` の矢印（`->`）で見分ける。P3-1 でボリュームを見るときに再訪 |
| `which`（🐧） | 1-2 と 1-5 で注釈。P0-2 でも使う |
| `uname`（🐧） | 1-5 で注釈。P0-2・P0-4 で繰り返し使う |
| `file` | 1-7 で使用。この教材ではここだけ。CPU の種類の話は P5-2 で再訪 |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌──────────────────────────────────────────────┐
│ P0-1  「動かない」はどこで起きるのか         │
│                                              │
│ 覚えること（1つ）                            │
│   動くかどうかは、コードではなく             │
│   「コードの下」で決まる                     │
│                                              │
│ 下にあるもの（Git で運べない）               │
│   OS の種類 / CPU の種類                     │
│   Python の版 / Python が使う部品            │
│                                              │
│ 自分で確かめたこと                           │
│   file で見たら Mach-O arm64 だった          │
│   → このパソコン専用に作られていた           │
│                                              │
│ たとえ                                       │
│   家=パソコン 土台=コードの下 荷物=コード    │
│   冷蔵庫だけ運んでもコンセントが無ければ×    │
│                                              │
│ 次回の宿題（頭の中だけでよい）               │
│   「土台」の本当の名前は何だろう？           │
└──────────────────────────────────────────────┘
```

---

▶ **次**: `M1: P0 ステップ2`（下へ続く）

---

## P0-2: OS とカーネルは違う

**この回で覚えること（1つだけ）**

> **カーネルは OS の一部。OS の「下半分」のこと。**

**重要度**: ⚪ 背景知識 — ここを分けておかないと、この先「OS が入っている」「カーネルを共有する」という話が全部ごちゃ混ぜになる
**❓ この回の問い**: P0-1 の答えは「**コードの下**で決まる」だった。→ **では、その「下」とは何なのか？**
**前ステップとの接続**: P0-1 で「土台」と呼んで**正体を伏せていたもの**の名前を、この回で明かす

### 2-0. このステップの初出トークン

**Linux**: `OS` / `カーネル` / 上半分・下半分（この教材での呼び分け）

新しい言葉は**3つだけ**。手は3回動かす。

---

### 2-1. 実践①: OS の「上半分」を数える

パソコンには、最初からたくさんのコマンドが入っている。`cat` や `cp` のような小さな道具たち。**それがどこにいて、何個あるのか**を見る。

**🔮 先に予想する**

> あなたのパソコンで**打てるコマンドは全部で何個**だと思うか。10個か、100個か、1000個か。

まず `/bin`（ビン）という場所を見る。

```bash
/bin/ls /bin
```

```
[
bash
cat
chmod
cp
csh
（…以下つづく）
```

```bash
/bin/ls /bin | wc -l
```

```
37
```

✅ 検証済み: macOS 26.6.2

> 🐧 **なぜ `/bin/ls` と場所から書くのか**: あなたのパソコンでは `ls` が別の新しいツール（eza）に置き換えられている。`cat` も別のツール（bat）になっている。`/bin/ls` と**場所から書く**と、もともと入っているほうが確実に動く。`wc -l`（ダブリューシー・エル）は**行数を数える**コマンド。

**「37個しかないの？」と思ったはず。そのとおり。**

`/bin` は**置き場所の1つ**でしかない。コマンドは**あちこちに分かれて置いてある**。全部数えてみる。

```bash
for d in /bin /sbin /usr/bin /usr/sbin /usr/local/bin; do
  printf '%-16s %5s 個\n' "$d" "$(/bin/ls $d | wc -l | tr -d ' ')"
done
```

```
/bin                37 個
/sbin               74 個
/usr/bin           924 個
/usr/sbin          228 個
/usr/local/bin      12 個
```

✅ 検証済み: macOS 26.6.2

**答え合わせ**: `/usr/bin` だけで **924個**。パソコン全体で打てるコマンドを数えると **1,428個**だった。

**なぜ場所が分かれているのか**

| 置き場所 | 何が入っているか | 個数 |
| --- | --- | --- |
| `/bin` | **パソコンが起動するのに要る、いちばん基本の道具**。ここが壊れると何もできない | 37 |
| `/sbin` | **管理者向け**の道具（`s` は system）。ディスクを直すなど、危ないもの | 74 |
| `/usr/bin` | **普段づかいの道具の大半**。ここがいちばん多い | 924 |
| `/usr/sbin` | 管理者向けのうち、起動には要らないもの | 228 |
| `/usr/local/bin` | **あとから自分で入れたもの** | 12 |

> 🧒 **かみくだくと**: 家の道具にたとえると、`/bin` は**非常持ち出し袋**。無いと困るものだけが少し入っている。`/usr/bin` は**物置**で、普段づかいの道具がぎっしり。`/usr/local/bin` は**あとから買って足した棚**。

**OS の「上半分」はどれか**: `/bin` `/sbin` `/usr/bin` `/usr/sbin` の **1,263個**。これは **macOS に最初から付いてきたもの**。`/usr/local/bin` は自分で入れたので、上半分には含めない。

P0-1 で `which python3` を打ったのを思い出してほしい。**打ったコマンドがどの場所から来たか**は、`which` で分かる。

```bash
which docker
```

```
/usr/local/bin/docker
```

✅ 検証済み: Docker Desktop 4.81.0

`docker` は `/usr/local/bin` にある。つまり **OS に最初から入っていたのではなく、あとから入れたもの**。当然だが、自分の目で確かめられる。

---

### 2-2. 実践②: 版番号が食い違う（今回の山場）

ここが今日いちばん大事なところ。**2つの番号を並べて見る。**

**🔮 先に予想する**

> このパソコンの macOS は **26.6.2** という版。
> では `uname -r`（カーネルの版を出すコマンド）を打つと、**26 で始まる番号**が出ると思うか。

```bash
sw_vers -productVersion
```

```
26.6.2
```

```bash
uname -r
```

```
25.6.0
```

✅ 検証済み: macOS 26.6.2 / Darwin 25.6.0

**答え合わせ**: **食い違った。** macOS は `26`、カーネルは `25`。

#### 「本当にカーネルの番号なのか」を確かめる

ここで当然の疑問が出る。**`uname -r` が出した `25.6.0` は、本当にカーネルの番号なのか。** 言われたから信じるのではなく、確かめる。

```bash
uname -v
```

```
Darwin Kernel Version 25.6.0: Fri Jul 31 19:18:48 PDT 2026; root:xnu-12377.161.14~5/RELEASE_ARM64_T6020
```

✅ 検証済み: macOS 26.6.2

**出力の中に答えが書いてある。**

| 出てきた文字 | 意味 |
| --- | --- |
| `Darwin` | このカーネルの**名前** |
| **`Kernel Version 25.6.0`** | **「カーネルの版は 25.6.0」と、そのまま書いてある** |
| `xnu-12377...` | `xnu`（エックスエヌユー）は、**このカーネルの本当の製品名** |
| `ARM64` | この CPU の種類向けに作られている |

`uname -r` が出した `25.6.0` と、ここに書かれた `Kernel Version 25.6.0` は**同じ番号**。これで「`uname -r` はカーネルの版を出している」ことが確かめられた。

> 🧒 **かみくだくと**: `uname`（ユーネーム）は「**このパソコンの名前を教えて**」というコマンド。ただし答えるのは**カーネルについて**。
> `-s` は system（システム）＝ **名前**を出す → `Darwin`
> `-r` は release（リリース）＝ **版**を出す → `25.6.0`
> `-v` は version（バージョン）＝ **くわしい説明**を出す → 上の長い行

> ⚠️ **注意**: `uname -a` と打つと全部まとめて出るが、**パソコンの名前（持ち主の名前が入っていることが多い）も一緒に出る**。ネットの質問サイトに貼るときは `-a` を避けて、`-s` や `-r` を使うほうがよい。

> 🧒 **かみくだくと**: 番号が別々についているということは、**別々に数えているもの**ということ。同じものなら番号は1つで足りる。
> 学校でいうと「**学年**」と「**クラス**」のようなもの。2つあるのは、それぞれ別のことを表しているから。

これが「**OS とカーネルは別物**」の動かぬ証拠。ただし**まったく無関係ではない**。次でその関係をはっきりさせる。

---

### 2-3. 🔬 仕組み解剖: OS とカーネルの関係

| 部品 | 正式名称 | かみくだくと | 何をしているか |
| --- | --- | --- | --- |
| カーネル | kernel | パソコンの**管理人さん** | **動いているあいだずっと**。「ファイルを開きたい」「メモリがほしい」というアプリの頼みを、代わりに実行する。1台に**1つだけ**動いている |
| OS | Operating System（基本ソフト） | **カーネル ＋ その周りの道具**をまとめた呼び名 | カーネルだけでは人は使えない。コマンドや画面を出す部分を足して、はじめて「使える1式」になる |

**この教材での呼び分け**

```
┌────────────────────────────────────┐
│          OS（基本ソフト）          │
│                                    │
│      上半分 … コマンドや道具       │
│    （/bin の37個、道具箱など）     │
│                                    │
│         下半分 … カーネル          │
│       （18MB のファイル1つ）       │
└────────────────────────────────────┘
```

**読み方**: `OS` という大きな箱の中に、`カーネル` が入っている。**並んでいるのではなく、入れ子**。2-1 で数えた37個のコマンドは上半分、2-2 で版番号を見たカーネルは下半分。

**あなたが知っているものでいうと**: 対応するものは無い。ただし考え方が近いのは **Web アプリの「フロント」と「サーバ」**。利用者が触るのは上（画面）だが、実際に仕事をしているのは下（サーバ）。上だけあっても下が無ければ何もできない。**カーネルは、全部の頼みごとを実際に処理している下の部分**。

**失敗するとどうなるか**: カーネルが止まると、上半分がどれだけ無事でも**パソコン全体が止まる**。Mac で画面が固まって何も反応しなくなるのがこれ。逆に上半分のコマンドが1つ壊れても、そのコマンドが使えなくなるだけで済む。

**なぜ分かれているのか**: 管理人さんの仕事（メモリの割り当て、ファイルへの書き込み）は**間違えると全部が壊れる**。だから小さくまとめて、めったに触らない場所に置く。一方、コマンドは**壊れても影響が小さい**ので、気軽に足したり消したりできる場所に置く。**壊れたときの被害の大きさで分けてある。**

---

### 2-4. たとえの更新 — 土台の正体

P0-1 で「土台」と呼んで名前を伏せていたもの。その正体が今回の**カーネル**。

| たとえ | 本当は何のことか | いつ分かったか |
| --- | --- | --- |
| **家** | パソコン | P0-1 |
| **土台・水道・電気** | **カーネル**（OS の下半分） | **← 今回** |
| **内装・家具・道具** | **OS の上半分**（/bin の37個など） | **← 今回** |
| **家一式**（土台 + 内装） | **OS** | **← 今回** |
| **荷物** | あなたのコード | P0-1 |

> 🧒 **かみくだくと**: 「家」と言ったとき、ふつうは**土台も内装もひっくるめて**「家」と呼ぶ。`OS` も同じで、**カーネルも道具もひっくるめた呼び名**。
> 「土台」だけを指したいときに使う言葉が `カーネル`。

### 2-5. 🐧 Linux注

> 🐧 **`/bin`**: コマンドのファイルが置いてある場所の**1つ**。`bin` は binary（バイナリ＝コンピュータが読む形式）の略。**人が読む文書ではなく、機械が実行するファイル**が入っている、という意味。置き場所は他にも `/usr/bin` などがある（2-1）。

### 2-6. 解説 — なぜこうするか

なぜわざわざ上半分と下半分を分けて覚えるのか。**この先ずっと、この2つが別々に動くから。**

- コンテナは **上半分を運び、下半分は借りる**（P0-3）
- Mac の中に Linux がある、というのは **下半分が2つある**という意味（P0-4）
- イメージの中に「OS のファイルが入っている」と言うとき、入っているのは **上半分だけ**

ここを1つの言葉（「OS」）で済ませていると、上の3つが全部ぼやける。

> 🧠 **Docker の考え方**: Docker を理解するうえでいちばん効く区別が、**上半分と下半分**。「OS が入っている」と聞いたら、**上か下か**を必ず確かめるとよい。

### 2-7. 🔮 予測 → 動作確認

最後にもう1回だけ手を動かす。**カーネルの本体を、目で見る。**

**🔮 先に予想する**

1. カーネルは 2-1 で見た `/bin` の中に、コマンドの1つとして入っていると思うか
2. カーネルのファイルの大きさは、`cat` のような普通のコマンドと**同じくらい**だと思うか

```bash
/bin/ls /bin | grep -i kernel
```

```
（何も出ない）
```

`grep`（グレップ）は**探すコマンド**。何も出ないということは、**`/bin` の中に kernel という名前は無い**。

```bash
/bin/ls -lh /System/Library/Kernels/kernel
```

```
-rwxr-xr-x  1 root  wheel    18M  8月 13 11:51 /System/Library/Kernels/kernel
```

```bash
/bin/ls -lh /bin/cat
```

```
-rwxr-xr-x  1 root  wheel   116K  8月 13 11:51 /bin/cat
```

✅ 検証済み: macOS 26.6.2

**答え合わせ**

| | 場所 | 大きさ |
| --- | --- | --- |
| `cat`（上半分のコマンド） | `/bin` | **116KB** |
| カーネル（下半分） | `/System/Library/Kernels` | **18MB** |

2つとも**予想は外れ**だったはず。カーネルは `/bin` にいないし、大きさも **約160倍**ちがう。

**置き場所が違うのは、役割が違うから。** 上半分のコマンドは人が使う道具なので、すぐ手の届く `/bin` にある。カーネルは**パソコンが起動するときに読み込まれて、あとはずっと裏で動き続ける**ので、人が触らない奥のほうに置いてある。

### 2-8. ✅ 想起チェック

何も見ずに答えてみる。

1. OS とカーネルの関係を1文で言うと？
2. macOS の版が `26.6.2` なのにカーネルの版が `25.6.0` だったことは、何を意味していたか？
3. あなたのパソコンで打てるコマンドは、だいたい何個だったか。そのうち「OS の上半分」はどれか

<details><summary>答え</summary>

1. **カーネルは OS の一部。** OS = カーネル（下半分）+ その周りの道具（上半分）
2. **別々に番号を付けている = 別のもの**だという証拠。同じものなら番号は1つで足りる
3. **全部で約1,400個。** そのうち `/bin` `/sbin` `/usr/bin` `/usr/sbin` の **1,263個**が OS に最初から付いてきた「上半分」。`/usr/local/bin` のものは自分で入れたので含めない。下半分（カーネル）は `/System/Library/Kernels` に1つだけ

</details>

### 2-9. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `カーネル` | 2-3 で仕組み解剖。「パソコンの管理人さん」＝ OS の下半分。2-2 で `uname -v` により版を、2-7 で本体のファイルを確認した。P0-4 で**もう1つのカーネル**が登場する |
| `uname`（🐧） | 2-2 で注釈。`-s` 名前 / `-r` 版 / `-v` くわしい説明。P0-4 で Linux 側と並べて使う |
| `OS` | 2-3 で仕組み解剖。「カーネル + その周りの道具」 |
| 上半分・下半分 | 2-3 の図で確定。**この教材ではこの呼び方で通す** |
| 土台（P0-1 から持ち越し） | ✅ **回収完了**。正体は**カーネル**だった（2-4）|
| `/bin`（🐧） | 2-1 と 2-5 で注釈。**置き場所は1つではない**（`/usr/bin` に924個ある）。P2-3 でイメージの中身を見るときに再訪 |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌───────────────────────────────────────────┐
│ P0-2  OS とカーネルは違う                 │
│                                           │
│ 覚えること（1つ）                         │
│   カーネルは OS の一部。OS の下半分。     │
│                                           │
│ OS の中身                                 │
│   上半分 = コマンドや道具                 │
│     /bin 37 /sbin 74 /usr/bin 924         │
│     /usr/sbin 228  … 計 1,263 個          │
│     （/bin だけではない。置き場所が複数） │
│   下半分 = カーネル（管理人さん）         │
│                                           │
│ 自分で確かめたこと                        │
│   macOS の版   26.6.2                     │
│   カーネルの版 25.6.0  ← 食い違う         │
│   uname -v に Kernel Version と明記       │
│   → 別々に番号を付けている = 別物         │
│                                           │
│   カーネルの本体は1つのファイル           │
│   /System/Library/Kernels/kernel  18MB    │
│   /bin/cat は 116KB … 桁が違う            │
│                                           │
│ たとえ（土台の正体が判明）                │
│   土台 = カーネル                         │
│   内装・家具 = OS の上半分                │
└───────────────────────────────────────────┘
```

---

▶ **次**: `M1: P0 ステップ3`（下へ続く）

---

## P0-3: 3つの運び方

**この回で覚えること（1つだけ）**

> **コンテナは OS の「上半分」だけを運び、「下半分」（カーネル）は借りる。**

**重要度**: ⚪ 背景知識 — 「借りる」と決めたことから、Docker の長所も短所も全部出てくる
**❓ この回の問い**: P0-2 の答えは「下 ＝ **OS の上半分と下半分**」だった。→ **では、そのうちどこまでを運べばよいのか？**
**前ステップとの接続**: P0-2 で OS を上半分と下半分に分けた。**その分け方を使って、3つの運び方を比べる**

### 3-0. このステップの初出トークン

**その他**: `仮想マシン` / `ゲスト OS` / `仮想化ソフト`

新しい言葉は**3つだけ**。どれも「運び方」の話で出てくる。

---

### 3-1. 実践①: 運ぶ荷物の重さを測る

「上半分を運ぶ」と言われても、それが**どのくらいの重さ**なのかが分からないと実感がわかない。測る。

**🔮 先に予想する**

> P0-2 で数えた OS の上半分（コマンド1,263個）は、**全部で何 MB** だと思うか。1MB か、10MB か、100MB 以上か。

```bash
du -sh /bin /sbin /usr/bin /usr/sbin
```

```
4.1M	/bin
2.0M	/sbin
 80M	/usr/bin
 46M	/usr/sbin
```

```bash
du -sc /bin /sbin /usr/bin /usr/sbin | tail -1
```

```
272488	total
```

✅ 検証済み: macOS 26.6.2

**答え合わせ**: 合計 `272488` KB ＝ **約 266MB**。`/usr/bin` だけで 80MB ある。

> 🐧 **`du`**: disk usage（ディスクの使用量）の略。**そのフォルダが何 MB か**を出すコマンド。`-s` は合計だけ出す、`-h` は人が読みやすい単位（M や G）で出す、`-c` は総合計も出す。

**この 266MB が「運ぶ荷物」の実物大。** コンテナが「OS の上半分を運ぶ」と言うとき、運んでいるのはこのくらいの量のファイル。

> 🧒 **かみくだくと**: 引っ越しで**段ボール266箱ぶん**の荷物を運ぶイメージ。少なくはない。だからこそ、**下半分まで運ばずに済むこと**が効いてくる。

---

### 3-2. 実践②: 図を自分で埋める

ここからは手ではなく頭を動かす。**紙に書くか、頭の中で埋めてから**答えを開く。

いま、1台のパソコンで**アプリを2つ**動かしたい。コンテナを使う場合、①と②には何が入るか。

```
┌──────────────────┐ ┌──────────────────┐
│     アプリ A     │ │     アプリ B     │
│       ① ?        │ │       ① ?        │
└──────────────────┘ └──────────────────┘
┌───────────────────────────────────────┐
│                  ② ?                  │
│               パソコン                │
└───────────────────────────────────────┘
```

**ヒント**: 使える言葉は「OS の上半分」「カーネル（OS の下半分）」の2つだけ。同じ言葉を2回使ってもよい。

<details><summary>答え</summary>

```
┌──────────────────┐ ┌──────────────────┐
│     アプリ A     │ │     アプリ B     │
│   OS の上半分    │ │   OS の上半分    │
└──────────────────┘ └──────────────────┘
┌───────────────────────────────────────┐
│        カーネル（OS の下半分）        │
│               パソコン                │
└───────────────────────────────────────┘
```

**① は「OS の上半分」**。アプリごとに1つずつ持つ。
**② は「カーネル（OS の下半分）」**。**1つだけ**で、2つのアプリが共同で使う。

間違えやすいのは②を空欄のままにすること。**カーネルは必ず要る**。持っていかないだけで、**借りて使っている**。

</details>

---

### 3-3. 🔬 仕組み解剖: 3つの運び方

**運ぶ範囲が違うだけ**で、3つに分かれる。

**その1: bare metal（ベアメタル）** — 何も運ばない

```
┌──────────────────┐ ┌──────────────────┐
│     アプリ A     │ │     アプリ B     │
└──────────────────┘ └──────────────────┘
┌───────────────────────────────────────┐
│        OS の上半分（1組だけ）         │
│        カーネル（OS の下半分）        │
│               パソコン                │
└───────────────────────────────────────┘
```

パソコンに直接インストールする、いちばん普通のやり方。**上半分は1組しかない**ので、アプリ A と B が**同じ Python を取り合う**ことになる。P0-1 で見た「名前は同じでも中身は別人」の問題が起きるのはここ。

**その2: 仮想マシン** — 上半分も下半分も丸ごと運ぶ

```
┌──────────────────┐ ┌──────────────────┐
│     アプリ A     │ │     アプリ B     │
│   OS の上半分    │ │   OS の上半分    │
│     カーネル     │ │     カーネル     │
└──────────────────┘ └──────────────────┘
    ↑ この箱1つ1つが「仮想マシン」
┌───────────────────────────────────────┐
│             仮想化ソフト              │
│        カーネル（OS の下半分）        │
│               パソコン                │
└───────────────────────────────────────┘
    ↑ 上の箱を作る係。箱そのものではない
```

| 部品 | 正式名称 | かみくだくと | 何をしているか |
| --- | --- | --- | --- |
| 仮想マシン | virtual machine（VM） | ソフトで作った**にせもののパソコン** | 本物のパソコンの中に、もう1台パソコンがあるように見せかける |
| ゲスト OS | guest OS | **お客さんとして持ち込まれた OS** | 上半分も下半分も、まるごと1式。**カーネルがもう1つ増える** |
| 仮想化ソフト | hypervisor（ハイパーバイザ） | にせもののパソコンを**作る係** | 本物の部品（CPU・メモリ）を分け与える |

> 🧒 **かみくだくと**: **ゲスト**は「**お客さん**」。旅館の**主人**と**泊まり客**と同じ関係。もとからあるパソコン側が主人で、持ち込まれた OS がお客さん。「人の家に、自分の家を丸ごと持ち込んで住んでいる」状態。

#### 「仮想マシン」と「仮想化ソフト」は何が違うのか

名前が似ていて混ざりやすい。**作る側**と**作られた物**の違い。

| | 何か | いくつあるか |
| --- | --- | --- |
| **仮想化ソフト** | にせものパソコンを**作って面倒を見る係**。本物の CPU やメモリを切り分けて配る | **1つ**（係は1人） |
| **仮想マシン** | その係が**作ったにせものパソコンそのもの** | **いくつでも**（作った数だけ） |

> 🧒 **かみくだくと**: **大家さん**と**部屋**の関係。
> **仮想化ソフト = 大家さん**。1つの建物を区切って部屋を作り、電気や水道を各部屋に配る。
> **仮想マシン = できた部屋**。住む場所はこっち。部屋は何室でも作れるが、**大家さんは1人**。
> 「大家さんに住む」とは言わない。同じように、**仮想化ソフトの上でアプリは動かない**。動くのは仮想マシンの中。

**実物を見る**

Docker Desktop は、この**大家さん役**をやっている。作らせた部屋（仮想マシン）を表示してみる。

```bash
docker info --format '仮想マシンの名前: {{.Name}}
割り当て CPU  : {{.NCPU}} 個
割り当てメモリ: {{.MemTotal}} バイト'
```

```
仮想マシンの名前: docker-desktop
割り当て CPU  : 12 個
割り当てメモリ: 8321232896 バイト
```

Mac 本体のほうも見てみる。

```bash
sysctl -n hw.ncpu hw.memsize
```

```
12
68719476736
```

✅ 検証済み: Docker Desktop 4.81.0 / macOS 26.6.2

**答え合わせ**

| | CPU | メモリ |
| --- | --- | --- |
| **Mac 本体**（建物ぜんたい） | 12個 | 68,719,476,736 バイト ＝ **64 GB** |
| **仮想マシン**（配られた部屋） | 12個 | 8,321,232,896 バイト ＝ **約 7.75 GB** |

**64GB のうち 7.75GB だけが部屋に配られている。** これが大家さん（仮想化ソフト）の仕事。しかも部屋には `docker-desktop` という**名前が付いている**。にせものとはいえ、**名前と持ち物を持った1台のパソコン**として存在している。

> 🐧 **`sysctl`**: パソコン本体の設定値を読むコマンド。`hw.ncpu` は CPU の個数、`hw.memsize` はメモリの総量。`hw` は hardware（ハードウェア＝部品）の略。

**その3: コンテナ** — 上半分だけ運ぶ（3-2 の答えの図）

カーネルを運ばない。下にある1つを借りる。

**ビルド時かランタイム時か**: 「何を運ぶか」は**イメージを作るとき**（ビルド時）に決まる。「どのカーネルを借りるか」は**動かすとき**（ランタイム時）に決まる。⏭️ **後で回収**: 作る側の話は P2 で扱う。

**あなたが知っているものでいうと**: 対応するものは無い。ただし考え方が近いのは **CDN のキャッシュ**。同じものを何度も配らず、**近くにある1つを共同で使う**。カーネルも同じで、コンテナごとに持たずに1つを共同で使う。

**失敗するとどうなるか**: 借りる相手のカーネルが**合わないと動かない**。Linux 用に作ったコンテナは、Linux のカーネルの上でしか動かない。⏭️ **後で回収**: では macOS ではどうなっているのか。**それが次回 P0-4**。

**なぜ「借りる」という設計にしたのか**: カーネルは**起動に時間がかかる**部品。パソコンの電源を入れてから使えるようになるまで待たされるのは、主にここ。**毎回これをやり直さずに済ませたい**から借りる。代わりに「カーネルを選べない」という制約を受け入れた。

---

### 3-4. 解説 — なぜこうするか

3つを並べると、**何を捨てて何を取ったか**が見える。

| | 上半分をアプリごとに分けられるか | カーネルを選べるか | 動き始めの速さ |
| --- | --- | --- | --- |
| bare metal | ✗ できない（1組を共用） | ✗（そのパソコンのもの） | — |
| 仮想マシン | ○ できる | **○ 選べる** | **遅い**（カーネルを起動する） |
| **コンテナ** | **○ できる** | **✗ 選べない** | **速い**（起動しない） |

コンテナは**「カーネルを選ぶ自由」を捨てて、「速さ」を取った**。それだけのこと。魔法ではない。

> 🧠 **Docker の考え方**: コンテナの長所も短所も、**「下半分を借りている」の一言から出てくる**。速いのも、軽いのも、カーネルを選べないのも、全部これ。迷ったらここに戻る。

### 3-5. 🔮 予測 → 動作確認

**🔮 先に予想する**

> アプリを **10個**動かしたいとする。
> 1. **仮想マシン**でやると、カーネルはいくつ動くか
> 2. **コンテナ**でやると、カーネルはいくつ動くか

<details><summary>答え</summary>

1. **11個**。ゲスト OS のカーネルが10個 ＋ もともとのパソコンのカーネルが1個
2. **1個**。もともとのパソコンのカーネル1個を、10個で共同利用する

「起動が速い」の正体がこれ。**10回ぶんの起動をしなくて済む。**

</details>

**動作確認**: この「借りている1つのカーネル」を、次回 P0-4 で**実際に表示させる**。あなたの Mac の中で、**カーネルが2つ見つかる**ことになる。

### 3-6. ✅ 想起チェック

何も見ずに答えてみる。

1. コンテナが運ぶのは OS のどちら側か。運ばないほうはどうするか
2. 仮想マシンとコンテナで、決定的に違う1点は何か
3. コンテナで「カーネルを選べない」のはなぜか
4. 「仮想マシン」と「仮想化ソフト」の違いを、大家さんのたとえで言うと

<details><summary>答え</summary>

1. **上半分を運ぶ。** 下半分（カーネル）は**運ばずに、下にある1つを借りる**
2. **カーネルを持ち込むかどうか。** 仮想マシンは持ち込む（だから選べるが遅い）、コンテナは借りる（だから選べないが速い）
3. **借り物だから。** 自分で持っていないものは、自分では替えられない
4. **仮想化ソフト = 大家さん**（部屋を作って電気や水道を配る係、1人）。**仮想マシン = 部屋**（実際に住む場所、何室でも作れる）

</details>

### 3-7. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `仮想マシン` | 3-3 で仕組み解剖。「作られたにせもののパソコン」＝**部屋**。実物の名前が `docker-desktop` であることまで確認した |
| `ゲスト OS` | 3-3 で仕組み解剖。「お客さんとして持ち込まれた OS」。主人＝ホストと対になる言葉。⏭️ `ホスト` は **P0-4 で回収** |
| `仮想化ソフト` | 3-3 で仕組み解剖。「作る係」＝**大家さん**。仮想マシン（＝部屋）との違いを実物で確認した |
| `sysctl`（🐧） | 3-3 で注釈。Mac 本体の部品を調べるコマンド |
| `du`（🐧） | 3-1 で注釈。P5-1 でイメージの大きさを測るときに再訪 |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌───────────────────────────────────────────────┐
│ P0-3  3つの運び方                             │
│                                               │
│ 覚えること（1つ）                             │
│   コンテナは OS の上半分だけ運び、            │
│   下半分（カーネル）は借りる                  │
│                                               │
│ 3つの運び方                                   │
│   bare metal … 何も運ばない。直に入れる       │
│                 上半分は1組しかない           │
│   仮想マシン  … 上半分も下半分も丸ごと運ぶ    │
│                 カーネルが増える → 重い       │
│   コンテナ    … 上半分だけ運ぶ                │
│                 カーネルは1つを借りる → 軽い  │
│                                               │
│ 自分で確かめたこと                            │
│   OS の上半分は 266MB（/usr/bin だけで 80MB） │
│   → これが「運ぶ荷物」の実物大                │
│                                               │
│ 借りるから起きること                          │
│   ○ 起動が速い（土台を作り直さない）          │
│   × カーネルは選べない（借り物だから）        │
└───────────────────────────────────────────────┘
```

---

▶ **次**: `M1: P0 ステップ4`（下へ続く）

---

## P0-4: Mac の中で Linux が動いている

**この回で覚えること（1つだけ）**

> **Mac には Linux のカーネルが無い。だから Docker は Linux の仮想マシンを1つ用意して、そのカーネルを貸している。**

**重要度**: ⚪ 背景知識 — これを知らないと、この先「ホストから見ると」と書かれるたびに、どこを指しているのか分からなくなる
**❓ この回の問い**: P0-3 の答えは「上半分を運び、**下半分は借りる**」だった。→ **では、その借りる相手はどこにいるのか？**
**前ステップとの接続**: P0-3 で「コンテナはカーネルを**借りる**」と決めた。**では、その借りる相手はどこにいるのか。** それが今回

### 4-0. このステップの初出トークン

**Docker**: `ホスト`
**その他**: `ゲスト` / `darwin`

新しい言葉は**3つだけ**。手は3回動かす。

---

### 4-1. 実践①: カーネルを2つ見つける（今回の山場）

P0-2 で、このパソコンのカーネルを調べた。`Darwin 25.6.0` だった。
P0-3 で、コンテナは**カーネルを借りる**と学んだ。

ここで大きな矛盾がある。

> **Linux 用に作られたコンテナは、Linux のカーネルを借りたい。**
> **でも、この Mac のカーネルは `Darwin` であって、`Linux` ではない。**
> **では、いったい何を借りているのか。**

**🔮 先に予想する**

> 1台のパソコンで動いているカーネルは、**1つだけ**のはず。P0-2 でそう学んだ。
> では、Docker が借りているカーネルを表示させたら、**`Darwin 25.6.0` と出る**と思うか。

まず Mac 本体のカーネルを、もう一度出す。

```bash
printf 'Mac 本体のカーネル : %s %s\n' "$(uname -s)" "$(uname -r)"
```

```
Mac 本体のカーネル : Darwin 25.6.0
```

次に、Docker が使っているカーネルを出す。

```bash
printf 'Docker のカーネル  : Linux %s\n' "$(docker info --format '{{.KernelVersion}}')"
```

```
Docker のカーネル  : Linux 6.12.76-linuxkit
```

✅ 検証済み: Docker Desktop 4.81.0 / macOS 26.6.2

**答え合わせ**: **別物だった。**

| | 名前 | 版 |
| --- | --- | --- |
| Mac 本体のカーネル | **Darwin** | 25.6.0 |
| Docker が使うカーネル | **Linux** | 6.12.76-linuxkit |

**1台のパソコンの中に、カーネルが2つある。** これは P0-2 で学んだ「1台に1つ」と食い違って見える。矛盾ではない。**パソコンが2台あるから、カーネルも2つある。** 1台は本物の Mac、もう1台は P0-3 で見た仮想マシン `docker-desktop`。

> 🧒 **かみくだくと**: `linuxkit`（リナックスキット）という名前は、**Docker 専用に作られた小さな Linux** という意味。普通の Linux をそのまま入れるのではなく、**コンテナを動かすのに必要な分だけ**にした特別あつらえのもの。

---

### 4-2. 実践②: `docker` コマンドはどちら側にいるのか

カーネルが2つあると分かった。では、あなたが打つ `docker` というコマンドは、**どちらのパソコンのプログラム**なのか。

**🔮 先に予想する**

> `docker` は Linux のカーネルを使う道具。ということは、**仮想マシンの中にあるプログラム**だと思うか。それとも Mac 側か。

```bash
docker version --format 'Client: {{.Client.Os}}/{{.Client.Arch}}
Server: {{.Server.Os}}/{{.Server.Arch}}'
```

```
Client: darwin/arm64
Server: linux/arm64
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: **割れた。** `Client`（あなたが打つ側）は `darwin`、`Server`（実際に動かす側）は `linux`。

つまり `docker` コマンドそのものは **Mac のプログラム**。打つと、**仮想マシンの中にいる相手にお願いを届けている**。

> 🧒 **かみくだくと**: `darwin`（ダーウィン）は **macOS のカーネルの名前**。P0-2 で `uname -s` を打ったとき `Darwin` と出たのと同じもの。ここで「`docker` は Mac 側のプログラムだ」と分かる。

⏭️ **後で回収**: 「お願いを届けている」と書いたが、**誰にどうやって届けているのか**は **P0-5** で扱う。

---

### 4-3. 🔬 仕組み解剖: ホストとゲスト

| 部品 | 正式名称 | かみくだくと | 何を指すか |
| --- | --- | --- | --- |
| ホスト | host | **主人**。泊める側 | コンテナを**動かしている側**のパソコン |
| ゲスト | guest | **お客さん**。泊まる側 | 持ち込まれた側。P0-3 の「ゲスト OS」の `ゲスト` はこれ |

`ホスト` と `ゲスト` は**対になった言葉**。旅館の主人と泊まり客の関係。どちらか片方だけでは意味が決まらない。

**全体図**

```
┌──────────────────────────────────────────────────────┐
│ ホスト（主人）= この Mac                             │
│                                                      │
│ カーネル: Darwin 25.6.0                              │
│ docker コマンド自身 = Client: darwin/arm64           │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ ゲスト（お客さん）= 仮想マシン「docker-desktop」 │ │
│ │                                                  │ │
│ │ カーネル: Linux 6.12.76-linuxkit                 │ │
│ │ コンテナを動かす側 = Server: linux/arm64         │ │
│ │                                                  │ │
│ │ ┌──────────┐ ┌──────────┐                        │ │
│ │ │ コンテナ │ │ コンテナ │                        │ │
│ │ └──────────┘ └──────────┘                        │ │
│ │ ↑ この2つが借りているのは Linux のカーネル       │ │
│ │    （外側の Darwin ではない）                    │ │
│ └──────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
```

**読み方**: 外側の箱が **ホスト（この Mac）**、内側の箱が **ゲスト（仮想マシン）**。コンテナは**いちばん内側**にいて、**すぐ外にある Linux のカーネル**を借りている。**外側の Darwin は借りていない。**

> 📖 **この教材での約束**: macOS では「コンテナの外側」が2段になっているので、**「ホスト」がどちらを指すのか毎回ぶれる**。そこでこう決める。
> **「ホスト」と書いたら、断りがない限り「コンテナのすぐ外側」＝ Linux の仮想マシン**のこと。
> **Mac そのもの**を指すときは「**この Mac**」と書く。
> Linux のパソコンで Docker を使う場合は、この2段が1つに重なるので迷わない。

**あなたが知っているものでいうと**: 近いのは **iframe**（アイフレーム）。Web ページの中に別のページを埋め込む仕組み。外側のページ（ホスト）の中で、内側のページ（ゲスト）が動いている。内側から見える世界は、外側とは別。

**失敗するとどうなるか**: 「ホスト側のファイルを見る」といった説明を、**Mac 側だと思って探すと見つからない**。P1-4 でコンテナのプロセス一覧を見るとき、**Mac のターミナルで `ps` を打っても何も出てこない**。見えるのは仮想マシンの中から見たときだけ。

**なぜこういう作りなのか**: Apple が macOS に Linux のカーネルを入れることはない。かといってコンテナを諦めるわけにもいかない。**Linux のカーネルが必要なら、Linux を1台用意すればいい**という解決。**仮想マシンを1つだけ立てて、コンテナは何個でもその中に並べる**ので、仮想マシンの重さを払うのは最初の1回だけで済む。

> 🏢 **実務メモ**: Docker Desktop が Mac の中で Linux の仮想マシンを動かすのは、公式に書かれた仕様であって裏技ではない。その仮想マシンを作る「大家さん」（P0-3 の仮想化ソフト）を選べるようにもなっている。
> 根拠: https://docs.docker.com/desktop/features/vmm/

### 4-4. 解説 — なぜこうするか

ここまでの4回が、1本の線でつながる。

1. アプリが動くかは**コードの下**で決まる（P0-1）
2. その「下」は **OS の上半分と下半分**に分かれる（P0-2）
3. コンテナは**上半分を運び、下半分は借りる**（P0-3）
4. **Mac には借りられる Linux の下半分が無い**。だから1台用意した（今回）

> 🧠 **Docker の考え方**: Mac で Docker を使うとき、**あなたは常に「Linux の中」を相手にしている**。うまくいかないときは「これは Mac の話か、仮想マシンの中の話か」を最初に分けると、原因の場所が半分に絞れる。

### 4-5. 🔮 予測 → 動作確認

**🔮 先に予想する**

> Docker が使っている OS の種類を出すと、**`darwin`** と出るか、**`linux`** と出るか。

```bash
docker info --format 'OSType: {{.OSType}}'
```

```
OSType: linux
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: `linux`。あなたは macOS を使っているのに、Docker から見た世界は **`linux`**。

この1行が、今回の話を全部まとめている。**Docker を触っているとき、相手は Linux。**

### 4-6. ✅ 想起チェック

何も見ずに答えてみる。

1. この Mac には、カーネルがいくつ動いているか。それぞれの名前は
2. `docker` コマンドは、Mac 側と仮想マシン側のどちらのプログラムか
3. この教材で「ホスト」と書いてあったら、どこを指すか

<details><summary>答え</summary>

1. **2つ。** Mac 本体の `Darwin 25.6.0` と、仮想マシンの `Linux 6.12.76-linuxkit`。パソコンが2台（本物1台 + にせもの1台）あるので、カーネルも2つ
2. **Mac 側。** `docker version` の `Client` が `darwin` と出ることで分かる。打つと仮想マシンの中の相手にお願いが届く
3. **コンテナのすぐ外側 ＝ Linux の仮想マシン。** Mac そのものを指すときは「この Mac」と書く約束

</details>

### 4-7. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `ホスト` | 4-3 で仕組み解剖。「主人＝泊める側」。**この教材では Linux の仮想マシンを指す**と約束した。P1-4・P3-2 で繰り返し使う |
| `ゲスト` | 4-3 で仕組み解剖。「お客さん＝泊まる側」。P0-3 の `ゲスト OS` の `ゲスト` を回収した |
| `darwin` | 4-2 で説明。macOS のカーネルの名前。P0-2 の `uname -s` の出力と同じもの |
| `docker info`（既出） | [`00-plan.md`](00-plan.md) §2 で役割を説明済み。今回は `--format` で項目を選んで表示した |

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌────────────────────────────────────────────────┐
│ P0-4  Mac の中で Linux が動いている            │
│                                                │
│ 覚えること（1つ）                              │
│   Mac に Linux のカーネルが無いから、          │
│   Docker は Linux の仮想マシンを1つ用意して    │
│   そのカーネルを貸している                     │
│                                                │
│ 自分で確かめたこと（1台に2つのカーネル）       │
│   uname -s -r        → Darwin 25.6.0           │
│   docker info の                               │
│   KernelVersion      → Linux 6.12.76-linuxkit  │
│                                                │
│   docker version                               │
│     Client: darwin/arm64  ← Mac 側のプログラム │
│     Server: linux/arm64   ← 仮想マシンの中     │
│                                                │
│ 言葉                                           │
│   ホスト = 主人  = 泊める側（この Mac）        │
│   ゲスト = お客さん = 泊まる側（仮想マシン）   │
│   darwin = macOS のカーネルの名前              │
│                                                │
│ この教材での約束                               │
│   「ホスト」= コンテナのすぐ外側               │
│            = Linux の仮想マシン                │
│   Mac 本体を指すときは「この Mac」と書く       │
└────────────────────────────────────────────────┘
```

---

▶ **次**: `M1: P0 ステップ5`（下へ続く）

---

## P0-5: Docker のアーキテクチャ

**この回で覚えること（1つだけ）**

> **`docker` と打っても、そのコマンド自身は何もしない。別のプログラムに頼んでいる。**

**重要度**: ⚪ 背景知識 — 「動かない」ときに、**どこに向かって調べればいいか**が変わる
**❓ この回の問い**: P0-4 の答えは「**Linux の仮想マシンを1つ用意して貸している**」だった。→ **では、そこに作ってと頼むのは誰なのか？**
**前ステップとの接続**: P0-4 で `docker` が Mac 側のプログラムだと分かり、「**仮想マシンの中の相手にお願いを届けている**」と書いて保留した。**その相手が誰なのか**が今回

### 5-0. このステップの初出トークン

**Docker**: `デーモン` / `docker system events`

新しい言葉は**2つだけ**。手は2回動かす。

---

### 5-1. 実践①: 相手の名前を確かめる

P0-4 で `docker version` を打ったとき、`Client` と `Server` の2つが出た。**`Server` とは誰のことなのか**を見る。

```bash
docker version --format 'あなたが打つ側 : docker {{.Client.Version}}
実際に動かす側 : Docker デーモン {{.Server.Version}}'
```

```
あなたが打つ側 : docker 29.6.1
実際に動かす側 : Docker デーモン 29.6.1
```

✅ 検証済み: Docker Desktop 4.81.0

**版は同じだが、別のプログラム。** `docker` コマンドは**お願いを送る係**、`Docker デーモン` は**実際に作る係**。

> 🧒 **かみくだくと**: **デーモン**（daemon）は「**ずっと裏で動き続けて、頼まれるのを待っているプログラム**」のこと。
> **お店の受付係**を思い浮かべるとよい。受付係はカウンターにずっと座っていて、お客さん（`docker` コマンド）が来たら注文を受け、**奥の厨房に指示を出す**。
> `docker` と打つあなたは**お客さん**。**自分で厨房には入らない。**

> 🐧 **なぜ「悪魔」と同じつづりなのか**: `daemon` は悪魔（demon）ではなく、ギリシャ神話の「**見えないところで働く精霊**」が語源。Linux では昔から、裏で動き続けるプログラムをこう呼ぶ。名前の最後に `d` を付ける習慣があり、Docker のデーモンも `dockerd`（ドッカーディー）という名前。

---

### 5-2. 実践②: 頼みごとを盗み聞きする（今回の山場）

受付係（デーモン）が**実際に何をしているか**を、のぞいてみる。

**🔮 先に予想する**

> `docker run --rm hello-world` と **1回**打つ。
> このとき、デーモンの側では**何個の出来事**が起きると思うか。1個か、3個か、10個か。

`docker system events`（ドッカー・システム・イベンツ）は、**デーモンが起こした出来事を一覧にする**コマンド。3行まとめて打つ。

```bash
START=$(date +%s)
docker run --rm hello-world > /dev/null
sleep 2
docker system events --since "$START" --until "$(date +%s)" --format '{{.Type}} {{.Action}}'
```

```
container create
container attach
network connect
container start
network disconnect
container die
container destroy
```

✅ 検証済み: Docker Desktop 4.81.0 / 2回実行して同じ結果

> 🐧 **3行の意味**: `START=$(date +%s)` は**いまの時刻を覚えておく**。`> /dev/null` は**画面に出さずに捨てる**（今回は出来事だけ見たいので）。`sleep 2` は**2秒待つ**。待たないと、最後の出来事が記録される前に一覧を取りに行ってしまう。

**答え合わせ**: **7個**だった。1個ではない。

| 順番 | 出来事 | かみくだくと |
| --- | --- | --- |
| 1 | `container create` | コンテナを**作った**（まだ動いていない） |
| 2 | `container attach` | 画面をつないだ（出力が見えるように） |
| 3 | `network connect` | ネットワークにつないだ |
| 4 | `container start` | **動かし始めた** |
| 5 | `network disconnect` | ネットワークを外した |
| 6 | `container die` | **プログラムが終わった** |
| 7 | `container destroy` | コンテナを**片づけた**（`--rm` を付けたため） |

**あなたが打ったのは1行。デーモンは7つの手順を踏んでいた。**

> 🧒 **かみくだくと**: レストランで「ハンバーグ1つ」と頼むのと同じ。注文は1回でも、厨房では**肉をこねる・焼く・盛りつける・運ぶ**と手順が分かれている。`docker run` も同じで、**「作る」と「動かす」は別の手順**。

⏭️ **後で回収**: `--rm` の意味、7の `destroy` が起きる条件、`create` と `start` を分けて打つ方法は **P1-1・P1-5** で扱う。今は「**1コマンドが1動作ではない**」とだけ分かればよい。

---

### 5-3. 🔬 仕組み解剖: お願いはどう届くのか

| 部品 | 正式名称 | かみくだくと | 何をしているか |
| --- | --- | --- | --- |
| `docker` コマンド | Docker CLI | **お客さん役**。注文を伝えるだけ | **この Mac の上**で動く。自分ではコンテナを作らない |
| Docker デーモン | Docker daemon（`dockerd`） | **受付係 兼 厨房**。実際に作る | **仮想マシンの中**で、ずっと動き続けている |

**全体の道すじ**

```
┌─────────────────────────────────────────┐
│ ホスト = この Mac                       │
│                                         │
│   あなた                                │
│     ↓  docker run と打つ                │
│   ① docker コマンド（Client）           │
│     ↓  お願いを送る                     │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ ゲスト = 仮想マシン docker-desktop  │ │
│ │                                     │ │
│ │         ↓  お願いが届く             │ │
│ │   ② Docker デーモン（dockerd）      │ │
│ │         ↓  「作れ」と指示           │ │
│ │   ③ Linux カーネル 6.12.76-linuxkit │ │
│ │         ↓  実際に作る               │ │
│ │      コンテナ（ただのプログラム）   │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

**読み方**: ①から③へ、上から下へお願いが降りていく。**あなたが触れるのは①だけ**。②と③は仮想マシンの中にいるので、直接は見えない。

<details><summary>もっと細かく見ると（読まなくてもよい）</summary>

②と③のあいだには、さらに小さな係が2つ入っている。`docker version` を打つと名前が出る。

```
containerd  v2.2.5   … コンテナの面倒を見る係
runc        1.3.6    … カーネルに「作って」と頼む係
```

✅ 検証済み: Docker Desktop 4.81.0

デーモンが全部を1人でやっているのではなく、**仕事が細かく分かれている**。ただし**今この違いを覚える必要はない**。「デーモンの下にもう少し係がいる」とだけ知っておけば十分。

</details>

**ビルド時かランタイム時か**: ここで見たのは全部**ランタイム時**（動かすとき）の話。イメージを**作るとき**にも同じくデーモンが働くが、それは P2 で扱う。

**あなたが知っているものでいうと**: **フロントエンドとバックエンド**の関係そのもの。React の画面（`docker` コマンド）がボタンを押すと、API サーバ（デーモン）にリクエストが飛び、**実際の処理はサーバ側**で起きる。画面側は結果を受け取って表示するだけ。

**失敗するとどうなるか**: デーモンが動いていないと、`docker` と打った瞬間に `Cannot connect to the Docker daemon` と出る。これは「**受付係がいません**」という意味。**あなたのコマンドが悪いのではなく、相手が不在**。Docker Desktop を起動すれば直る。

**なぜ2つに分けてあるのか**: 分けておくと、**離れた場所のデーモンにも同じコマンドで頼める**から。手元の `docker` コマンドから、別のサーバにあるデーモンに注文する、ということができる。⏭️ **後で回収**: 実際にやるのは **P6-5**（任意）。

### 5-4. 解説 — なぜこうするか

この区別は、**うまくいかないときに効く**。

「動かない」とき、原因の場所は大きく2つに分かれる。

| どちらの問題か | 見分け方 |
| --- | --- |
| **お願いが届いていない** | `docker` と打っても `Cannot connect...` と出る |
| **届いたが、中で失敗した** | コマンドは通るが、コンテナが起動しない・すぐ落ちる |

前者は Docker Desktop の問題。後者はあなたの書いた設定の問題。**この切り分けができるだけで、調べる範囲が半分になる。**

> 🧠 **Docker の考え方**: `docker` コマンドは**注文票**であって、料理人ではない。エラーを読むときは「**これは注文票の書き方の問題か、厨房の中の問題か**」をまず分ける。

### 5-5. 🔮 予測 → 動作確認

**🔮 先に予想する**

> いま動いているコンテナは0個のはず（`--rm` で片づけたので）。
> では、デーモン自身は**まだ動いている**と思うか。それとも仕事が終わったので止まっているか。

```bash
docker info --format 'いま動いているコンテナ : {{.Containers}} 個
デーモンは応答するか   : 応答あり（このメッセージが出ている時点で）'
```

```
いま動いているコンテナ : 0 個
デーモンは応答するか   : 応答あり（このメッセージが出ている時点で）
```

✅ 検証済み: Docker Desktop 4.81.0

**答え合わせ**: コンテナは0個。でも**デーモンは動き続けている**。この返事が返ってきたこと自体が証拠。

これが「デーモン」という言葉の意味。**仕事が無くても、頼まれるのを待ってずっと起きている。**

### 5-6. ✅ 想起チェック

何も見ずに答えてみる。

1. `docker run` と打ったとき、コンテナを実際に作っているのは誰か
2. 「デーモン」とはどういうプログラムか。1文で
3. `docker run --rm hello-world` を1回打つと、出来事はいくつ起きたか。それは何を意味するか

<details><summary>答え</summary>

1. **Docker デーモン**（`dockerd`）。`docker` コマンドは**お願いを送っただけ**。しかもデーモンは**仮想マシンの中**にいる
2. **ずっと裏で動き続けて、頼まれるのを待っているプログラム。** お店の受付係のようなもの
3. **7個**（create → attach → network connect → start → network disconnect → die → destroy）。**1コマンドが1動作ではない**こと、デーモンが順番に手を動かしていることを意味する

</details>

### 5-7. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `デーモン` | 5-1・5-3 で仕組み解剖。「裏で待っている受付係」＝ `dockerd`。P1-3 でログを見るとき、P4-3 で起動順を見るときに再訪 |
| `docker system events` | 5-2 で使用。デーモンの出来事を一覧にする。P1-5・P4-5 で「何が起きたか分からない」ときの道具として再訪 |
| `docker run`（⏭️） | 5-2 で**打っただけ**。オプションと挙動は **P1-1 で回収** |
| `--rm`（⏭️） | 5-2 の7番目の出来事の原因。**P1-5 で回収** |
| `containerd` / `runc` | 5-3 の折りたたみで名前だけ。**この教材ではこれ以上扱わない**（[`00-plan.md`](00-plan.md) §7 の範囲外）|

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌─────────────────────────────────────────────────┐
│ P0-5  Docker のアーキテクチャ                   │
│                                                 │
│ 覚えること（1つ）                               │
│   docker と打っても、自分では何もしない。       │
│   別のプログラム（デーモン）に頼んでいる        │
│                                                 │
│ 頼みごとの道すじ                                │
│   ① docker コマンド … Mac 側。お願いを送るだけ  │
│   ② デーモン       … 仮想マシンの中。実際に作る │
│   ③ カーネル       … 実際にプログラムを起こす   │
│                                                 │
│ 自分で確かめたこと                              │
│   docker run --rm hello-world と1回打つと       │
│   イベントが 7 個出た                           │
│     create → attach → network connect           │
│     → start → network disconnect                │
│     → die → destroy                             │
│                                                 │
│   1コマンド = 1動作 ではない。                  │
│   デーモンが順番に手を動かしている              │
│                                                 │
│ デーモンとは                                    │
│   ずっと裏で動き続けて、頼まれるのを            │
│   待っているプログラム（受付係）                │
└─────────────────────────────────────────────────┘
```

---

▶ **次**: `M1: P0 ステップ6`（下へ続く）

---

## P0-6: 用語の地図（P0 のまとめ）

**この回で覚えること（1つだけ）**

> **P0 の5回は、バラバラの話ではなく「1本の問いの連なり」だった。**

**重要度**: 🔴 毎日使う — ここで出た言葉は、この先すべてのステップに出てくる。**読めないと、エラーメッセージも公式ドキュメントも読めない**
**❓ この回の問い**: P0-5 の答えは「**デーモンが作る**」だった。→ **では、ここまでの5つの答えは、どうつながっているのか？**
**前ステップとの接続**: P0-1 から P0-5 までの**総まとめ**。新しいことは学ばない

### 6-0. このステップの初出トークン

**新しい言葉はない。** これまでに出た言葉を並べ直すだけ。そのかわり、**自分の手で書く**。

---

### 6-1. 1本の線につなぐ

P0 の5回は、**前の回の答えが次の回の問いになる**形でつながっていた。

```
┌───────────────────────────────────────────────────┐
│ P0-1  アプリが動くかは「コードの下」で決まる      │
│           │  では、その「下」とは何か             │
│           ↓                                       │
│ P0-2  OS ＝ 上半分（道具）＋ 下半分（カーネル）   │
│           │  では、どこまで運べばよいか           │
│           ↓                                       │
│ P0-3  コンテナ ＝ 上半分を運び、下半分は借りる    │
│           │  では、借りる相手はどこにいるのか     │
│           ↓                                       │
│ P0-4  Mac に Linux の下半分が無い                 │
│       → Linux の仮想マシンを1つ用意して貸している │
│           │  では、誰に頼んで作ってもらうのか     │
│           ↓                                       │
│ P0-5  docker コマンドではなく デーモン が作る     │
│           │                                       │
│           ↓                                       │
│ P1    ここから、実際にコンテナを動かす            │
└───────────────────────────────────────────────────┘
```

**読み方**: 左の縦線をたどると、**ずっと同じことを聞き続けている**のが分かる。「動かないのはなぜか」→「では下とは何か」→「では何を運ぶのか」→「では借りる相手は」→「では誰が作るのか」。

> 🧒 **かみくだくと**: Docker の話は、**1つの困りごとから始まった1本道**。
> 困りごと ＝ 「**自分のパソコンでは動くのに、他では動かない**」。
> Docker のあらゆる機能は、これを解くためにある。迷ったらここに戻る。

**1行でまとめると**

> **コンテナとは、OS の上半分を持ち運び、下半分は借りて動く、ただのプログラム。**

この1文が言えれば P0 は終わり。

---

### 6-2. 実践: 用語集を自分の言葉で埋める（今回の手を動かす）

**ここが今回のいちばん大事なところ。** 読んで分かった気になるのと、書けるのは別。

[`docs/01-glossary.md`](01-glossary.md) を開く。**A・B・C・F の表と、「紛らわしい対」の表が空いている。**

**やり方**

1. **何も見ずに**、書ける行から埋める
2. 手が止まった行は、表の右端「詳しい解説」に書いてある場所を読み返す
3. 読み返したら、**教材を閉じてから**書く。写さない

**埋める列**

- **🧒 かみくだくと** … 専門用語を使わずに書く。たとえでよい
- **1行の意味** … その言葉を知らない人に、1文で言える形

**どのくらい書けたら合格か**

| 埋まった数 | 判断 |
| --- | --- |
| 12個中 **9個以上** を何も見ずに書けた | ✅ P1 に進んでよい |
| **5〜8個** | もう一度、書けなかった行の「詳しい解説」だけ読み返す |
| **4個以下** | 各回の `📇 まとめカード` を上から読み直す。**本文は読まなくてよい** |

<details><summary>書いたあとで開く（解答例。自分の言葉と違ってよい）</summary>

**A. コンテナの実体**

| 用語 | 🧒 かみくだくと | 1行の意味 |
| --- | --- | --- |
| コンテナ | 荷物だけ持って引っ越した部屋 | OS の上半分を持ち運び、下半分は借りて動くプログラム |
| ホスト | 引っ越し先の建物（主人） | コンテナを動かしている側。この教材では Linux の仮想マシンを指す |
| ゲスト | 泊まり客 | 持ち込まれた側。仮想マシンや、その中の OS |
| デーモン | お店の受付係 | 裏でずっと動き続け、頼まれるのを待っているプログラム |

**B. OS まわり**

| 用語 | 🧒 かみくだくと | 1行の意味 |
| --- | --- | --- |
| OS | 家一式（土台＋内装） | カーネルと、その周りの道具をまとめた呼び名 |
| カーネル | パソコンの管理人さん | OS の中心。アプリの頼みごとを実際に処理する部分 |
| OS の上半分 | 内装・家具・道具 | コマンドや道具箱。**運べる** |
| OS の下半分 | 家の土台・水道・電気 | カーネル。**運べないので借りる** |
| darwin | macOS の土台の名前 | macOS のカーネルの名前。`uname -s` で出る |

**C. 仮想化**

| 用語 | 🧒 かみくだくと | 1行の意味 |
| --- | --- | --- |
| 仮想マシン | 大家さんが作った部屋 | ソフトで作ったにせもののパソコン。いくつでも作れる |
| 仮想化ソフト | 大家さん | 部屋を作って資源を配る係。1つだけ |
| ゲスト OS | 持ち込んだ家一式 | 仮想マシンの中の OS。**カーネルまで含む** |

**F. Linux 側**

| 用語 | 🧒 かみくだくと | 1行の意味 |
| --- | --- | --- |
| シンボリックリンク | ショートカット | 本体ではなく、本体への道しるべになっているファイル |
| `/bin` | 非常持ち出し袋 | 起動に要る基本コマンドの置き場所。置き場所は他にもある |

**紛らわしい対**

| 対 | 何が違うか |
| --- | --- |
| OS / カーネル | カーネルは OS の一部（下半分）。並んでいるのではなく入れ子 |
| 仮想マシン / 仮想化ソフト | 作られた部屋 と 作る大家さん |
| ホスト / ゲスト | 泊める側 と 泊まる側 |
| `docker` コマンド / デーモン | 注文する側（Mac） と 実際に作る側（仮想マシンの中） |

</details>

---

### 6-3. まだ白紙の言葉

P0 では**わざと触れなかった**言葉がある。使う場面が来てから学ぶほうが残るため。

| 言葉 | たとえでいうと | いつ埋めるか |
| --- | --- | --- |
| **イメージ** | 荷造りして封をした段ボール | **P1-2** |
| **レジストリ** | 段ボールの置き場・配送センター | **P1-2** |
| **レイヤ** | 荷物の積み方 | **P2-3** |
| **タグ** | 箱に貼るラベル | **P5-5** |

用語集の **D の表**が空いているのはこのため。**空欄のまま進んでよい。**

### 6-4. ✅ 想起チェック（P0 の総合）

何も見ずに答えてみる。**ここが P0 の卒業試験。**

1. 「自分のパソコンでは動くのに」が起きる原因は、どこにあるか
2. コンテナが運ぶものと、運ばずに借りるものは、それぞれ何か
3. なぜ Mac では仮想マシンが必要なのか
4. `docker run` と打ったとき、コンテナを実際に作るのは誰か
5. コンテナを1行で説明すると

<details><summary>答え</summary>

1. **コードの下の層。** OS の種類・CPU の種類・言語のバージョン・道具箱。**どれもコードには書かれていないので Git で運べない**
2. **運ぶのは OS の上半分**（コマンドや道具箱）。**借りるのは下半分**（カーネル）
3. **Mac のカーネルは Darwin で、Linux ではないから。** 借りる相手がいないので、Linux の仮想マシンを1つ立てて貸している
4. **Docker デーモン**（`dockerd`）。`docker` コマンドはお願いを送っただけ。デーモンは仮想マシンの中にいる
5. **OS の上半分を持ち運び、下半分は借りて動く、ただのプログラム**

</details>

**5問中4問以上**を何も見ずに答えられたら、P1 に進んでよい。

### 6-5. 初出トークンの回収確認（P0 全体）

新しい言葉は出していない。**P0 全体で宣言した「後で回収」の約束**を確認する。

| 約束したこと | どこで宣言 | 状態 |
| --- | --- | --- |
| 「土台」の正体を明かす | P0-1 1-4 | ✅ **回収済み**（P0-2 2-4。正体はカーネル）|
| `コンテナ` が何を運ぶ方法なのか | P0-1 1-3 | ✅ **回収済み**（P0-3 3-3。上半分を運び下半分を借りる）|
| macOS では何を借りているのか | P0-3 3-3 | ✅ **回収済み**（P0-4 4-1。Linux の仮想マシンのカーネル）|
| お願いを誰にどう届けているのか | P0-4 4-2 | ✅ **回収済み**（P0-5 5-3。デーモンに届く）|
| `docker run` のオプションと挙動 | P0-5 5-2 | ⏭️ **P1-1 で回収予定** |
| `--rm` の意味 | P0-5 5-2 | ⏭️ **P1-5 で回収予定** |
| イメージを**作る**側の話 | P0-3 3-3 | ⏭️ **P2 で回収予定** |
| 離れたデーモンに頼む方法 | P0-5 5-3 | ⏭️ **P6-5 で回収予定（任意）** |

**P0 の中で閉じるべき約束は、すべて閉じた。** 残り4つは P1 以降に持ち越す。

---

### 📇 まとめカード（翌日はここだけ見返す）

```
┌───────────────────────────────────────────────────┐
│ P0-6  用語の地図（P0 のまとめ）                   │
│                                                   │
│ 覚えること（1つ）                                 │
│   P0 の5回は、1本の問いの連なりだった             │
│                                                   │
│ 1行でいうと                                       │
│   コンテナ = OS の上半分を運び、                  │
│              下半分は借りて動く、ただのプログラム │
│                                                   │
│   Mac では借りる相手が無いので、                  │
│   Linux の仮想マシンを1つ立てて貸している         │
│                                                   │
│   作るのは docker コマンドではなく デーモン       │
│                                                   │
│ まだ白紙の言葉（P1 以降で埋める）                 │
│   イメージ   … 箱の中身      → P1-2               │
│   レジストリ … 箱の置き場    → P1-2               │
│   レイヤ     … 荷物の積み方  → P2-3               │
│   タグ       … 箱のラベル    → P5-5               │
│                                                   │
│ 用語集 docs/01-glossary.md を                     │
│ 自分の言葉で埋めたら P0 は終わり                  │
└───────────────────────────────────────────────────┘
```

---

## 🎓 P0 修了

ここまでで、**コンテナが何であるかを、自分の言葉で説明できる**状態になった。

| 回 | 覚えたこと1つ | 手を動かしたこと |
| --- | --- | --- |
| P0-1 | 動くかは「コードの下」で決まる | `file` で `Mach-O arm64` を確認 |
| P0-2 | カーネルは OS の一部（下半分） | 版番号の食い違いを確認 |
| P0-3 | コンテナは下半分を借りる | 上半分の重さ 266MB を実測 |
| P0-4 | Mac には借りる相手が無い | **1台にカーネルが2つ**あることを確認 |
| P0-5 | 作るのはデーモン | **1コマンドで7つの出来事**が起きるのを観察 |
| P0-6 | 5回は1本の問いの連なり | 用語集を自分の言葉で作成 |

**P0 には M2（フェーズ末パック）は無い。** 再現すべき設定ファイルがまだ無いため。次から本番。

---

▶ **次**: `M1: P1 ステップ1` — **既存イメージを動かす**（ここから実際にコンテナを動かす。nginx を起動して、ホストから見える形を確認する）
