# P0. 基礎 — なぜコンテナか / アーキテクチャ / 用語

手を動かす前に、言葉と全体像を作るフェーズ。全3ステップ。
計画は [`docs/00-plan.md`](00-plan.md) §4 を参照。

---

## P0-1: なぜコンテナが必要か

**作ること / 確かめること**: 「自分のパソコンでは動くのに、人のパソコンでは動かない」が**どこで起きるのか**を、3つのやり方を比べて自分の言葉で言えるようにする
**重要度**: ⚪ 背景知識 — ここを分かっていないと、「なぜイメージの中に OS のファイルが入っているのか」が最後まで分からないままになる
**前ステップとの接続**: 最初のステップ。[`docs/00-plan.md`](00-plan.md) §2 で確認した環境だけを前提にする

### 1-0. このステップの初出トークン

**Docker**: `コンテナ` `イメージ` `ホスト`（`コンテナ`と`イメージ`は**名前を置くだけ**。関係を整理するのは P0-3）
**Linux**: OS とカーネルの違い / システムコール / 共有ライブラリ
**その他**: 仮想マシン / ゲスト OS / 仮想化ソフト（仮想マシンの用語。1-1 で説明する）

### 1-1. 実践

まず、自分のパソコンを見るところから始める。実行する場所はどこでもよい。

```bash
sw_vers -productVersion   # macOS のバージョン
uname -m                  # CPU の種類
python3 --version         # この Mac に入っている Python
```

```
26.6.2
arm64
Python 3.14.3
```

✅ 検証済み: macOS 26.6.2 / arm64 / Docker Desktop 4.81.0

この3行が、あなたのコードが立っている**地面**。そして**この3行はコードのどこにも書かれていない**。書かれていないから、Git にも入らない。だから**隣の人のパソコンの地面が違えば、同じコードでも動きが変わる**。これが「自分の環境では動く」の正体。

#### この教材ぜんたいで使うたとえ: 引っ越し

アプリを別のパソコンで動かすことを、**引っ越し**にたとえる。この先ずっとこのたとえを使う。

- **家** … パソコン。土台・水道・電気がついている
- **荷物** … あなたが作ったアプリと、それが使う道具一式
- **困ること** … 引っ越し先に水道が無かったら、荷物だけ運んでも生活できない

運ぶ範囲が違う3つのやり方がある。

| | たとえて言うと | 実際にやっていること |
| --- | --- | --- |
| **bare metal**（ベアメタル） | 引っ越し先で**家を一から建てる** | パソコンに直接インストールする |
| **VM**（仮想マシン） | **家ごと運ぶ**。土台も水道も電気も全部 | OS をまるごと持っていく |
| **コンテナ** | 土台と水道は**引っ越し先のものを借りて**、荷物だけ運ぶ | 一番下の部分は借りて、その上だけ運ぶ |

#### まず: OS とカーネルは違う

ここを分けておかないと、この先ずっと混乱する。**カーネルは OS の一部**。

```
┌──────────────────────────┐
│ コマンドや道具（上半分） │
│    カーネル（下半分）    │
└──────────────────────────┘
```

> 🧒 **かみくだくと**:
> **カーネル** = OS の**中心部分だけ**。「パソコンの管理人さん」。
> **OS（基本ソフト）** = **カーネル ＋ その周りの道具**（コマンド、道具箱、画面を出す部分など）をまとめた呼び名。
> 家でいうと、**カーネルが土台・水道・電気**で、**OS はそれに内装と家具と道具を足した家一式**。

この先は「**OS の下半分＝カーネル**」「**OS の上半分＝その周りの道具**」と呼び分ける。

#### 図1: アプリを2つ動かすと、違いがはっきりする

**仮想マシンの場合** — アプリごとに OS を丸ごと持ち込む（カーネルまで持ってくる）

```
┌────────────────┐ ┌────────────────┐
│    アプリ A    │ │    アプリ B    │
│  OS の上半分   │ │  OS の上半分   │
│    カーネル    │ │    カーネル    │
└────────────────┘ └────────────────┘
┌───────────────────────────────────┐
│           仮想化ソフト            │
│       ホスト OS のカーネル        │
│             パソコン              │
└───────────────────────────────────┘
```

ゲスト OS は**カーネルまで含んだ一式**。だからアプリが2つあると、**カーネルも2つ**になる。ここが重い原因。

> 🧒 **かみくだくと**: この図に出てくる3つの言葉。
> **仮想**（かそう） = 「ソフトウェアで作った、にせものの」という意味。にせものだが本物と同じように動く。
> **仮想マシン** = ソフトウェアで作った**にせもののパソコン**。本物のパソコンの中に、もう1台パソコンがあるように見せかけたもの。
> **仮想化ソフト** = そのにせもののパソコンを**作る係**のソフト。

> 🧒 **かみくだくと**: **ゲスト**は「**お客さん**」という意味。旅館の**主人（ホスト）** と**泊まり客（ゲスト）** と同じ関係。
> 仮想マシンでは、**もとからあるパソコン側がホスト**、**その中に持ち込まれた OS がゲスト**。「人の家に、自分の家を丸ごと持ち込んで住んでいる」状態。
> だから **ゲスト OS = お客さんとして持ち込まれた OS**。ホストについては 1-2 でくわしく扱う。

**コンテナの場合** — 運ぶのは OS の上半分だけ。カーネルは下の1つを借りる

```
┌────────────────┐ ┌────────────────┐
│    アプリ A    │ │    アプリ B    │
│  OS の上半分   │ │  OS の上半分   │
└────────────────┘ └────────────────┘
┌───────────────────────────────────┐
│       ホスト OS のカーネル        │
│             パソコン              │
└───────────────────────────────────┘
```

コンテナの箱に**カーネルが入っていない**ことに注目してほしい。アプリが2つに増えても、**カーネルは下に1つだけ**。増えたのは「アプリ + OS の上半分」だけ。

**これが「なぜイメージの中に OS のファイルが入っているのか」の答え。** 入っているのは OS の**上半分だけ**で、下半分（カーネル）は入っていない。

> 🧒 **かみくだくと**: コンテナが速いのは、**家を建て直さないから**。借りられるものは借りて、自分の荷物だけ置く。そのかわり、**土台は選べない**。借り物だから。

この「借りている土台」が、次に出てくる**カーネル**。

| | bare metal | VM | **コンテナ** |
| --- | --- | --- | --- |
| **何を運ぶか** | 何も運ばない | OS まるごと（土台ごと） | 荷物だけ。**土台は運ばない** |
| **土台（カーネル）** | そのパソコンのもの | 自分で持ってきて**新しく起動する** | **引っ越し先のものを借りる** |
| **動き始めるまで** | 起動という考え方がない | 秒〜分（家を建てる時間がかかる） | ミリ秒〜秒（荷物を置くだけ） |
| **まわりとの仕切り** | 無い。全部が同じ部屋 | 厚い。家が別なので完全に別世界 | 中くらい。**土台は共有**なので、家ほど厚くない |
| **選べないもの** | — | — | **土台（カーネル）と CPU の種類** |

一番下の行が、この表でいちばん大事。**土台を借りているので、土台だけは選べない。** この決まりごとは、P1 から先で何度も形を変えて出てくる。

### 1-2. 🔬 仕組み解剖: 「コンテナ」とは何か

| 部品 | 正式名称 | かみくだくと | 実行するときに何が起きるか |
| --- | --- | --- | --- |
| コンテナ | container | **箱を開けて、生活を始めた部屋** | **動かすとき**の話。パソコンの管理人さん（カーネル）が、あるプログラムを「**見える範囲を狭めた状態**」で動かす。**新しい OS は立ち上がらない**。動いているプログラムが1つ増えるだけ |
| イメージ | image | **荷造りして封をした段ボール** | **作るとき**の話。中身は後から変わらない。アプリ・道具・OS のファイルが入っている。⏭️ **後で回収**: 中身の作りは P0-3、積み方は P2-3。今は「コンテナのもとになる箱」とだけ分かればよい |
| ホスト | host | **引っ越し先の建物** | コンテナを動かしている側のパソコン。コンテナから見た「外側」 |

> 📖 **ホストについての注意**: macOS では外側が**2段**になっている。**この Mac** → その中の **Linux の仮想マシン**（Docker Desktop が勝手に1つ立てている）→ コンテナ、という順。

**なぜ2段も必要なのか** — Mac と Linux は**別の OS** だから。

> 🧒 **かみくだくと**: **Mac（macOS）と Linux は、別の OS**。ただし**親戚**のような関係で、**上半分はよく似ている**。だから `ls` や `cd` のような同じコマンドが両方で使える。違うのは**下半分（カーネル）**のほう。

| | macOS | Linux |
| --- | --- | --- |
| **OS の上半分**（コマンドや道具） | `ls` `cd` `ps` … | `ls` `cd` `ps` … **よく似ている** |
| **OS の下半分**（カーネル） | **Darwin / XNU** という別物 | **Linux** カーネル |
| 生まれ | どちらも **Unix** という古い OS の子孫。だから似ている | 同左 |

ここで 図1b を思い出してほしい。**コンテナは OS の下半分（カーネル）を借りて動く**。ということは…

1. Linux 用に作られたコンテナは、**Linux のカーネルを借りたい**
2. でも Mac のカーネルは Darwin であって、Linux ではない
3. **借りる相手がいない**
4. だから Docker Desktop が、Mac の中に **Linux の仮想マシンを1つ用意して、貸し出す係にする**

> 🏢 **実務メモ**: Docker Desktop が Mac の中で Linux の仮想マシンを動かしているのは、公式に書かれている仕様であって裏技ではない。「コンテナを動かすための Linux 仮想マシン」を管理するソフトを選べるようにもなっている。
> 根拠: https://docs.docker.com/desktop/features/vmm/

1-6 で出てくる `Client: darwin/arm64` の **`darwin`（ダーウィン）は、macOS の中心部分の名前**。`docker` コマンドが Mac 側のプログラムであることを、この1語が表している。

#### 図2: macOS では外側が2段ある

```
┌─────────────────────────────────────────────────────┐
│ この Mac（macOS 26.6.2 / arm64）                    │
│ docker コマンド自身 = Client: darwin/arm64          │
│                                                     │
│ ┌─────────────────────────────────────────────────┐ │
│ │ Linux の仮想マシン（Docker Desktop が用意する） │ │
│ │ コンテナを動かす側 = Server: linux/arm64        │ │
│ │                                                 │ │
│ │ ┌──────────┐ ┌──────────┐                       │ │
│ │ │ コンテナ │ │ コンテナ │                       │ │
│ │ └──────────┘ └──────────┘                       │ │
│ │                                                 │ │
│ │ 土台（Linux カーネル）                          │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

この図は 1-6 で実際にコマンドを打って確かめる。
> この教材で「**ホスト**」と書いたら、ことわりが無いかぎり**コンテナのすぐ外側、つまり Linux の仮想マシン**のこと。Mac そのものを指すときは「**この Mac**」と書く。
> なお、Linux のパソコンで Docker を使う場合は、この2段が1つに重なる。

**あなたが知っているものでいうと**: いちばん近いのは **Python の `venv`**。`venv` は「どの Python の部品が見えるか」を切り替える道具。コンテナも考え方は同じで、**切り替える範囲がもっと広い**。動いているプログラムの一覧、ファイル置き場、通信、ユーザ、そこまで全部を切り替える。ただし**土台（カーネル）だけは切り替えられない**。ここが `venv` との違い。

**外から見るとどう見えるか**: コンテナの中で動いているプログラムは、**外から普通のプログラムとして見える**。仮想マシンのように中身が隠れたりしない。ただし macOS の場合、見えるのは Linux の仮想マシンの中から見たときだけ。**Mac のターミナルでプログラム一覧を出しても、コンテナの中身は出てこない**。⏭️ **後で回収**: 実際に並べて見るのは P1-4。

**失敗するとどうなるか**: CPU の種類が合わない箱は動かない。

> 🧒 **かみくだくと**: `arm64` と `amd64` は **CPU の種類の名前**。CPU は計算をする部品で、種類が違うと**言葉が違う**。`arm64` 向けに書かれた命令は、`amd64` の CPU には通じない。日本語の説明書をフランス語しか読めない人に渡すようなもの。Apple の M シリーズのチップは `arm64`、少し前の Mac や多くの Windows パソコンは `amd64`。

この Mac は `arm64`。別の種類（`amd64`）専用に作られた箱を開けようとすると、`exec format error`（形式が違う、という意味）と出るか、無理やり翻訳しながら動くので極端に遅くなる。⏭️ **後で回収**: 対処のしかたは P5-2。

**なぜこういう作りなのか**: 仮想マシンは家ごと複製する。だから仕切りは厚いが、そのたびに土台から作り直すので重い。コンテナは**一番重い土台を借りてしまって、その上だけ入れ替える**。仕切りの厚さを捨てて、速さを取った作り。**「仮想マシンの軽い版」ではなく、捨てたものが違う別のやり方**。

### 1-3. 🐧 Linux注

> 🐧 **カーネル**: パソコンの**管理人さん**。「ファイルを開きたい」「メモリがほしい」というアプリの頼みごとを、代わりに実行してくれる。この頼み方のことを**システムコール**と呼ぶ。

> 🐧 **共有ライブラリ**: みんなで使う**道具箱**。**OS の上半分**に入っている。アプリ本体の中には無く、動かすときに借りに行く。**引っ越し先に道具箱が無ければ動かない**。コンテナが OS の上半分ごと運ぶのは、これを一緒に持っていくため。

### 1-4. 解説 — なぜこうするか

「自分のパソコンでは動く」の原因は、コードではない。**コードの下にある地面**のほう。

1-1 で見た3行を、もう一度見てほしい。このうち **Python のバージョンと道具箱（共有ライブラリ）は、OS の上半分に入っているので箱に詰めて運べる**。運べないのは **CPU の種類と、OS の下半分（カーネル）** だけ。

つまりコンテナとは、**Git では運べない「書かれていない前提」を、運べる形にしたもの**。

> 🧠 **Docker の考え方**: コンテナは「軽い仮想マシン」ではなく、**まわりが見えないようにしただけの、普通のプログラム**。外から見えることも、動き始めが速いことも、土台を選べないことも、全部この1つのことから出てくる。

### 1-5. ⚠️ アンチパターン

> ⚠️ **やってはいけない理解のしかた**: コンテナを「軽い仮想マシン」だと思うこと。
> こう考えると、2つの勘違いがついてくる。**「コンテナごとに OS を選べる」**と**「外の世界とは完全に別」**。どちらも違う。土台を借りている以上、**Linux 用の土台の上でしか動かない**し、CPU の種類も引っ越し先に合わせるしかない。仕切りも仮想マシンより薄い。
> P3 でファイルの持ち主が外の世界とつながっていて驚くことになるが、その原因もここにある。
> 根拠: https://docs.docker.com/get-started/docker-overview/

### 1-6. 🔮 予測 → 動作確認

あなたが使っているのは **macOS**。でもコンテナは **Linux の土台**の上でしか動かない。

**答えを見る前に、自分で予想してみる**

1. macOS に Linux の土台は入っていない。では、なぜこの Mac でコンテナが動いているのか
2. `docker version` は「命令する側」と「実際に動かす側」の情報を両方出す。**この2つの OS の名前は同じになるか、ばらけるか**。ばらけるなら、どちらが `linux` になるか

**動作確認**

```bash
docker version --format 'Client: {{.Client.Os}}/{{.Client.Arch}}
Server: {{.Server.Os}}/{{.Server.Arch}}'
```

**こう出るはず**

```
Client: darwin/arm64
Server: linux/arm64
```

✅ 検証済み: Docker Desktop 4.81.0 / macOS 26.6.2

`darwin` は macOS のこと。つまり **`docker` という命令そのものは Mac のプログラム**。でも、**実際にコンテナを動かしている側は `linux`** と出ている。

これが **1-2 の図2** の正体。Docker Desktop が Mac の中に Linux の仮想マシンをこっそり1つ立てて、その中でコンテナを動かしている。**Mac の上のコンテナは、仮想マシンの中のコンテナ**。図2 をもう一度見ると、`Client` と `Server` がどの箱のことを指していたかが分かる。

CPU の種類がどちらも `arm64` でそろっているのは、**土台を借りている以上ずらせない**から。

⏭️ **後で回収**: この仕組みを図にするのは **P0-2**。

### 1-7. ✅ 想起チェック

1. コンテナが仮想マシンより速く動き始めるのはなぜか。1文で
2. コンテナで「選べないもの」は何か。2つ
3. `venv` とコンテナは、何が違うか

<details><summary>答え</summary>

1. **土台（カーネル）を作り直さないから。** 引っ越し先のものを借りて、荷物を置くだけで済む
2. **土台（カーネル）と CPU の種類。** 借り物なので、こちらから替えられない
3. `venv` が切り替えるのは **Python の部品だけ**。コンテナは**プログラム一覧・ファイル置き場・通信・ユーザ**まで切り替える。ただし**土台は切り替えない**

</details>

### 1-8. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `コンテナ` | 1-2 で仕組み解剖。「まわりが見えないようにした、普通のプログラム」として確定 |
| `ホスト` | 1-2 で定義。macOS では「この Mac」と「Linux の仮想マシン」の2段になる点も明示。図にするのは P0-2 |
| `イメージ` | ⏭️ **P0-3 で回収**（他の言葉との関係を整理する）。積み方は P2-3。今は「荷造りして封をした段ボール」とだけ分かればよい |
| 仮想マシン / 仮想化ソフト | 1-1 で注釈。「ソフトで作ったにせもののパソコン」と「それを作る係」 |
| ゲスト OS | 1-1 で注釈。「お客さんとして持ち込まれた OS」。ホストと対になる言葉 |
| OS とカーネルの違い（🐧） | 1-1 の図0 で説明。**カーネルは OS の一部**（下半分）。上半分はコマンドや道具箱 |
| Mac と Linux の違い | 1-2 で説明。**別の OS だが親戚**。上半分は似ていて、下半分（カーネル）が違う |
| `darwin` | 1-2 で説明。macOS の中心部分の名前。1-6 の `docker version` の出力で実物を見る |
| カーネル（🐧） | 1-1 と 1-3 で注釈。「パソコンの管理人さん」＝ OS の下半分。P0-2 で図の一番下として再登場 |
| システムコール（🐧） | 1-3 で注釈。「管理人さんへの頼み方」。これ以上は掘らない |
| 共有ライブラリ（🐧） | 1-3 で注釈。「みんなで使う道具箱」。P2-3 で「なぜ `slim` と `alpine` で動きが変わるか」として再訪 |

---

▶ **次**: `M1: P0 ステップ2` — Docker のアーキテクチャ（`docker` と打ってからコンテナが動き出すまで、誰が何をしているのか）
