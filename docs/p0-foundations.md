# P0. 基礎 — なぜコンテナか / アーキテクチャ / 用語

手を動かす前に、語彙と全体像を作るフェーズ。全3ステップ。
計画は [`docs/00-plan.md`](00-plan.md) §4 を参照。

---

## P0-1: なぜコンテナが必要か

**作ること / 確かめること**: 「自分の環境では動く」が**どの層**で壊れるのかを、bare metal / VM / コンテナの3つで比較して言語化する
**重要度**: ⚪ 背景知識 — ここの区別が無いと「なぜイメージの中に OS のファイルが入っているのか」が最後まで腑に落ちない
**前ステップとの接続**: 最初のステップ。`docs/00-plan.md` §2 で確認した環境だけを前提にする

### 1-0. このステップの初出トークン

**Docker**: `コンテナ` `イメージ`（この2語は**名前を置くだけ**。関係の整理は P0-3）
**Linux**: カーネル / システムコール / 共有ライブラリ

### 1-1. 実践

コードが**何に依存して動いているか**を3つ確認する。実行ディレクトリはどこでもよい。

```bash
sw_vers -productVersion   # macOS のバージョン
uname -m                  # CPU アーキテクチャ
python3 --version         # ホストに入っている Python
```

```
26.6.2
arm64
Python 3.14.3
```

✅ 検証済み: macOS 26.6.2 / arm64 / Docker Desktop 4.81.0

この3行が、コードが乗っている土台。**どれもコードには書かれていないので Git では運べない。** 「自分の環境では動く」はここから生まれる。

この土台のどこまでを一緒に運ぶか。運ぶ範囲が違う3つを並べる。

| | bare metal | VM | **コンテナ** |
| --- | --- | --- | --- |
| **何を運ぶか** | 何も。マシンに直接入れる | OS まるごと（カーネル込み） | アプリ + 依存 + OS のファイル群。**カーネルは運ばない** |
| **カーネル** | そのマシンのもの | ゲスト OS のものを**別に起動する** | **ホストのものを共有する** |
| **起動時間** | 起動の概念がない | 秒〜分（OS の起動が走る） | ミリ秒〜秒（プロセスが1つ増えるだけ） |
| **隔離の強さ** | 無し | 強い。カーネルが別なので境界が厚い | 中間。**カーネルは共有**なので VM ほど厚くない |
| **選べないもの** | — | — | **カーネルと CPU アーキテクチャ** |

最後の行が要点。カーネルを運ばないので**カーネルだけは選べない**。この制約は P1 以降、形を変えて何度も出てくる。

### 1-2. 🔬 仕組み解剖: 「コンテナ」とは何か

| 部品 | 正式名称 | 実行時に何が起きるか |
| --- | --- | --- |
| コンテナ | container | **ランタイム時**。ホストのカーネルが、あるプロセスを「見える範囲を制限した状態」で起動する。**新しい OS は起動しない**。プロセスが1つ増えるだけ |
| イメージ | image | **ビルド時**に作られる読み取り専用のファイル群。アプリ・依存・OS のファイルが入っている。⏭️ **後で回収**: 中身の構造は P0-3、レイヤの積み方は P2-3。今は「コンテナの材料」とだけ理解する |

**既知スタックとの対応**: 近いのは **Python の `venv`**。`venv` が切り替えるのは「どのパッケージが見えるか」だけ。コンテナは同じ発想を **OS の層まで広げ**、プロセス一覧・ファイルシステム・ネットワーク・ユーザまで切り替える。ただし**カーネルは切り替えられない**。

**ホストからどう見えるか**: コンテナ内のプロセスは**ホストの `ps` にも普通に見える**。VM のようなブラックボックスにはならない。⏭️ **後で回収**: 実際に並べるのは P1-4。

**失敗したらどうなるか**: アーキテクチャが合わないイメージは動かない。このマシンは `arm64` なので、`amd64` 専用のイメージは `exec format error` になるか、エミュレーションで極端に遅くなる。⏭️ **後で回収**: 対処は P5-2。

**なぜこの設計なのか**: VM は OS ごと複製するので隔離は強いが、起動のたびにカーネルの初期化からやり直す。コンテナは**最も重いカーネルを共有し、その上の層だけ差し替える**。隔離の厚みと引き換えに速度を取った設計で、「VM の軽い版」ではなく**別のものを捨てた別物**。

### 1-3. 🐧 Linux注

> 🐧 **カーネル**: OS の中核。ファイルを開く・メモリを確保するといった要求を、アプリに代わって処理する。アプリはこれを**システムコール**で頼む。

> 🐧 **共有ライブラリ**: `libc` など多数のプログラムが共通で使う部品。実行時に読み込まれるため、**ホストに無い・版が違うと動かない**。イメージが OS のファイル群を抱えるのは、ここを一緒に運ぶため。

### 1-4. 解説 — なぜこうするか

原因はコードではなく**コードの下の層**にある。1-1 の3行のうち Python の版と共有ライブラリは**イメージ側に固定でき**、CPU アーキテクチャとカーネルだけがホストに残る。コンテナとは、Git で運べないこの前提を**運べる形にした**もの。

> 🧠 **Docker の考え方**: コンテナは「軽い仮想マシン」ではなく**隔離されたプロセス**。ホストの `ps` に見えることも、起動が速いことも、カーネルを選べないことも、全部この1つの事実から出ている。

### 1-5. ⚠️ アンチパターン

> ⚠️ **アンチパターン**: コンテナを「軽量な仮想マシン」と理解すること。
> この比喩には「OS を選べる」「ホストとは別世界」という2つの誤解が付いてくる。実際は**Linux カーネルの上でしか動かず**、ホストと同じアーキテクチャに縛られ、隔離も VM より薄い。P3 の bind mount で所有者がホストと地続きになって驚く原因もここ。
> 根拠: https://docs.docker.com/get-started/docker-overview/

### 1-6. 🔮 予測 → 動作確認

このマシンは **macOS**。一方コンテナは **Linux カーネル**の機能を使う。

**予測してから進む**

1. macOS に Linux カーネルは無い。ではなぜこのマシンで Linux コンテナが動くのか
2. `docker version` が出すクライアントとサーバの OS 表記は**一致するか、割れるか**。割れるならどちらが `linux` か

**動作確認**

```bash
docker version --format 'Client: {{.Client.Os}}/{{.Client.Arch}}
Server: {{.Server.Os}}/{{.Server.Arch}}'
```

**期待される出力**

```
Client: darwin/arm64
Server: linux/arm64
```

✅ 検証済み: Docker Desktop 4.81.0 / macOS 26.6.2

`docker` 自身（Client）は macOS のプログラム。だが実際に動かす側（Server）は **`linux`** — Docker Desktop が Linux VM を1つ立て、その中でコンテナを動かしているため。**macOS のコンテナは「VM の中のコンテナ」**。両方が `arm64` なのは、カーネルを共有する以上ずらせないから。

⏭️ **後で回収**: この VM とデーモンの関係を図にするのは **P0-2**。

### 1-7. ✅ 想起チェック

1. コンテナが VM より速く起動するのはなぜか。1文で
2. コンテナで「選べないもの」は何か。2つ
3. `venv` とコンテナは、隔離する範囲がどう違うか

<details><summary>答え</summary>

1. **新しい OS（カーネル）を起動しないから。** プロセスが1つ増えるだけで済む
2. **カーネルと CPU アーキテクチャ。** 共有している以上、コンテナ側から差し替えられない
3. `venv` は **Python パッケージだけ**。コンテナは**プロセス一覧・ファイルシステム・ネットワーク・ユーザ**まで隔離する。ただしカーネルは隔離しない

</details>

### 1-8. 初出トークンの回収確認

| トークン | 扱い |
| --- | --- |
| `コンテナ` | 1-2 で仕組み解剖。「隔離されたプロセス」として確定 |
| `イメージ` | ⏭️ **P0-3 で回収**（他の用語との関係を整理）。レイヤ構造は P2-3。今は「コンテナの材料になる読み取り専用のファイル群」とだけ理解する |
| カーネル（🐧） | 1-3 で注釈。P0-2 でアーキテクチャ図の最下層として再登場 |
| システムコール（🐧） | 1-3 で注釈。これ以上は掘らない |
| 共有ライブラリ（🐧） | 1-3 で注釈。P2-3 で「なぜ `slim` と `alpine` で挙動が変わるか」として再訪 |

---

▶ **次**: `M1: P0 ステップ2` — Docker のアーキテクチャ（`docker` と打ってからコンテナが生まれるまで、誰が何をするか）
