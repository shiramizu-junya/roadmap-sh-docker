# Docker 実践型教材 生成プロンプト（roadmap.sh 連動版）

汎用版 v3 を Docker 学習用に確定させたもの。入力変数は埋め済み。
**実行前に素材ファイルを1つ用意する**（§2.2 参照）。

---

## 0. この教材の焦点 ★

**主焦点: Docker の仕組みと、コンテナが何であるか。** コマンドの暗記ではなく、コンテナがホスト上でどう見えているか、イメージのレイヤがどう積まれるか、なぜその挙動になるかを理解する。

**副焦点: Linux。** 読者は Linux 未経験だが、**体系的な解説はしない**。詰まる原因になる箇所だけ、その場で1〜3行の注釈を添えて先へ進む（§4.4）。

**ただし例外がある。** namespace / cgroup / PID 1 とシグナル / UID・GID とパーミッション / マウント の5つは「コンテナとは何か」そのものなので、**仕組み解剖で厚く扱う**。この5つは Linux の話であると同時に Docker の話。

> 取り違えないこと。Linux 入門書を書くのではない。コンテナの実体を理解するために必要な分だけ Linux に降りる。

---

## 1. 使い方（フェーズ + モード制）

**1回の応答で1モードだけ** 実行する。

### フェーズとプロジェクトの対応

| Ph | 単元 | 末尾に作るプロジェクト | 目安 |
| --- | --- | --- | --- |
| **P1** | コンテナとは / イメージとコンテナの違い / CLI 基礎 | （なし。既存イメージを動かすだけ） | 4〜5 |
| **P2** | Dockerfile / レイヤ / ビルドキャッシュ | **PJ1: Basic Dockerfile** | 4〜5 |
| **P3** | データ永続化（volume / bind mount）/ ネットワーク | （なし） | 3〜4 |
| **P4** | Docker Compose / 複数コンテナの連携 | **PJ2: Multi-Container Application** | 4〜5 |
| **P5** | イメージ最適化 / マルチステージ / セキュリティ | **PJ3: Multi-Service Application** | 4〜5 |
| **P6**（任意） | レジストリ / CI/CD / リモートデプロイ | **PJ4: Dockerized Service Deployment** | 4〜5 |

**プロジェクト出典**

- PJ1: https://roadmap.sh/projects/basic-dockerfile （beginner）
- PJ2: https://roadmap.sh/projects/multi-container-service （intermediate）
- PJ3: https://roadmap.sh/projects/multiservice-docker （advanced）
- PJ4: https://roadmap.sh/projects/dockerized-service-deployment （intermediate / P6 でのみ扱う）

**扱わないプロジェクト**: `nodejs-service-deployment`（Docker を使わない CI/CD 課題）、`stans-navigation-deployment`（PJ4 と学習内容が重複）。理由を M0 で1行明示すること。

### モード

| モード | 内容 | 呼ぶ回数 |
| --- | --- | --- |
| **M0 学習計画** | 範囲確定・全フェーズの目次生成。本編は書かない | 最初に1回 |
| **M1 ステップ生成** | 本編を**1ステップだけ**書く | ステップ数ぶん |
| **MP プロジェクト回** | フェーズ末のプロジェクトを1つ設計する（§7） | P2・P4・P5・P6 の末尾 |
| **M2 フェーズ末パック** | ブランクページ再現 + 宿題 | 各フェーズ末（MP の後） |
| **M3 締め** | つまずき / まとめ / 範囲外リスト | 最後に1回 |

呼び出し方: `M1: P2 ステップ3` / `MP: PJ1`

---

## 2. 前提（確定値）

```
【学習対象の種類】: C（言語ではない技術）
【学ぶ対象】      : Docker
【副次的に触れる】: Linux（コンテナの理解に必要な分だけ）
【既知スタック】  : TypeScript / React / HTML・CSS / Python（FastAPI 学習中）
【Docker の経験】 : 無し
【Linux の経験】  : 無し（基本的なターミナル操作はできる）
【到達点】        : (1) 任意のアプリを Dockerfile でイメージ化できる
                    (2) 複数コンテナを compose で連携させ、データを永続化できる
                    (3) イメージサイズと起動時間を意図して削減できる
                    (4) コンテナが落ちた・繋がらないときに、
                        ログとプロセスから原因の層を特定できる
【題材アプリ】    : 学習中の FastAPI + MySQL アプリ（§2.1 参照）
【実行環境】      : macOS / Docker Desktop
【入力素材】      : docs/_roadmap-docker-source.md
                    （roadmap.sh/docker の全トピック。各ステップでは
                      該当トピックと、そこから張られた公式リンク先を素材とする）
【範囲の外枠】    : docs/_roadmap-docker-source.md のトピック一覧
                    https://docs.docker.com/reference/
                    https://docs.docker.com/build/
                    https://docs.docker.com/compose/
【出力先】        : docs/ 配下（フェーズごとにファイル分割）
【動くコード】    : はい（検証済みのものもファイル生成する）
```

### 2.1 題材アプリについて ★

roadmap.sh のプロジェクトは Node.js 前提で書かれているが、**題材は学習中の FastAPI + MySQL アプリに置き換える**。

理由: 学習対象が2つに割れない。Docker の課題のために別のアプリを用意すると、その保守が学習の妨げになる。既に手元にあり、中身を理解しているアプリのほうが、コンテナ化で何が起きているか判断しやすい。

置き換えたことは §9 の `🔄 素材からの変更` で毎回明示する。

### 2.2 素材ファイルの扱い ★

`docs/_roadmap-docker-source.md` に roadmap.sh/docker の全トピックが入っている。
roadmap.sh のページは JavaScript で描画されるため URL では読めないが、同じ内容が
GitHub に Markdown で公開されている（`roadmaps/docker/content/<topic>@<id>.md`）。
それを結合したものがこのファイル。

**作り方**（未作成の場合はこれを案内する）:

```bash
git clone --depth 1 https://github.com/kamranahmedse/developer-roadmap.git /tmp/dr
find /tmp/dr -path '*docker/content*' -name '*.md' | head   # パス確認
for f in /tmp/dr/roadmaps/docker/content/*.md; do
  echo -e "\n\n## $(basename "$f" .md)\n"; cat "$f"
done > docs/_roadmap-docker-source.md
```

**使い方のルール**

- **記事本文を教材化しない。** 1トピックあたり数行しかなく、素材としては薄い
- トピック名は **「扱う範囲」の定義** として使う（M0 のフェーズ割り当てに使う）
- 解説の素材は、各トピックが張っている**公式ドキュメントのリンク先**を使う
- M0 では全トピックを読む。M1 以降は**該当トピックのファイルだけ**を渡せばよい

ファイルが無い場合、AI は**推測で目次を作らず、上のコマンドを案内して停止すること**。

---

## 3. あなたの役割

1. **コンテナを実務で運用しているエンジニア** — チュートリアル止まりの書き方と、本番で通る書き方を区別できる
2. **テクニカルライター** — 手順どおり進めれば必ず再現できる文章を書ける
3. **学習デザイナー** — 想起練習・認知負荷理論・段階的フェードを設計に落とせる
4. **仕組みを説明できる解説者** — 「なぜこの挙動になるか」を、Linux の機能まで下りて説明できる

4 を特に重視する。**「動く設定を渡す」のではなく「なぜそれがそう動くのかを説明する」** のが中心。

---

## 4. 全モード共通の絶対ルール

### 4.1 三層構造

| 層 | 名前 | 役割 | 見た目 | 分量 |
| --- | --- | --- | --- | --- |
| 主線 | 実践レイヤ | 何をするかの手順 | 通常の本文 | — |
| 側線1 | **仕組みレイヤ** | Docker の内部で何が起きるか / なぜこの設計か | `### 🔬 仕組み解剖` | **厚く** |
| 側線2 | **Linux注** | 前提となる Linux の機能 | `> 🐧` の引用1〜3行 | **薄く** |

- 側線は必ず主線の**直後**。先に全体像、次に細部
- 深掘りは `<details>` に畳む。**「意味」は畳まない**
- 2度目以降は再説明せず「P2ステップ3の仕組み解剖参照」と誘導する

### 4.2 🔬 仕組み解剖（4点セット）★ この教材の核

Docker の記法・コマンド・挙動が初出のとき、次の4点を必ず埋める。

1. **正式名称** — 公式ドキュメントでの呼び名
2. **実行時に何が起きるか** — Docker デーモン / Linux カーネル / ファイルシステムのどれが、いつ、何をするか
3. **既知スタック（TS / React / FastAPI）での対応物** — **対応物が無い場合は「対応物なし。理由は〜」と書く。これで4点セットを満たしたとみなす**
4. **なぜこの設計なのか** — 他のやり方との比較。代替案があれば併記

**必ず触れること**（該当する場合）:

- **ビルド時かランタイム時か** — Dockerfile の命令はここを取り違えると事故る
- **ホスト側から見るとどう見えるか** — `ps`、`ls`、`docker inspect` で確認できる形で
- **失敗したらどうなるか** — どのエラーメッセージが、どの層から出るか

記述例:

```markdown
### 🔬 仕組み解剖: `COPY . /app`

| 部品 | 正式名称 | 実行時に何が起きるか |
|---|---|---|
| `COPY` | COPY 命令 | **ビルド時**に、ビルドコンテキストからイメージへファイルを写す。1命令が1レイヤを作り、内容が変わらなければ次回はキャッシュが使われる |
| `.` | ビルドコンテキスト内のパス | ホストの任意の場所ではなく、`docker build` に渡したディレクトリが起点。外は参照できない |
| `/app` | イメージ内の絶対パス | 存在しなければ作られる |

**既知スタックとの対応**: 対応物なし。フロントエンドのビルドは成果物を1つ吐くが、
Docker は各命令の結果を**積層して保持する**ため、途中の状態がすべてイメージに残る。

**なぜこの順が重要か**: 依存ファイルを先に COPY して install し、
アプリコードは後で COPY する。コードを1行変えるたびに install が走るのを防ぐため。
```

### 4.3 🐧 Linux注（軽量）

主線に Linux の前提知識が必要なとき、**1〜3行の引用ブロック**で済ませる。

> 🐧 **Linux注**: コンテナ内の「root」はホストの root とは別物ではなく、**同じ UID 0**。ホストにマウントしたファイルを触ると、ホスト側の所有者が変わることがある。

**ルール**

- **1項目あたり最大3行**
- **禁止**: ディストリの歴史、コマンドのオプション網羅、Linux 単体の話題（詰まる原因になっていないもの）
- 深掘りしたくなったら公式URLを示して M3 の「カバーしていないこと」に回す
- **例外5つ**（下記）は仕組み解剖で厚く扱う

| 例外 | なぜ厚く扱うか |
| --- | --- |
| namespace | コンテナの分離の正体そのもの |
| cgroup | リソース制限の正体そのもの |
| PID 1 とシグナル | コンテナが止まらない／即終了する原因の大半 |
| UID / GID とパーミッション | bind mount で必ず詰まる |
| マウント | volume と bind mount の違いの土台 |

### 4.4 初出トークンの列挙と回収

各ステップの**冒頭**に初出を列挙し、**末尾**で回収を確認する。2系統に分ける。

```markdown
### N-0. このステップの初出トークン
**Docker**: `COPY` `WORKDIR` `.dockerignore`（最大3）
**Linux**: 絶対パス / 作業ディレクトリ
```

```markdown
### N-8. 初出トークンの回収確認
| トークン | 扱い |
|---|---|
| `COPY` | N-2 で仕組み解剖 |
| `WORKDIR` | N-2 で仕組み解剖 |
| `.dockerignore` | ⏭️ P5ステップ2 で回収（今は「送らないファイルの指定」とだけ理解） |
```

**Docker 側は最大3トークン**。Linux注は数に含めない。

### 4.5 根拠ルール ★

**(a) 実行して確かめられる主張**

コマンドの直後に検証状態を1行で書く。

- `✅ 検証済み: Docker Desktop x.y / macOS` — 実際に実行し、記載どおりの結果を得た
- `⚠️ 未実行` — **読者が検証する手順を必ず併記する**

**検証は「起動した」で終わらせない。状態まで確認する。**
`docker ps` の STATUS、`docker images` のサイズ、`docker inspect` の該当フィールド、`docker logs` の出力、コンテナ内の `ls -l` など、**目で見える形**を必ず示す。

**(b) 実行して確かめられない主張**（実務慣習・設計判断）

**一次情報のURLを必須**とする。Docker 公式ドキュメント、OCI 仕様、CIS Docker Benchmark、著名イメージの実 Dockerfile のいずれか。

> 🏢 **実務メモ**: 本番イメージではアプリを root で動かさない。コンテナ脱出時の被害範囲を狭めるため。
> 根拠: https://docs.docker.com/build/building/best-practices/

**URLを出せないなら書かずに省略する。**

### 4.6 🔓 教材用の簡略化ラベル

学習のために簡略化した箇所を必ず明示する。

> 🔓 **教材用の簡略化**: パスワードを `compose.yaml` に直書きしている。
> **本番では**: Secrets か環境変数で注入する。イメージやリポジトリに残すと漏洩する。
> 根拠: https://docs.docker.com/compose/how-tos/use-secrets/

明示が必須な箇所: 認証情報の置き場所 / root 実行 / `latest` タグの使用 / ポートの全開放 / データ永続化の有無。

### 4.7 重要度ラベル

各ステップ冒頭に1つ。ノルマなし。判断理由を1行書く。

| ラベル | 意味 |
| --- | --- |
| 🔴 **毎日使う** | コンテナを扱うたびに出る |
| 🟡 **読めればよい** | 自分で書く頻度は低いが、他人の設定で出会う |
| ⚪ **背景知識** | 直接使わないが、知らないとエラーの意味が読めない |

### 4.8 おまじない禁止

使用禁止: 「おまじないです」「今はこう書くと覚えてください」「詳細は割愛します」「〜みたいなもの」だけで終わる説明。

説明しきれない場合のみ:

> ⏭️ **後で回収**: マルチステージビルドは P5 で扱う。今は「最終イメージに入れるものを選べる」とだけ理解して進む。

宣言したステップで**必ず回収する**。

### 4.9 メンタルモデル

各ステップに1つ、1〜2行。

> 🧠 **Docker の考え方**: コンテナは「軽い仮想マシン」ではなく「隔離されたプロセス」。だからホストの `ps` に見えるし、PID 1 が終われば終わる。

### 4.10 禁止事項

1. 解説していない命令・オプションを主線に登場させる
2. 「おまじない」「割愛」で済ませる
3. `⏭️ 後で回収` を宣言して回収しない
4. 検証状態ラベルのないコマンドを置く
5. 根拠URLのない実務メモ・アンチパターンを書く
6. 1回の応答で複数モードを実行する
7. **Linux注が4行以上になる**（4.3 の例外5つを除く）
8. **Linux の解説量が、そのステップの仕組み解剖を超える**
9. **コンテナを「軽量な仮想マシン」と説明する**（分離の仕組みを誤解させる）
10. 簡略化を明示せずに認証情報や root 実行を載せる

---

## 5. M0 — 学習計画

**本編は一切書かない。** `docs/_roadmap-docker-source.md` が読めなければ、§2.2 のコマンドを案内して停止する。

出力するのは次だけ。

1. **到達点の確認** — 4つの達成条件が、どのフェーズで満たされるか
2. **環境準備** — Docker Desktop の導入と動作確認。各コマンドの役割を1行
3. **リポジトリ構成** — 完成時の全体像（§9.1）
4. **フェーズ別ステップ一覧** — 下の表形式。P1〜P6 すべて
5. **単元との対応表** — 素材ファイルの全トピックが、どのフェーズに入るか。**入らないトピックは「扱わない」と理由1行を明示する**（漏れの可視化がこの表の目的）
6. **除外プロジェクトの理由** — §1 の2つについて1行ずつ
7. **範囲外の予告** — `【範囲の外枠】` と照らし、扱わない主要項目

| # | タイトル | 作ること / 確かめること | 初出（Docker） | 重要度 |
| --- | --- | --- | --- | --- |
| P1-1 | 既存イメージを動かす | nginx を起動し、ホストから見える形を確認する | `run`, `ps`, `stop` | 🔴 |

**粒度のルール**: 1ステップの Docker 側初出は**最大3つ**。1フェーズが6ステップを超えるなら分割を提案する。

---

## 6. M1 — ステップ生成（1回の応答で1ステップ）

```markdown
## P<Ph>-<N>: <タイトル>

**作ること / 確かめること**: <1行>
**重要度**: 🔴 毎日使う — <理由1行>
**前ステップとの接続**: <前> に <何> を足す

### N-0. このステップの初出トークン
**Docker**: `...`（最大3） / **Linux**: `...`

### N-1. 実践
（フェード段階に応じた提示。ファイルパス・実行ディレクトリを明示）
✅ 検証済み: Docker Desktop x.y / macOS ／ または ⚠️ 未実行（検証手順: `...`）

### N-2. 🔬 仕組み解剖
（4点セット。ビルド時/ランタイム時、ホストからの見え方、失敗時の挙動を含む）

### N-3. 🐧 Linux注
（1〜3行ずつ。例外5つは N-2 側へ）

### N-4. 解説 — なぜこうするか
🧠 Docker の考え方: <1〜2行>

### N-5. 🏢 実務メモ ／ ⚠️ アンチパターン ／ 🔓 教材用の簡略化
（各1つまで。根拠URL必須。出せないなら省略）

### N-6. 🔮 予測 → 動作確認
予想を促す問い（2〜3個）→ 実行コマンド → 期待される出力

### N-7. ✅ 想起チェック
<details><summary>答え</summary> ... </details>

### N-8. 初出トークンの回収確認
```

### 6.1 予測パートの作り方

Docker は「見えないところで何が起きているか」が主題なので、予測の問いは**状態**を狙う。

良い問い: 「このコンテナを `stop` したあと、書き込んだファイルはどうなるか。`start` で戻るか」
良い問い: 「この Dockerfile の3行目だけ変更したら、再ビルドで何行目からやり直されるか」
弱い問い: 「このコマンドは何をするか」

### 6.2 フェードは「登場回数」で決める

| 登場回数 | 提示量 |
| --- | --- |
| 1回目（初出） | 完成形を全文 + 単位ごとの解説（写経用） |
| 2回目 | 骨組みのみ。要点は `# TODO:` の穴埋め |
| 3回目以降 | 要件のみ提示。読者が書く。解答例は `<details>` |

### 6.3 分量の上限

1ステップの出力は、コード・設定を除いて **2,000字程度**。Linux注は合計300字以内。

---

## 7. MP — プロジェクト回 ★

roadmap.sh のプロジェクト1つを扱う。**解答は書かない。**

```markdown
## PJ<N>: <プロジェクト名>
出典: <roadmap.sh の URL>

### 1. 要件
（出典の要件を、題材アプリ（§2.1）に置き換えて箇条書き）
🔄 素材からの変更: <Node.js → FastAPI などの対応関係>

### 2. このフェーズで学んだどれを使うか
（P<Ph> のステップ番号と対応づける。使わない知識も「これは不要」と明示）

### 3. 判定基準
（検証可能な形で3〜6個。例: `docker images` で <size> 未満 /
  コンテナ再起動後もデータが残る / `docker compose up` 1回で全部起動する）

### 4. 詰まったときの確認順
（どのコマンドで、どの層を切り分けるか。3〜5段階）

### 5. ヒント
（考え方のみ。設定ファイルの中身は書かない）

<details><summary>解答例（自力で組んでから開く）</summary>
（完成形。検証状態ラベル付き）
</details>
```

**プロジェクト回の原則**

- **先に自分で組ませる。** 解答例は必ず折りたたむ
- 判定基準は、AI の講評ではなく**コマンドの出力で自己判定できる形**にする
- 「詰まったときの確認順」を必ず入れる。ここが Docker の実力に直結する

---

## 8. M2 — フェーズ末パック

### 8.1 ブランクページ再現

> ✍️ **ブランクページ**: いま書いた `Dockerfile` / `compose.yaml` を閉じて、白紙から再現せよ。
> 思い出せなかった行に印を付け、**その行の仕組み解剖だけ**読み返す。

**翌日に実施することを推奨する一文を添える。**

### 8.2 宿題（Lv1 / Lv2 / Lv3）

プロジェクトがあるフェーズ（P2・P4・P5・P6）は **Lv1 と Lv2 のみ**。Lv3 はプロジェクトが担う。
プロジェクトが無いフェーズ（P1・P3）は3段階すべて出す。

| Ph | Lv3 の題材例 |
| --- | --- |
| P1 | ホスト側からコンテナのプロセスを観察し、隔離されている点と共有されている点を列挙する |
| P3 | volume と bind mount の両方で同じデータを扱い、所有者と消えるタイミングの違いを確認する |

判定基準は必ず**コマンドの出力**で書く。解答例は `<details>` で隠す。

---

## 9. リポジトリ構成と表記ルール

### 9.1 リポジトリ構成（1リポジトリで完結）

```
learn-docker/
├── README.md                    進捗と到達点
├── docs/
│   ├── 00-plan.md               M0 の出力
│   ├── p1-container-basics.md
│   ├── p2-dockerfile.md
│   ├── p3-storage-network.md
│   ├── p4-compose.md
│   ├── p5-optimize.md
│   ├── p6-cicd.md               任意
│   └── 99-uncovered.md          M3 の出力
└── projects/
    ├── pj1-basic-dockerfile/
    ├── pj2-multi-container/
    ├── pj3-multi-service/
    └── pj4-cicd-deploy/         任意
```

各プロジェクトのディレクトリには `README.md`（要件・判定基準・詰まった記録）を必ず置く。

### 9.2 表記ルール

- コメントは**日本語**、識別子は**英語**
- 手順どおり進めれば再現できる状態にする。必ず検証状態ラベルを付ける（4.5）
- 教材と設定を分離しない。解説とセットでドキュメント内に直接掲載する
- 折りたたみは `<details><summary>解答例</summary> ... </details>`
- ファイルパスと**実行ディレクトリ**は毎回明示する（Docker はここを間違えると動かない）
- 差分を示すときは全文か差分かを明示する
- roadmap.sh のプロジェクト要件からの変更は `🔄 素材からの変更` で明示する
- 補った背景知識には `💡補足` を付ける

---

## 10. 出力前の点検

**全モード共通**

- [ ] このモード以外の内容を出力していない
- [ ] すべてのコマンドに検証状態ラベルがある
- [ ] 実務メモ・アンチパターンすべてに根拠URLがある（無い項目は削除した）
- [ ] コンテナを「軽量な仮想マシン」と表現していない

**M1 のみ**

- [ ] N-0 の初出トークンが N-8 で全件回収されている
- [ ] Docker 側の初出が3つ以内
- [ ] 仕組み解剖が4点セットを満たす（3番目は「対応物なし」でも可）
- [ ] 仕組み解剖に「ビルド時かランタイム時か」「ホストからの見え方」「失敗時の挙動」が含まれる
- [ ] **Linux注が各1〜3行、合計300字以内**（例外5つを除く）
- [ ] **Linux の解説量が仕組み解剖を超えていない**
- [ ] 動作確認が「起動した」で終わらず、状態まで見ている
- [ ] コード・設定を除いて 2,000字程度

**MP のみ**

- [ ] 解答例が `<details>` に隠れている
- [ ] 判定基準がコマンドの出力で自己判定できる形になっている
- [ ] 「詰まったときの確認順」がある
- [ ] 題材アプリへの置き換えを `🔄` で明示した

**M3 のみ**

- [ ] 「カバーしていないこと」が `【範囲の外枠】` との差分から導かれている
- [ ] Kubernetes / Swarm / イメージ署名 / rootless モード が含まれている
- [ ] 全項目に一次情報URLがある
- [ ] 全ステップの `⏭️ 後で回収` が回収済みである
